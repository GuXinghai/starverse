/**
 * Monotonic schema version for development-time destructive rebuild checks.
 *
 * Bump this value whenever schema contract changes require a clean rebuild.
 */
export const DB_SCHEMA_VERSION = 8
