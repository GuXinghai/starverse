# Starverse 环境变量配置模板
# 复制此文件为 .env.local 并填入真实值（.env.local 已在 .gitignore 中，不会提交到版本控制）

# =============================================================================
# AI 提供商配置
# =============================================================================

# Google Gemini API Key
# 获取地址: https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=

# OpenRouter API Key
# 获取地址: https://openrouter.ai/keys
VITE_OPENROUTER_API_KEY=

# OpenRouter Base URL（可选，默认为官方 API 端点）
# 如果使用代理或自托管服务，可修改此值
VITE_OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# =============================================================================
# 应用配置
# =============================================================================

# 应用环境（development | production | test）
NODE_ENV=development

# 开发模式下启用详细日志
VITE_ENABLE_DEBUG_LOGS=true

# =============================================================================
# Electron 构建配置（仅用于打包）
# =============================================================================

# 应用名称
VITE_APP_NAME=Starverse

# 应用版本（自动从 package.json 读取）
# VITE_APP_VERSION=

# =============================================================================
# 数据库配置
# =============================================================================

# SQLite 数据库文件路径（自动配置，通常无需修改）
# Windows: %APPDATA%\Starverse\data.db
# macOS: ~/Library/Application Support/Starverse/data.db
# Linux: ~/.config/Starverse/data.db

# =============================================================================
# 安全配置
# =============================================================================

# 内容安全策略（Content Security Policy）
# 生产环境建议启用，开发环境可能需要宽松策略
VITE_CSP_ENABLED=false

# =============================================================================
# 性能调优
# =============================================================================

# Worker 线程池大小（默认为 CPU 核心数 - 1）
# VITE_WORKER_POOL_SIZE=

# 持久化防抖延迟（毫秒，默认 500ms）
# VITE_PERSISTENCE_DEBOUNCE_MS=500

# =============================================================================
# 实验性功能（谨慎启用）
# =============================================================================

# 启用实验性功能标志
VITE_ENABLE_EXPERIMENTAL_FEATURES=false

# 虚拟滚动（长对话性能优化）
VITE_ENABLE_VIRTUAL_SCROLL=false

# =============================================================================
# 开发者工具
# =============================================================================

# 启用 Vue DevTools
VITE_ENABLE_VUE_DEVTOOLS=true

# 启用源码映射（Source Maps）
VITE_ENABLE_SOURCE_MAPS=true

# =============================================================================
# 注意事项
# =============================================================================
# 1. 环境变量必须以 VITE_ 开头才能在渲染进程中访问
# 2. 生产构建时，敏感信息（API Key）应通过 electron-store 存储，而非环境变量
# 3. 修改环境变量后需重启开发服务器
# 4. 真实的 API Key 绝不能提交到版本控制系统
