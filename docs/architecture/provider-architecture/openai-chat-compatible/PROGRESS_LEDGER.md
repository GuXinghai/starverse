# OpenAI Chat Completions-compatible Rebuild Progress Ledger

## Current status

- Goal: implement TP-01 through TP-17 in frozen order, without commits.
- Current package: TP-17 — complete; Goal 2 awaits independent Goal 3 review.
- Current package progress: TP-01 through TP-17 are complete.
- Execution state: complete; independent Goal 3 review pending.
- Production-compatible sending is reachable only through the canonical TP-15 main-owned route; no alternate compatible send path is permitted.
- Staging, commits, pushes and merges: prohibited for Goal 2.

### 2026-07-11 — TP-14 completed

- Added the renderer-safe compatible settings surface with multi-instance CRUD, Base URL and immutable endpoint revisions, independent `compatibility_first` / `strict_ssrf`, organization/project, public headers/query, write-only bearer/basic/custom-header credentials, explicit auth clear, credential/provider deletion and persistent HTTP warning.
- Connection diagnostics and `/models` sync/query/manual CRUD resolve only the selected provider instance through the existing governed broker. Catalog UI exposes remote/manual/stale presence, manual-over-remote conflicts, per-field provenance and explicit unknown capability/context/pricing values.
- Added immutable same-lineage `version + 1` request/reasoning/inline/response profile revisions. Discovery accept/ignore is explicit; accept requires exactly one matching text rule, advances and pins future profile versions in one SQLite transaction, and never reinterprets the current response.
- Added explicit D15 modes and ordered mapping controls without exposing source lock as a per-response switch. The UI states that the first valid source locks per choice and does not switch.
- Added a separate configuration-only picker identity containing provider instance, model, endpoint revision, opaque credential ref and all profile pins. It does not enter `RuntimeProviderKey`. Composer and AppChatApp both block send until TP-15; ordinary runtime-model selection clears the compatible selection.
- Credential lifecycle closure: auth clear atomically creates the no-auth endpoint revision and tombstones the old descriptor; provider delete atomically tombstones provider plus all descriptors. Main consumes transaction-returned authoritative refs, attempts every secure deletion and retries every failure without restart. No proxy route, transport or security policy switches or degrades.
- Focused command: `npx vitest --run electron/ipc/compatibleProviderRegistryIpc.test.ts electron/ipc/compatibleProviderTransportIpc.test.ts electron/ipc/compatibleCatalogIpc.test.ts electron/preload.test.ts infra/db/worker.compatibleRegistry.test.ts infra/db/worker.compatibleRouteLifecycle.test.ts infra/db/repo/compatibleDiagnosticsRepo.test.ts src/shared/provider/openai-chat-compatible/ui/compatibleProviderRegistryClient.test.ts src/shared/provider/openai-chat-compatible/ui/compatibleConfigurationSelection.test.ts src/next/modelCatalog/compatibleCatalogClient.test.ts src/shared/modelCatalog/providers/openai-chat-compatible/compatibleCatalogMerge.test.ts src/ui-app/components/compatible/CompatibleProviderSettingsPanel.test.ts src/ui-app/components/ModelPickerDialog.compatibleConfiguration.test.ts src/ui-app/components/ChatAppComposer.compatibleConfiguration.test.ts src/shared/provider/openai-chat-compatible/productionBoundary.test.ts src/shared/i18n/messageSchema.test.ts` — 16 files / 82 tests passed.
- Follow-up P0/P1 closure command: registry/boundary/composer focused tests — 26 tests passed. `npx tsc --noEmit --pretty false` and `npx vue-tsc --noEmit` passed. `npm run gate:network-egress` passed over 1,026 files with exactly one classified compatible dual transport. `git diff --check` passed with line-ending warnings only.
- Independent final risk review: P0 0, P1 0. No external API request, real key, commit, stage or push occurred. Current ABI target remains Node; no ABI mismatch was encountered and no native artifact was committed.
- Next entry: TP-15 must consume the parent-owned configuration selection, prepare immutable provenance before network, register the first production chat IPC, and close send/stop/retry/regenerate/edit-resend without introducing any alias or fallback.
- TP-15 in-progress checkpoint: added `compatibleRoute.prepareChoices`, which atomically and idempotently creates every builder-declared choice placeholder while the route is still `prepared`; the focused DB lifecycle suite passes 7/7 and proves all choice identities exist before fetch.
- Added the main-owned `CompatibleChatRuntimeService`. It prepares current or historical route provenance first, loads the exact pinned endpoint revision and request-profile version, builds the sole Chat Completions request, resolves only the route credential ref in main, preserves strict proxy settings, uses the governed compatible transport, and runs the canonical SSE/non-stream decoders. It never reads `endpoints[0]`, latest settings after preparation, OpenRouter, Responses, DeepSeek, or LocalEndpoint builders/transports.
- Registered sender-owned `compatible-chat:start` / `compatible-chat:abort` IPC plus preload event/end subscriptions. Window destruction and explicit abort remain owner-scoped through the compatible request registry. Raw extension values are not sent to renderer; the temporary IPC event contains only bounded path metadata while TP-10/11/12 durable coordination is being connected.
- Connected ordinary wire events to `CompatibleTurnProjector`, streaming checkpoints, tool child persistence and atomic `compatibleProjection.finalizeRoute` on terminal. This is not yet TP-15 complete: custom extension/reasoning/inline coordination, typed failure-finalization coverage, UI new-send/stop, and retry/regenerate/edit-resend remain open.
- Latest validation: `npx vitest --run electron/services/compatibleChatRuntimeService.test.ts infra/db/worker.compatibleRouteLifecycle.test.ts` passed 2 files / 9 tests; `npx vitest --run electron/ipc/compatibleChatIpc.test.ts electron/services/compatibleChatRuntimeService.test.ts electron/preload.test.ts electron/ipc/startupIpcAudit.test.ts` passed 4 files / 9 tests; `npx tsc --noEmit --pretty false` passed. No external request, staging or commit occurred. Current ABI target remains Node.
- Resume TP-15 at `electron/services/compatibleChatRuntimeService.ts`: add TP-10/11/12 response coordination and typed terminal compensation tests before wiring `src/ui-app/app/appChatApp.logic.ts` historical actions. The runtime is registered but the composer still intentionally blocks compatible sends until that durable response boundary is closed.
- TP-15 follow-up: transport/parser failures after projector creation now synthesize one typed provider-neutral terminal event and attempt atomic failed/aborted projection finalization before falling back to a route-only terminal transition. This prevents ordinary governed-transport failures from leaving a completed-looking or projection-less assistant turn. A strict-SSRF-unavailable regression test verifies zero successful response handling and durable `failed` projection semantics; the focused runtime suite is 3/3 green. `npx tsc --noEmit --pretty false` passed after widening the Vitest transport mock result union.
- TP-15 response-coordinator checkpoint: added `CompatibleChatResponseCoordinator` as the provider-neutral owner of TP-08 wire events, TP-10 mapped extensions, TP-11 per-choice source locks, TP-12 inline parsing and TP-13 display projection. It loads the exact route-pinned reasoning mapping and inline policy in main. Custom mappings outrank builtin `reasoning` / `reasoning_content` / `thinking`; `custom_only` rejects builtin and inline sources; inline `<think>` text becomes a reasoning block and is removed from content without duplication.
- Every observed extension is now coalesced under its pinned response profile, bounded and redacted before DB persistence. The runtime creates opaque raw record IDs before terminal projection, persists raw child rows, and attaches their IDs to the matching durable choice so `compatibleProjection.finalizeRoute` can validate the complete child set. Raw values remain absent from renderer IPC.
- Latest validation after coordinator/raw integration: `npx vitest --run src/shared/provider/openai-chat-compatible/runtime/compatibleChatResponseCoordinator.test.ts electron/services/compatibleChatRuntimeService.test.ts electron/ipc/compatibleChatIpc.test.ts` passed 3 files / 8 tests; `npx tsc --noEmit --pretty false` and `git diff --check` passed (existing line-ending warnings only). TP-15 remains in progress: discovery aggregate upserts, local-mock DB integration, renderer UI and historical actions are still open.
- TP-15 UI/discovery checkpoint: unmapped observations now merge monotonically into provider/response-profile-scoped discovery aggregates while preserving ignored/confirmed state; builtin reasoning and mapped custom fields are excluded from candidate discovery. Main reads the existing aggregate before upsert so occurrence/sample counts never regress.
- The renderer new-send path is production reachable. `AppChatApp` calls the canonical `compatibleChat` preload bridge when a compatible configuration selection is active, builds standard role/content history, uses prepared/event notifications to refresh the durable transcript, clears the draft only after provenance exists, exposes one combined running state, and routes stop to owner-scoped compatible abort. Composer no longer displays the pre-TP-15 `compatible_send_not_enabled` barrier.
- Latest UI validation: existing `AppChatApp.send.test.ts` passed 37/37 (large pre-existing mock diagnostic output); compatible picker/composer tests passed 2 files / 2 tests after updating the frozen phase expectation to TP-15 send-enabled. `npx tsc --noEmit --pretty false` and `npx vue-tsc --noEmit` passed. Remaining TP-15 work is historical retry/regenerate/edit-resend, a full mocked DB/runtime integration and final package gates/docs.
- Historical-action checkpoint: added `compatible-chat:resolve-historical`, a main-owned read-only probe that resolves request/choice/route provenance through the canonical route coordinator and returns only protocol/provider/model/route identity. Endpoint revision and credential refs never cross IPC. This lets UI classify a historical action before any branch mutation instead of consulting current picker/settings or guessing from legacy message meta.
- Repository evidence found that existing `prepareHistoricalTurn` always creates a fresh user+assistant pair, while regenerate/retry branch operations already create their assistant placeholder. TP-15 must therefore add one atomic DB operation that pins the branch-operation-created assistant to the source compatible route; directly invoking current `prepareHistoricalTurn` would duplicate messages and is prohibited. The safe historical probe test passes 3/3; TypeScript requires the canonical branded historical lookup schema and was updated accordingly for the next validation.
- Historical-existing DB checkpoint: added `compatibleRoute.prepareExistingHistorical`. In one transaction it verifies the source route, active branch/head, question/assistant conversation and parent identity, and assistant streaming state, then creates a new immutable route+choice using every exact source endpoint/credential/request/response/reasoning/inline pin. It never creates duplicate messages and rechecks route availability plus secure credential presence before network.
- The focused test exposed and corrected a fresh-schema defect: `compatible_route_provenance.request_message_id UNIQUE` prohibited multiple immutable answer attempts for one question. The unique constraint was removed; request-message lookup is now explicitly newest-attempt ordered, while choice-message lookup remains unambiguous. No migration compatibility was added because TP-16 performs the approved targeted reset. `worker.compatibleRouteLifecycle` plus route schemas passed 2 files / 10 tests, including a regenerate-created assistant bound to historical pins with one persisted message row.
- Historical UI checkpoint: regenerate and retry-replace now probe the selected source answer before any legacy runtime preflight or branch mutation. A compatible match performs the existing branch operation once, then starts the canonical runtime with `existingHistoricalTurn` and the source route ID; edit new/replace similarly probes the chosen source answer (or latest request attempt), creates its new question/assistant once, and binds that pair to the immutable source pins. Current picker/settings are never consulted in these branches. Unsupported historical attachments fail explicitly before mutation.
- Existing non-compatible behavior remains green: `AppChatApp.regenRetry.test.ts` passed 12/12, `npx tsc --noEmit --pretty false` and `npx vue-tsc --noEmit` passed. Compatible-specific historical UI fixtures and the full local-mock main/DB transport integration remain required before TP-15 completion.
- TP-15 integration checkpoint: added a real in-memory SQLite worker + route coordinator + runtime + mocked transport test. It proves provenance is committed before the mocked request, exact credential and endpoint pins are consumed, non-stream decoding reaches a completed durable projection, and no external request occurs. Focused production/UI suites passed 8 files / 90 tests before final risk review.
- Final risk review found and fixed one P0 and one P1. New sends now carry every picker-owned immutable endpoint/credential/request/response/reasoning/inline pin through strict IPC; worker preparation reads the selected endpoint revision and rejects any pin mismatch before messages or fetch rather than selecting the latest endpoint. A post-rotation stale-picker regression proves the selected old revision and credential remain authoritative.
- Terminal discovery/raw/tool/reasoning children and all choice projections now finalize through worker-only `compatibleProjection.finalizeBundle` in one SQLite transaction. A fault-injection test forces final projection failure after a discovery write and proves the discovery write rolls back while the route remains streaming; runtime compensation retries one internally consistent failed/aborted bundle without applying a second terminal event to the projector.
- Latest acceptance command covered route lifecycle, IPC, runtime, response coordinator, production boundary, DB registry and handler registration: 7 files / 40 tests passed. `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` (1,033 files; exactly one classified compatible dual-transport hit) and `git diff --check` passed. Independent post-fix P0/P1 re-review is pending before TP-15 is marked complete. No real API, key, stage or commit was used; ABI target remains Node.
- First post-fix review found one derived historical-command P1: the new-send full-pin schema also rejected minimal historical source identity after regenerate/retry/edit had created its assistant. IPC now models the two commands explicitly: new sends require the complete immutable selected pins; `historicalSource` / `existingHistoricalTurn` accept only provider/model source identity and remain mutually exclusive. The IPC regression proves historical input reaches main and a new send with minimal identity fails closed. TypeScript plus IPC/runtime/real-SQLite route suites passed 3 files / 17 tests; second post-fix P0/P1 review is pending.
- TP-15 complete: final independent review reports P0 0 / P1 0. Historical commands were additionally tightened to reject full pin payloads rather than silently ignore them; only minimal source identity is accepted, while new sends require complete selected pins. No OpenRouter, Responses, DeepSeek, Anthropic, Gemini, Ollama, LocalEndpoint or legacy builder/parser/transport fallback is reachable. Stable TP-16 handoff is the single canonical runtime plus a targeted, previewable reset boundary; no commit, stage, external request or real key occurred.
- TP-16 census/reset checkpoint: repository and Git-history evidence proves the deleted Generic stack was fixture-only and never owned a production endpoint registry, secure store or live route. Implemented destructive predicates cover only confirmed exact `generic_openai_compatible` provider/source catalog and preference rows plus deprecated config key `openRouterBaseUrl`. Providerless rows and conversation/message metadata without route provenance are reported under `ambiguous` with destructive action stopped; ordinary OpenRouter history, URL/model/display-name matches, canonical `openai_chat_compatible`, native providers and all LocalEndpoint/LM Studio/Ollama `openai_compatible` modes are excluded.
- Added worker-only deterministic `compatibleReset.census` and confirmation-gated transactional `compatibleReset.execute`, a main-owned redacted preview/apply service, retryable canonical secure-store orphan cleanup, typed IPC/preload maintenance bridge, and `gate:no-legacy-compatible`. Current mixed SQLite fixtures prove exact-alias deletion, near-match exclusion, idempotence, late-delete rollback, catalog-child cleanup, and preservation of the explicitly seeded OpenRouter, LocalEndpoint, mixed-provider conversation, project, asset, canonical provider and canonical schema objects. Config/secure tests prove only `openRouterBaseUrl` is removed, official `openRouterApiKey` and active compatible refs remain, config interruption stops secure cleanup, and orphan deletion retries without exposing refs or values.
- Current TP-16 focused evidence: 7 files / 19 tests passed across reset worker/service/IPC/preload/startup/DB registration; `gate:no-legacy-compatible` passed over 677 production files; `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 1,038 files with one classified compatible dual transport, and `git diff --check` passed. Independent risk and documentation reviews are pending before package completion. No real user data reset was executed; all destructive evidence used in-memory synthetic fixtures.
- TP-16 complete after two risk-review corrections: conversation/message/providerless matches are ambiguous-only and never deleted, and `catalog_models` is now symmetric between census and execute. Final P0/P1 review reports 0/0. Expanded focused reset/fresh evidence is 7 files / 20 tests, including mixed conversation preservation, canonical provider continuity, catalog child deletion, late transaction rollback, config interruption/resume, native/local config preservation and secure orphan retry. The exact negative inventory gate covers the TP-01 forbidden symbols and deleted paths. No real user data was reset; TP-17 receives a previewable, idempotent, confirmation-gated boundary.

## Repository baseline

- Branch: `codex/image-reasoning-display`
- HEAD: `3530c1d0b8555b3886eb623f6642a7f7ba8382d4`
- HEAD subject: `docs(architecture): record OpenAI-compatible owner decisions`
- Worktree at planning start: clean.
- Goal 2 start worktree: 21 untracked planning documents under this architecture directory; no other dirty paths.
- Authoritative owner source: `OPENAI_CHAT_COMPATIBLE_OWNER_DECISIONS.md`, 2,190 lines, strict UTF-8.

## Goal 2 execution log

### 2026-07-10 — TP-01 started

- Re-recorded branch `codex/image-reasoning-display`, HEAD `3530c1d0b8555b3886eb623f6642a7f7ba8382d4`, subject `docs(architecture): record OpenAI-compatible owner decisions` and complete untracked status.
- Set TP-01 to `in_progress`; all later packages remain `planned`.
- Began read-only mapping of Generic fixture imports, provider defaults, `legacyOpenRouter`, OpenRouter custom endpoint identity, generation/multimodal aliases and compatibility-only tests.
- No production/test/schema/config file has been edited at this checkpoint.
- Next: freeze the exact neutral-mapper extraction needed to preserve LocalEndpoint/LM Studio/Ollama, then implement TP-01 deletion and explicit-unset behavior.

### 2026-07-10 — TP-01 implementation checkpoint

- Deleted the complete runtime-dead `src/next/provider/generic/` implementation/test stack and the compatibility-only endpoint registry baseline test.
- Extracted the only production dependency into a local-only neutral seam, `src/next/streaming/core/localOpenAIChatCompletionsStreamMapper.ts`, and rewired LocalEndpoint, LM Studio and Ollama without creating a cloud-compatible route.
- Removed `generic_openai_compatible` from generation-parameter and multimodal identities; added an explicit `unset` generation profile rather than substituting OpenRouter.
- Removed default-provider and `legacyOpenRouter` history fallback behavior. Current send now remains unset without an explicit provider/model pair; historical regenerate/retry/edit resend requires persisted provider/model identity and fails closed when it is absent.
- Removed the OpenRouter custom-endpoint/base-URL runtime identity from Settings, preload types, credential IPC, catalog credential resolution and stream transport. Native OpenRouter now uses its official URL only; the new compatible provider remains production-unreachable.
- Type evidence: `npx tsc --noEmit --pretty false` passed; `npx vue-tsc --noEmit` passed.
- Focused evidence already green: local streaming/runtime/generation/multimodal group, 134 tests passed; picker/composer group, 57 tests passed.
- Current focused App routing evidence: 6 files, 73 passed and 9 failed. All failures are old fixtures that omit the newly mandatory explicit provider/model identity; production code correctly fails closed. Exact fixture repairs and explicit no-fallback assertions remain before TP-01 acceptance.
- OpenRouter focused group was reduced to four stale expectations and those assertions were updated; the complete group still requires a clean rerun.
- `better-sqlite3` ABI mismatch encountered: no. Rebuild command: none. No DB-heavy test has been run yet.
- Worktree includes only Goal 2 implementation/planning paths recorded by `git status --short --untracked-files=all`; no staging or commit was created.
- Next: rename the remaining test filename containing `Generic`, repair App route fixtures without restoring fallback, rerun all TP-01 focused tests, run type/network/diff gates and negative searches, then mark TP-01 complete and enter TP-02.

### 2026-07-10 — TP-01 complete

- Deleted the entire runtime-dead Generic provider stack, Generic generation/multimodal aliases, compatibility-only source guards, default-provider identity, `legacyOpenRouter` history fallback and OpenRouter custom-endpoint/base-URL behavior.
- Preserved OpenRouter official and independent LocalEndpoint/LM Studio/Ollama behavior. The shared local Chat Completions semantic mapper now lives under `src/next/streaming/core/` and carries no cloud-compatible provider identity.
- Current sends require an explicit provider/model pair. Providerless and provider-only historical routes fail closed; neither current picker state nor OpenRouter auto is substituted.
- Fixed the final explicit-selection regressions found by P0/P1 review: Console emits a full provider/model object, explicit `openrouter/auto` persists as a complete selection, and incomplete historical routes cannot acquire a model fallback.
- OpenRouter official uses the fixed official endpoint for Settings status, credential resolution, catalog access and stream transport. The physical legacy key is no longer read or written; targeted data cleanup remains owned by TP-16.
- Schema/data impact: no schema change and no data deletion. Existing providerless history remains intentionally unroutable until TP-16 targeted reset.
- Focused core/Electron evidence: 19 files, 266 tests passed.
- Final focused UI evidence: 10 files, 158 tests passed, including `AppChatApp.send.test.ts` 37/37 and explicit providerless/provider-only no-fallback cases.
- `npx tsc --noEmit --pretty false`: passed.
- `npx vue-tsc --noEmit`: passed.
- `npm run gate:network-egress`: passed; no new unclassified egress.
- `git diff --check`: passed.
- Full content/path negative searches found no `streamViaGeneric`, `GenericEndpointConfig`, `decodeGenericSSE`, `generic_openai_compatible`, `legacyOpenRouter`, `DEFAULT_CHAT_PROVIDER_ID`, `DEFAULT_CHAT_MODEL_SELECTION`, `openrouter-custom-legacy-store`, `openRouterBaseUrl` or Generic compatible filenames outside historical planning documents.
- Final read-only risk review: no remaining P0/P1 blocker for TP-01.
- `better-sqlite3` ABI mismatch encountered: no. Rebuild command: none. TP-01 did not run DB-heavy tests.
- No files were staged and no commit was created.

### 2026-07-10 — TP-02 started

- Read TP-02 and Master Plan Sections 3–5; mapped the current fresh-schema/bootstrap/repository/IPC boundaries before editing.
- Current fact: `infra/db/worker/runtime.ts` executes the real `infra/db/schema.sql` first and then unrelated legacy ensures. Compatible tables must exist only in `schema.sql`; TP-02 will not add an `ensureCompatible*`, migration DDL or schema-version-triggered whole-database rebuild.
- Current fact: bumping `DB_SCHEMA_VERSION` can delete the entire dev database on mismatch and would violate targeted preservation. TP-02 will add independent fresh tables without using that global rebuild mechanism.
- Current fact: `npm run verify:ssot` ends in the clean-worktree TC-15 gate, which cannot pass during this deliberately uncommitted Goal. TP-02 will run its functional component gates and record TC-15 as deferred to Goal 3 rather than hiding, staging or committing changes.
- Production-compatible send remains unreachable: TP-02 will define only domain/schema/repository contracts and worker-only DB methods; runtime selection, UI, preload send IPC and network transport remain unchanged.
- Next: implement canonical branded IDs and strict JSON validators, then add the 17 fresh compatible tables and schema-introspection tests before repository handlers.

### 2026-07-10 — TP-02 implementation and risk-hardening checkpoint

- Added the dedicated canonical domain under `src/shared/provider/openai-chat-compatible/`: one protocol key, opaque IDs, provider/model identity, strict Zod contracts, renderer-safe projections and repository interfaces. The key remains absent from runtime selection, UI, preload send APIs and network transport.
- Added all 17 compatible tables only to `infra/db/schema.sql`; no compatible migration, `ensureCompatible*`, schema-version bump or second DDL path exists. `DbWorkerRuntime` constructs five repositories and exposes their methods only to the worker allowlist (`renderer: false`).
- Implemented provider/credential descriptor/endpoint revision, immutable profile/mapping, remote/manual catalog source, route/choice/tool foundation and bounded diagnostics/raw repositories with strict handler parsing and read-back parsing.
- Closed TP-02 risk-review blockers:
  - credential summaries contain only structural mask state; arbitrary masked labels are not persisted or exposed;
  - ordinary header/query values require explicit `public_non_secret` classification and still pass sensitive-name/value and transport-header deny checks;
  - durable raw string values require fixed redaction tokens, discovery previews are structural summaries, catalog diagnostics use message keys, and limits are 16 KiB per record / 256 records / 256 KiB per route response across all choices;
  - route provenance pins protocol key, endpoint, credential, request/response/reasoning/inline versions and canonical reasoning mode; SQLite composite FKs plus an insert trigger reject contradictory pins;
  - route lifecycle is represented as `prepared -> streaming -> completed|failed|aborted|interrupted` with immutable pins, monotonic transition triggers and terminal timestamps;
  - request and assistant choice messages must belong to the same conversation at repository and schema-trigger boundaries; conversation deletion cascades the complete compatible route graph instead of failing FK checks.
- Current focused evidence after hardening: 12 files, 264 tests passed. Includes schema introspection/runtime parity, all five compatible repos, strict domain/IPC decoding, worker registration/allowlist and production-unreachable source guards.
- `npx tsc --noEmit --pretty false`: passed after the latest hardening changes.
- `npm run gate:network-egress`: passed; no compatible transport primitive or new unclassified egress exists.
- `git diff --check`: passed (line-ending conversion warnings only).
- `npm run verify:ssot` was attempted after `npm run rebuild:node`. It stopped in the full Vitest phase before SSOT component gates: 381 files passed, 5 skipped, 18 failed; 4,055 tests passed, 14 skipped, 66 failed. Failures include stale TP-01 source-characterization assertions and full-concurrency UI timeouts; they require focused triage and cannot be attributed to TP-02 solely from the aggregate run. TC-15 dirty-worktree was not reached and remains inherently deferred to Goal 3.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command run: `npm run rebuild:node` (directly and again through `npm run verify:ssot`). Current ABI target: Node. Electron smoke retried: no. No native artifacts staged or committed.
- Worktree remains intentionally uncommitted and includes TP-01/TP-02 implementation plus planning documents; no staging, commit, push or merge was created.
- Next: obtain final P0/P1 re-review of the hardening, rerun network/diff/negative gates, triage the 18 full-suite failing files in focused groups, then either close TP-02 or record exact residual failures before entering TP-03.

### 2026-07-10 — TP-02 complete

- Final P0/P1 re-review found no remaining TP-02 blocker. Secret-like values are rejected at schema/repository/IPC boundaries; renderer credential projection exposes only structural mask state; route pins, lifecycle, same-conversation constraints and response-wide raw limits remain enforced.
- Re-ran the 12-file compatible domain/schema/repository/worker/IPC suite: 12 files passed, 264 tests passed.
- `npx tsc --noEmit --pretty false`: passed. `npx vue-tsc --noEmit`: passed. `npm run gate:network-egress`: passed. `git diff --check`: passed with line-ending conversion warnings only. Production-boundary, second-DDL and transport negative searches passed.
- Full Vitest was run after `npm run rebuild:node`: 387 files passed, 17 failed; 4,070 tests passed, 55 failed, 14 pending. Exact residuals were attributed before package close. TP-01-attributable explicit route fixtures, OpenRouter auto sentinel literals and locale-unsafe guards were repaired in focused runs; a DeepSeek reasoning-display upsert wire leak and a Gemini normalized snapshot expectation were also repaired and verified. Remaining residuals are non-TP-02 environment/tool timeouts, DFC evidence state and stale unrelated UI fixture expectations; they do not exercise the compatible schema/domain implementation.
- `npm run verify:ssot` cannot reach a green final TC-15 during Goal 2 because this Goal intentionally keeps all implementation uncommitted. TC-15 remains deferred to independent Goal 3; no staging, commit or stash was used to bypass it.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command: `npm run rebuild:node`. Current ABI target: Node. Electron smoke retried: no. No native artifacts staged or committed.
- Production-compatible send remains absent and prohibited until TP-15. TP-03 receives the canonical immutable records, strict repository APIs and fresh-schema SSOT.
- No files were staged and no commit was created.

### 2026-07-10 — TP-03 complete

- Added the main-owned multi-instance compatible provider registry, canonical endpoint/auth/header/query decoders, immutable endpoint and credential version creation, typed CRUD-only preload/IPC contracts and renderer-safe DTOs. Caller-supplied IDs/timestamps/revisions are rejected; main and worker transactions own them.
- Compatible Base URLs now reject userinfo/query/fragment, preserve gateway prefixes and normalize exactly one terminal `/v1`. Plain HTTP is retained with persisted warning state. Ordinary headers and static query values are explicitly non-secret; transport-owned, hop-by-hop, `Sec-*` and `Proxy-*` headers are rejected case-insensitively. Bearer, Basic and explicitly sensitive custom-header payloads remain encrypted main-only records.
- Added immutable safe-storage records under `compatibleCredentials.v1.<credentialRef>` with no plaintext fallback or reveal API. Generic store IPC now blocks credential namespace ancestors, leaves and descendants under electron-store dot notation; renderer safe-clear and config cleanup preserve both native and compatible credential roots.
- Added atomic worker transactions for provider graph creation, endpoint revision edits and credential rotation. `ERR_UNAVAILABLE` late-commit outcomes reconcile by main-owned provider/ref before compensation. Credential delete tombstones SQLite first, is idempotently retryable, and never restores/falls back to another credential.
- Added main-only secure-ref reconciliation: startup and subsequent registry access clean orphaned/tombstoned encrypted payloads, report missing active payloads as not configured, and emit only fixed generic warnings on unresolved cleanup. No ref, ciphertext, raw credential or error object is logged or returned to renderer.
- Final read-only P0/P1 review found no remaining TP-03 blocker. Production-compatible fetch/send/stream/probe/reveal remains absent and prohibited until TP-15.
- Focused evidence: 13 files passed, 295 tests passed, covering registry schemas, safe storage, CRUD/rotation/delete/reconciliation IPC, repository/worker transactions, worker-only DB methods, preload/store/config boundaries, renderer DTOs and production-unreachable guards. Latest post-review subset: 3 files, 24 tests passed.
- `npx tsc --noEmit --pretty false`: passed. `npx vue-tsc --noEmit`: passed. `npm run gate:network-egress`: passed with no unclassified egress. `git diff --check`: passed with line-ending conversion warnings only.
- `node scripts/gates/privacy-scan.mjs` remains red on the pre-existing whole-repository baseline: 76 violations in unrelated file-pipeline and historical/diagnostic documents. Focused compatible source/privacy guards pass and the new compatible path contributes no listed violation; the baseline was not weakened or edited.
- Schema/config impact: TP-02 tables are used without a second DDL path or global schema-version reset. Only encrypted compatible credential records and their config-root allowlist were added; no data reset or external request occurred.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command during TP-03: none; current ABI target remains Node from TP-02. Electron smoke retried: no. No native artifacts, files or commits were staged or created.
- Stable handoff: TP-04 receives immutable endpoint/profile/credential refs, active/deleted availability, strict instance scope and a main-only credential resolver. It must commit complete route provenance before any future transport factory can run and must not enable network behavior.

### 2026-07-10 — TP-04 complete

- Added strict route preparation, choice, historical-lookup and availability schemas plus a worker-only composite prepare transaction. User message, assistant choice 0, complete immutable route pins, branch choice/head, attachment/search side effects and lifecycle state now commit or roll back together.
- Added deterministic contiguous additional-choice creation, idempotent monotonic terminal transitions and startup recovery that changes only incomplete routes to `interrupted` while updating their assistant messages consistently. Startup recovery failure aborts main initialization before renderer IPC/window creation.
- Current and historical preparation resolve immutable endpoint/profile/credential/model versions independently. Historical retry/regenerate/edit inputs resolve the persisted route and never accept current picker overrides; disabled/deleted providers and missing/deleted credentials produce explicit interrupted/blocked outcomes with no fallback.
- Context, message-list and provider-neutral state projections carry only opaque route provenance ID and choice index. Reload round-trips those references without exposing endpoint or credential payloads, and schema uniqueness/role triggers prevent JOIN duplication or request/choice ambiguity.
- No production transport was added: the main-only coordinator is not instantiated or exposed, compatible route DB methods remain worker-only, preload has no route channel, and production boundary tests reject fetch/session/net primitives in the coordinator.
- Final focused evidence: 11 files passed, 250 tests passed, covering atomic rollback/crash injection, immutable edit pinning, historical lookup, provider/credential tombstones, missing secure payload, multi-choice identity, transition/recovery idempotency, context/message reload, strict schemas, worker registration and production-unreachable guards.
- `npx tsc --noEmit --pretty false`: passed. `npx vue-tsc --noEmit`: passed. `npm run gate:network-egress`: passed with no unclassified compatible egress. `git diff --check`: passed with line-ending conversion warnings only.
- The latest full Vitest baseline reached 401 passing files and 4,174 passing tests, with 5 failing files / 6 failing tests and 5 files / 14 tests skipped. Remaining failures are the already recorded DFC evidence state, image UI smoke and stale candidate/no-answer/user-render fixture expectations; a focused candidate rerun proved transcript route decoding now succeeds and the residual pager assertion is unrelated to TP-04 provenance.
- `npm run verify:ssot` rebuilt the Node ABI and stopped in that full Vitest baseline before TC-15. The final clean-worktree gate remains intentionally deferred to Goal 3; no test, fixture or gate was weakened to bypass it.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command: `npm run rebuild:node` (directly and through `npm run verify:ssot`). Current ABI target: Node. Electron smoke retried: no. No native artifacts, files or commits were staged or created.
- Stable handoff: TP-05 may add a main-only governed Electron-session transport whose input is an already committed route endpoint/credential configuration. It must not register a production compatible send path before TP-15.

## Completed evidence

### 2026-07-10 — Owner decision ingestion

- Read the owner document completely from line 1 through line 2,190; no sampling or heading-only pass was used.
- Captured frozen decisions D1–D14 and the overriding/additional D15 custom-reasoning source-selection policy.
- Confirmed mandatory sequencing constraints: delete false identities first; close domain/schema/provenance/network safety before production send; then close request, response, tool, reasoning, persistence and UI end to end; perform targeted destructive reset and final legacy cleanup.
- Confirmed destructive scope is targeted, not a full user-data wipe.
- Confirmed LocalEndpoint remains an independent local capability and must not be reused, aliased or used as fallback for the new cloud provider.

### 2026-07-10 — Master architecture and dependency freeze

- Created `REBUILD_MASTER_PLAN.md` as the planning SSOT.
- Froze canonical provider key `openai_chat_compatible`, immutable instance/endpoint/profile/credential/provenance identities, targeted fresh-schema tables, secure storage boundary, catalog merge rules, request field ownership, multi-choice behavior, observe-only tool handling, D15 source locking, inline state transitions, bounded raw retention, network security matrix, reset predicates, error taxonomy, test matrix and final acceptance.
- Froze a 17-package dependency graph. Production compatible sending is not allowed until domain/schema, atomic provenance and governed network security are complete.
- Resolved the prior custom-query requirement as static non-secret endpoint query configuration; secrets remain forbidden in query strings.
- No owner conflict or product-contract blocker remains at this stage.

### 2026-07-10 — Task-package foundation

- Created `task-packages/README.md` with the fixed 17-package order and hard execution rules.
- Wrote TP-01 through TP-04 as closed packages covering legacy/identity excision, canonical domain and fresh schema, secure multi-instance registry, and atomic route provenance.
- Current-source revalidation confirms: runtime union still lacks a compatible canonical key; Generic remains fixture-only/deferred; LocalEndpoint remains loopback/direct-fetch; historical route identity still persists provider/model while reconstructing endpoint/profile from current settings.
- Historical pre-D16 planning note: catalog snapshot machinery and provider-neutral storage/network primitives remained candidates for selective reuse. The former blanket connect-time-enforcement assumption is superseded by D16 below.

### 2026-07-10 — Complete dependency-ordered package planning set

- Completed the planning definitions for TP-01 through TP-17 with one fixed order and a hard gate: the first production-compatible send appears only in TP-15 after identity/schema, credentials, route provenance, governed network, catalog, request/response/tool/reasoning, display/persistence and UI contracts are closed.
- Every package contains goal, dependencies, production/deletion scope, data impact, invariants, implementation steps, tests/gates, acceptance, prohibitions, a suggested commit and a stable downstream contract.
- Closed the high-risk boundaries explicitly: write-only renderer credentials; static non-secret query configuration; the later D16 two-policy SSRF contract; before-network atomic provenance; multi-choice persistence; observe-only tool handling; D15 per-choice source lock; bounded redacted raw extensions; transactional terminal persistence; targeted rather than global reset.

### 2026-07-10 — Frozen-decision coverage matrix

- Created `DECISION_COVERAGE_MATRIX.md` with 64 granular D1–D15 obligations and 27 architecture/test/order/document obligations.
- All 91 rows have one primary owner and required proof; unowned rows: 0; reopened Owner choices: 0; unresolved Owner decisions: 0.
- D15 is separately mapped at per-mode, per-choice source-lock, invalid-config, custom-only, conflict, final-reconciliation, UI and persistence granularity.

### 2026-07-10 — Final planning audit

- Package count: 17; sequence is contiguous TP-01 through TP-17.
- Mandatory package headings: 187/187 present.
- Decision coverage rows: 91/91 uniquely identified; primary-owner gaps: 0; required-proof gaps: 0.
- Markdown internal links: 0 broken after correcting three index filenames.
- Strict UTF-8 validation: 0 decode errors; NUL files: 0; trailing-whitespace findings: 0.
- Scope audit: every new/changed path is inside `docs/architecture/provider-architecture/openai-chat-compatible/`; production code, tests, schema, configuration and unrelated documents remain untouched.
- `git diff --check`: passed. Untracked-file whitespace was checked separately because Git does not include untracked files in that command.
- Code/type/Vitest/Electron tests were not run: this Goal changes planning documents only and has no runtime artifact to validate.
- `better-sqlite3` ABI mismatch encountered: no. Rebuild command: none. Native artifacts committed: none; no commit was created.

## Working boundary

| Classification | Current planning conclusion |
| --- | --- |
| retain | Provider-neutral event/state/persistence primitives only after contract-by-contract verification; shared model-picker presentation and secure redaction primitives are candidates, not yet final. |
| replace | Identity, endpoint revision, credential/profile registry, route provenance, compatible catalog, governed transport, request/response/tool/reasoning contracts, error model and compatible UI integration. |
| delete | Runtime-dead Generic fixture stack and tests, duplicate Generic mapper, overlapping aliases, default-provider and `legacyOpenRouter` fallbacks, OpenRouter custom-endpoint legacy identity, compatibility-only source guards, and any LocalEndpoint-as-compatible coupling. |
| reset | Only compatible-identity/config/catalog/preference/credential/conversation/message/stream rows that cannot satisfy the new route provenance and ownership contracts. Unrelated assets, native-provider data and independent LocalEndpoint data remain preserved. |

## Active verification

- TP-05 governed transport is complete from the TP-03 secure registry and TP-04 before-network route contracts.
- Production-compatible chat send remains unreachable. Compatible renderer/preload network operations are limited to the typed connection test and bounded catalog sync/query/manual catalog bridge; no compatible chat-send path exists before TP-15.

## Blockers

- The prior TP-05 Owner-decision blocker is resolved. Owner retained the existing `system` / `manual` / `environment` / `direct` proxy model and dual-transport architecture, and added orthogonal `compatibility_first` / `strict_ssrf` endpoint policies. `compatibility_first` keeps native route behavior with pre-request/per-redirect checks; `strict_ssrf` blocks before egress when the selected transport cannot prove lease consumption. No proxy route, transport or security policy may silently switch or downgrade.
- The all-repository Vitest baseline still contains exact non-TP-02 residuals recorded in the TP-02 completion entry. The final TC-15 clean-worktree gate is intentionally impossible before Goal 3 because Goal 2 forbids commits and requires the implementation worktree to remain dirty.

### TP-05 connect-time feasibility evidence rounds

1. **Public API/type and repository scan.** Hypothesis: Electron may expose a request option or response field that binds/observes the actual remote address. Evidence: Electron 38 `ClientRequestConstructorOptions` has URL/session/host components but no `lookup`, socket, remote-address or server-name override; `IncomingMessage` and webRequest completion details expose no remote IP. Repository transports offer only unbound `session.fetch`, preflight-only `urlProbe`, provider-specific fixed-origin policy or LocalEndpoint direct fetch. Result: no existing compliant primitive.
2. **Local Electron runtime probe.** Hypothesis: supplying original `url` together with a fixed `hostname` may separate HTTP identity from connect address. Command: `npx electron .codex-tp05-electron-request-feasibility.cjs` against a loopback-only mock, followed by deletion of the temporary probe. Result: `url + hostname` followed the original URL/proxy path and never reached the fixed loopback server; a fixed-IP component request with an original `Host` header failed `net::ERR_INVALID_ARGUMENT`. No external request, credential or production file was used.
3. **Loopback enforcing-proxy architecture review.** Hypothesis: a main-only request-scoped proxy can consume the exact-IP lease while keeping Chromium TLS. Result: technically plausible for direct HTTPS tunnels, but it creates a new Node socket/proxy executor, needs an unforgeable one-shot listener/capability, exact-IP upstream CONNECT/SOCKS handling, HTTP parsing/backpressure/smuggling controls and lifecycle cleanup. `resolveProxy()` does not supply Chromium's proxy executor, so PAC ordering, proxy auth, HTTPS proxy trust and dedicated/default-session parity would need reimplementation or explicit fail-closed behavior. Independent P0/P1 review concluded this cannot be declared compliant without Owner approval.

### TP-05 implementation completed before the decision boundary

- Added canonical `{apiRoot}/models` and `{apiRoot}/chat/completions` composition with gateway-prefix preservation, deterministic static query merge, transport-boundary revalidation and HTTP warning metadata.
- Corrected the secret-like query-name classifier so common public `api-version` is allowed while `api_key`/auth/token/credential names and secret-like values remain forbidden.
- Added IPv4, IPv6, IPv4-mapped IPv6 and well-known NAT64 classification; special/private/loopback/link-local/documentation/shared/multicast/reserved ranges are blocked. A mixed A/AAAA set fails if any answer is blocked.
- Added Electron-session A and AAAA resolution with cache disabled, immutable one-use address leases, exact hostname/port/address consumption, expiry/revoke-all behavior and rebinding rejection before a connector can receive a changed address.
- Added manual redirect state with maximum five hops, loop detection, per-hop URL/query validation, method-preserving POST and monotonic cross-origin credential stripping. Address renewal remains a future broker responsibility after the connector decision.
- Added a provider-neutral fixed safe network error envelope with no raw URL, query, credential, body, response or exception fields.
- Focused evidence: 5 files, 76 tests passed. `npx tsc --noEmit --pretty false`: passed. `npm run gate:network-egress`: passed over 941 files, but the current gate does not scan `node:net`; this pass is not used as evidence that the proposed helper is authorized. `git diff --check`: passed with line-ending warnings only.
- No compatible IPC, preload channel, catalog sync, chat send, transport fetch or external request was added. The temporary Electron feasibility probe was deleted. Current ABI target remains Node; no rebuild was run for TP-05 and no native artifacts were staged or committed.

### 2026-07-10 — TP-05 completed after Owner decision D16

- Persisted required immutable endpoint `securityPolicy` as `compatibility_first | strict_ssrf`; it is independent from the existing runtime proxy setting `system | manual | environment | direct`.
- Preserved route behavior exactly: `system` uses Electron session transport; `manual`, `environment` and `direct` use the existing Node/Undici family and their native dispatcher behavior. Strict route parsing blocks corrupt or unavailable routes instead of normalizing, defaulting, switching or falling back.
- Implemented the canonical main-process broker, typed connection-test IPC, sender-owned lifecycle registry, late credential injection, manual redirects, response/header/body/SSE bounds and safe diagnostics. No production compatible catalog sync or chat send was registered.
- `compatibility_first` audits all A/AAAA answers before the first request and after every redirect without claiming connect-time proof. `strict_ssrf` requires exact one-use audited-address lease consumption; both current native adapters truthfully declare only pre-request audit capability, so strict mode returns `compatible_strict_ssrf_unavailable` before DNS, credential resolution or adapter execution.
- Closed the final P0/P1 review findings: IPv4-compatible/translated IPv6 classification, fail-closed unknown DNS-family errors, aggregate no-auth header bounds, fixed credential-backend errors, SSE event/pending limits and global/per-WebContents request capacity. A second independent read-only review found no remaining P0/P1 and no D16 route/policy switching.
- Focused evidence: 13 files, 168 tests passed. `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 951 files with exactly one classified compatible Node transport, and `git diff --check` passed. The repository-wide privacy scan remains at the pre-existing 76 violations and reported no compatible-transport path. No real external request or API key was used.
- Current ABI target remains Node. No ABI mismatch was encountered, no rebuild was run in TP-05 and no native artifact was staged or committed.

### 2026-07-10 — TP-06 completed

- Added a bounded OpenAI-compatible `/models` source parser. A real empty array is `empty_success`; malformed envelopes, oversize lists and non-empty all-malformed lists fail without staling the last valid catalog. Duplicate IDs retain the first valid row and emit count-only diagnostics.
- Added deterministic remote/manual merge by `(providerInstanceId, modelId)`: explicit manual field, then explicit remote field, then `unknown`; every merged field carries provenance and neither source writes into the other.
- Replaced destructive remote replacement with atomic `applyRemoteSyncSuccess`: snapshot insert, old-remote stale marking, active upsert, sync state, retention pruning and composite snapshot/state/merged readback are one SQLite transaction. Failure updates only sync state; crash-left `syncing` rows recover at startup.
- Added provider-active checks inside DB write boundaries, pre-commit abort linearization, retry/backoff with explicit force refresh, bounded sequential startup sync, per-instance recent-20 snapshot retention, 10,000 stale-remote cap and 10,000 manual-record cap.
- Added main-only catalog sync through the TP-05 broker and current latest endpoint/secure credential, plus renderer-safe sync/status/manual CRUD/merged query IPC. Renderer inputs contain stable instance/model/query controls only; no Base URL, credential, headers or request body. No OpenRouter, native provider catalog, LocalEndpoint or compatible chat-send route is reused.
- Focused evidence: 14 files, 86 tests passed. `npm run rebuild:node` completed successfully; `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 961 files with one classified compatible broker transport, and `git diff --check` passed. A second P0/P1 review confirmed all five TP-06 findings closed with no remaining P0/P1.
- `npm run test:model-catalog:smoke` was not used as TP-06 evidence because the script exclusively targets the retained OpenRouter/native catalog path and would violate the requirement not to substitute that fixture for the compatible catalog. No real external request or API key was used.
- Current ABI target is Node. No ABI mismatch was encountered; no native artifact was staged or committed.

### 2026-07-10 — TP-07 completed

- Added a pure canonical Chat Completions request builder with strict runtime decoding for `system` / `developer` / `user` / `assistant` / `tool` messages, text and bounded HTTPS/PNG/JPEG image parts, assistant tool-call continuity and explicit rejection of unsupported file/audio/unknown content.
- Added deterministic ownership and three-state resolution for standard fields, structured response/stream/tool parameters, request-side reasoning mappings and bounded `extraBody`. Exact, ancestor and descendant conflicts fail before fetch; unknown configured fields, unsafe paths, internal provenance, secret-like keys/values, prototypes, cycles and profile limit violations are rejected rather than dropped.
- Extended immutable request profiles with bounded defaults and declarative `enabled` / `effort` / `budget` value mappings. Provider creation now writes profile, mappings and endpoint in one transaction; once referenced by an endpoint, the request mapping bundle is sealed against drift.
- Added stable deterministic serialization and value-free ownership diagnostics. The builder performs no DB, network, IPC, UI or transport side effect and remains production-unreachable until TP-15; boundary tests reject native, OpenRouter, Responses, DeepSeek and LocalEndpoint delegation.
- Final focused evidence: 10 files, 80 tests passed. `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 966 files with exactly one classified compatible broker transport, and `git diff --check` passed. No real external request or API key was used.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command previously run for the current Node target: `npm run rebuild:node`. Current ABI target: Node. Electron smoke retried: no. No native artifact was staged or committed.
- TP-07 production scope: `request/messageTypes.ts`, `request/fieldOwnership.ts`, `request/buildCompatibleChatRequest.ts`, request exports, compatible schemas/domain/repository contracts, profile repository/worker bundle reads, registry command/IPC/preload typing and their focused tests. Deleted files: none. New tables/reset: none.
- Exact focused command: `npx vitest --run src/shared/provider/openai-chat-compatible/request/buildCompatibleChatRequest.test.ts src/shared/provider/openai-chat-compatible/domainSchemas.test.ts src/shared/provider/openai-chat-compatible/registry/registrySchemas.test.ts electron/ipc/compatibleProviderRegistryIpc.test.ts infra/db/repo/compatibleProfileRepo.test.ts infra/db/worker.compatibleRegistry.test.ts infra/db/worker.compatibleRouteLifecycle.test.ts infra/db/dbMethodsRegistry.test.ts infra/db/worker.handlerRegistration.test.ts src/shared/provider/openai-chat-compatible/productionBoundary.test.ts`.
- Acceptance evidence covers field-state/ownership matrices, fail-before-fetch conflicts, secret/provenance exclusion, request/response reasoning separation and the TP-15 production-unreachable source boundary. TP-07 blockers: none. Downstream parser, response tool semantics and production orchestration remain TP-08, TP-09 and TP-15 obligations.
- Stable handoff: deterministic validated JSON plus value-free ownership diagnostics and a pinned immutable request profile/mapping bundle for TP-09/TP-15. Goal 2 created no commit or staging; this scope remains an uncommitted working-tree change for Goal 3 review.

### 2026-07-10 — TP-08 completed

- Added a pure provider-neutral wire layer with incremental fatal UTF-8 SSE framing, LF/CRLF and exact single-space semantics, comments/keep-alives/empty events, multiline data, explicit `[DONE]`, EOF-without-DONE, abort/interruption and exactly-one-terminal behavior under cumulative/event/pending byte bounds.
- Added shared strict stream/non-stream Chat Completions decoding for all indexed choices, role/content string-null-array variants, unmerged tool fragments, finish reasons, usage, response metadata and bounded ordered unknown-extension candidates. Duplicate/missing/out-of-range choices, unfinished choices at DONE, post-finish deltas and incomplete non-stream final tools fail explicitly.
- Added Section 22 compatible JSON/SSE/shape/tool/extension error codes plus safe HTTP/provider normalization. Errors retain only fixed messages, status and shape booleans; partial raw SSE frames stay in a one-use module-private WeakMap and never become Error properties or diagnostics.
- Preserved the D16 network boundary: the parser performs no fetch, IPC, DB or UI work; bounded broker/system/manual/environment/direct transport tests remain green and production compatible send remains absent until TP-15. No OpenRouter, Generic, DeepSeek, Responses, LocalEndpoint or native parser/error type is imported.
- Final focused command: `npx vitest --run src/shared/provider/openai-chat-compatible/wire/sseFramer.test.ts src/shared/provider/openai-chat-compatible/wire/streamParser.test.ts src/shared/provider/openai-chat-compatible/wire/nonStreamDecoder.test.ts src/shared/provider/openai-chat-compatible/productionBoundary.test.ts src/shared/network/compatibleNetworkError.test.ts electron/net/compatibleProviderTransport.test.ts electron/services/compatibleProviderTransportService.test.ts electron/ipc/compatibleProviderTransportIpc.test.ts` — 8 files / 94 tests passed.
- `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 979 files with exactly one classified compatible dual-transport hit, and `git diff --check` passed. Two independent P0/P1 review rounds closed five findings; final review found no P0/P1. No real request or API key was used.
- TP-08 production files are `wire/wireTypes.ts`, `wire/wireError.ts`, `wire/sseFramer.ts`, `wire/extensionCapture.ts`, `wire/semanticDecoder.ts`, `wire/semanticMapper.ts`, `wire/streamParser.ts`, `wire/nonStreamDecoder.ts`, `wire/httpErrorMapper.ts`, exports and shared compatible error taxonomy. Deleted files/tables/reset: none. No staging or commit was created.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command previously run for the current Node target: `npm run rebuild:node`. Current ABI target: Node. Electron smoke retried: no. No native artifact was staged or committed.

### 2026-07-10 — TP-09 completed and execution paused

- Added route/message/choice-bound observe-only tool aggregation keyed by `(routeProvenanceId, choiceIndex, toolIndex)`. Fragments preserve global sequence values and merge ID/name/arguments atomically; exact duplicate sequences are idempotent, changed/out-of-order sequences, identity drift, duplicate provider tool IDs and post-final fragments fail explicitly without partial mutation.
- Added atomic finalization for `complete | malformed | incomplete`, with bounded object-only JSON validation, exact raw/validated argument equality, visible parse diagnostics and fixed `executionState = not_executed`. Deep/node/byte overflow, malformed/scalar JSON and missing ID/type/name/arguments remain non-executable and retain bounded raw text.
- Split tool persistence out of `CompatibleRouteRepo` into `CompatibleToolRepo`; removed the old `compatibleRoute.upsertToolCall/getToolCall/createToolResult` double-write scaffold. Worker-only save/batch/get/list/result/load-chain methods are registered under `compatibleTool.*`; renderer/preload receive no DB or executor channel.
- Strengthened the existing fresh `compatible_tool_calls` / `compatible_tool_results` tables with raw/validated argument columns, terminal diagnostics, identity/sequence/terminal immutability, object/depth/node checks and exact result binding. Repository and SQLite both require result messages to be later `role=tool` descendants of the exact assistant choice in the same conversation; sibling branches, mismatched bodies, incomplete calls and DB bypasses fail.
- Added structured reload and historical reconstruction to canonical assistant `tool_calls` plus ordered `role=tool` messages. It rejects mixed route/message/choice identity, malformed/incomplete calls, raw/validated drift and missing/duplicate/orphan results, and feeds directly into the TP-07 strict request builder. Actual transcript UI/reload and retry/regenerate/edit-resend orchestration remain TP-13/TP-15 obligations.
- Added plain observe-only safe view models and source guards proving no executor, plugin/function registry, OpenRouter/native/LocalEndpoint mapper, fetch, send IPC or production compatible runtime path was introduced. D16 proxy routes, dual transport and endpoint security policies were untouched.
- Final focused command: `npx vitest --run src/shared/provider/openai-chat-compatible/tools/toolAccumulator.test.ts src/shared/provider/openai-chat-compatible/tools/toolContextReconstruction.test.ts src/shared/provider/openai-chat-compatible/wire/streamParser.test.ts src/shared/provider/openai-chat-compatible/request/buildCompatibleChatRequest.test.ts infra/db/repo/compatibleToolRepo.test.ts infra/db/repo/compatibleRouteRepo.test.ts infra/db/repo/compatibleSchema.test.ts infra/db/dbMethodsRegistry.test.ts infra/db/worker.handlerRegistration.test.ts src/shared/provider/openai-chat-compatible/productionBoundary.test.ts` — 10 files / 87 tests passed.
- `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 988 files with exactly one classified compatible dual-transport hit, and `git diff --check` passed. P0/P1 reviews closed four findings and the final review found no P0/P1. No real request or API key was used.
- TP-09 production scope: `tools/toolTypes.ts`, `toolAccumulator.ts`, `toolContextReconstruction.ts`, `toolViewModel.ts`, tool exports; compatible domain/repository contracts; `compatibleToolRepo.ts`; fresh schema tool columns/triggers; runtime/worker/method registry/type/validation wiring; production-boundary guard. Deleted files: none. Destructive reset/config/credential changes: none. No staging or commit was created.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command run: `npm run rebuild:node`. Current ABI target: Node. Tests retried after rebuild: the focused TP-09 DB/tool suites and final 10-file acceptance command. Electron smoke retried: no. No native artifacts staged or committed: confirmed.
- TP-09 blockers: none. Stable handoff is a structured, reloadable, observe-only tool chain with immutable route/choice/tool/sequence identity and standard Chat Completions message reconstruction. Downstream reasoning replay, display/UI and production historical orchestration remain TP-11, TP-13/14 and TP-15.
- Per user instruction, execution stops normally here. This is not a technical blocker, not Goal completion and not TP-10 start; wait for explicit notification before continuing.

### 2026-07-11 — TP-10 completed

- Added a provider-neutral headless extension layer with a restricted literal/index/wildcard path DSL; script, regex, function, computed and prototype access are rejected. Immutable route/message/provider/response-profile and allowed-mapping pins are copied into observations, semantic candidates and discovery observations; mixed identity or future/unpinned mappings fail before interpretation.
- Added structural raw redaction, append/snapshot coalescing keyed by route/profile/choice/path/semantic/mode, 16 KiB record, 255 ordinary + one reserved overflow summary record, 256 KiB response and 512-byte reserved summary budgets. Opaque/signature/encrypted and all durable strings are redacted; count/byte overflow produces a bounded path/shape hash summary rather than silent loss.
- Added observation-only discovery aggregation for shape stability, nonempty counts, timing and normalized stream/final pairing. Built-in reasoning is excluded from discovery; citation/audio/refusal/trace/status/metadata families remain excluded candidates. Discovery never changes the current response or creates a mapping/profile version.
- Strengthened diagnostics persistence with monotonic candidate state/count, 128 candidates per profile version, immutable route/profile checks, response-scoped atomic raw batch writes and rollback. Durable raw records now round-trip sequence, append/snapshot kind, reasoning/diagnostic semantic and redaction state. The single-record worker/contract bypass was removed.
- Final focused command: `npx vitest --run src/shared/provider/openai-chat-compatible/extensions/extensions.test.ts infra/db/repo/compatibleDiagnosticsRepo.test.ts infra/db/repo/compatibleSchema.test.ts infra/db/worker.compatibleFreshSchema.test.ts infra/db/dbMethodsRegistry.test.ts infra/db/worker.handlerRegistration.test.ts src/shared/provider/openai-chat-compatible/wire/streamParser.test.ts src/shared/provider/openai-chat-compatible/wire/nonStreamDecoder.test.ts` — 8 files / 70 tests passed.
- `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 996 files with exactly one classified compatible dual-transport hit, and `git diff --check` passed. Privacy search found only detector/test fixtures and no raw-to-Markdown path. Final independent P0/P1 review found no residual P0/P1. No real request, endpoint or API key was used.
- TP-10 production scope: `extensions/*`, compatible exports/domain/schema/repository contract, diagnostics repository, fresh raw table columns, worker-only batch handler and DB registry. Deleted files: none. No renderer UI, production send, source lock, display assembler, reset, staging or commit was introduced.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command previously run for the current Node target: `npm run rebuild:node`. Current ABI target: Node. Tests retried after rebuild: TP-10 focused DB/extension/wire suites. Electron smoke retried: no. No native artifacts staged or committed: confirmed.
- TP-10 blockers: none. Stable handoff is profile-pinned semantic candidates, bounded reloadable raw evidence and observation-only discovery aggregates. TP-11 exclusively owns source priority, validity, per-choice lock and conflicts; TP-13/14 own display/persistence integration and UI.

### 2026-07-11 — TP-11 completed

- Added one canonical immutable reasoning mapping schema shared by save and headless validation: exactly two modes, ordered rules with independent stream/final path/mode/textPath contracts, text/blocks/opaque semantics, default-disabled replay, and canonical `assistant_field|assistant_content_tags` / `tool_call_chain_only|all_assistant_messages` values. Invalid prototype/wildcard textPath, half-configured final rules, invalid tags and unsupported shapes fail at save.
- Added typed custom/builtin candidate projection, safe numeric textPath evaluation, all-or-nothing block extraction and opaque non-display semantics. Per-choice selector applies custom→reasoning→reasoning_content→thinking→inline priority, exact event identity/idempotency, custom-only isolation, one immutable source key, bounded conflicts, append/snapshot semantics and same-source-only final reconciliation.
- Added exact historical replay projection from the pinned policy and TP-09 structured call/result chain identity; default replay omits reasoning, sibling choice/missing/duplicate/mismatched tool chains fail closed. No current-profile reinterpretation or response→request inference exists.
- Added `compatible_reasoning_choice_state`, exact route/mapping/source-key pins, monotonic lock/status, terminal immutability and worker-only save/get methods. Exact terminal repeats are idempotent; rewrite, source mismatch and direct-SQL prefix/wildcard bypasses fail.
- Final focused command: `npx vitest --run src/shared/provider/openai-chat-compatible/reasoning/reasoning.test.ts src/shared/provider/openai-chat-compatible/domainSchemas.test.ts infra/db/repo/compatibleProfileRepo.test.ts infra/db/repo/compatibleDiagnosticsRepo.test.ts infra/db/repo/compatibleSchema.test.ts infra/db/worker.compatibleFreshSchema.test.ts infra/db/dbMethodsRegistry.test.ts infra/db/worker.handlerRegistration.test.ts src/shared/provider/openai-chat-compatible/extensions/extensions.test.ts src/shared/provider/openai-chat-compatible/tools/toolContextReconstruction.test.ts` — 10 files / 77 tests passed.
- `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 1004 files with exactly one classified compatible dual-transport hit, and `git diff --check` passed. Final independent P0/P1 review found no residual P0/P1; documentation contract review found no remaining TP-11 blocker. No real request, endpoint or API key was used.
- TP-11 production scope: `reasoning/*`, canonical compatible schema/domain/repository contract, `compatibleReasoningRepo.ts`, fresh reasoning choice table/trigger, runtime/worker/registry/type/validation wiring and fixture updates. Deleted files: none. No inline parser, renderer UI, display assembler, production send, reset, staging or commit was introduced.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command previously run for the current Node target: `npm run rebuild:node`. Current ABI target: Node. Tests retried after rebuild: TP-11 focused mapping/selector/replay/repository/fresh-schema suites. Electron smoke retried: no. No native artifacts staged or committed: confirmed.
- TP-11 blockers: none. Stable handoff: TP-12 may emit inline candidates into the frozen per-choice selector; TP-13 may persist/display the selected source without recomputing priority or mapping mode.

### 2026-07-11 — TP-12 in progress checkpoint

- Implemented canonical `<think>` plus additive custom-tag policy validation with count, UTF-8 byte, cross-pair prefix, Markdown delimiter and 4 KiB matcher-state limits shared by production save schema and runtime construction.
- Implemented conservative EOF restoration, fenced/inline-code/escaped/XML-example literal handling, custom-only and structured-lock participation gates, sequence ranges, stable tag-pair/reasoning-segment IDs, TP-11 inline projection, multi-segment final reconciliation, selected segment persistence, exact route pins and unified 1 MiB UTF-8 reasoning limits.
- Current focused evidence: `npx vitest --run src/shared/provider/openai-chat-compatible/reasoning/reasoning.test.ts infra/db/repo/compatibleDiagnosticsRepo.test.ts src/shared/provider/openai-chat-compatible/reasoning/compatibleInlineReasoningParser.test.ts` — 3 files / 49 tests passed; `npx tsc --noEmit --pretty false` passed. Earlier TP-12-inclusive acceptance command reached 11 files / 77 tests with type/vue/network/diff gates green before the latest inline additions; it must be rerun after the incremental rewrite.
- Final P0/P1 review after literal, sequence, multi-segment, source-key, route-pin and UTF-8-boundary fixes found no remaining P0/P1 in those implemented paths.
- Confirmed blocker: `CompatibleInlineReasoningParser.push()` still reparses the complete accumulated text. The same legal input can therefore consume a different cumulative scan budget depending on transport chunking. This violates arbitrary-boundary/chunk-invariance requirements and is not accepted as TP-12 completion.
- Remaining exact steps: replace full-buffer reparse with persistent `content | possible_start_tag | reasoning | possible_end_tag | code_fence | terminal` state and bounded pending tail; add single-vs-many near-limit equivalence, every-boundary, abort and interrupted terminal tests; rerun focused/full gates and independent reviews.
- Current working-tree scope remains uncommitted and unstaged. No production compatible send, UI, external request, real endpoint or API key was introduced. Current ABI target remains Node; no native artifacts were staged or committed.

### 2026-07-11 — TP-12 completed

- Replaced the full-buffer reparse with a persistent incremental queue/cursor state machine, bounded pending tag/XML/fence state, exact long-fence width tracking, canonical plus additive custom tags, conservative EOF/abort/interrupted restoration and stable content/reasoning segment identities.
- Hardened UTF-16 transport partition invariance and UTF-8 budgets: custom tags and reasoning payloads remain correct across surrogate splits; input is processed in bounded internal batches; 2 MiB input and 1 MiB reasoning boundaries are enforced before mutation without whole-fragment per-character object amplification.
- Completed literal/fence, XML/example, escaped, nested, unmatched, custom-only/structured-lock, multi-segment, final reconciliation and TP-11 selected-segment persistence contracts. Schema impact remains the immutable inline policy fields and `compatible_reasoning_choice_state.selected_segment_ids_json`; no reset or migration path was added. Deleted files: none.
- Final focused command: `npx vitest --run --maxWorkers=1 --minWorkers=1 src/shared/provider/openai-chat-compatible/reasoning/compatibleInlineReasoningParser.test.ts src/shared/provider/openai-chat-compatible/reasoning/reasoning.test.ts src/shared/provider/openai-chat-compatible/domainSchemas.test.ts infra/db/repo/compatibleProfileRepo.test.ts infra/db/repo/compatibleDiagnosticsRepo.test.ts infra/db/repo/compatibleSchema.test.ts infra/db/worker.compatibleFreshSchema.test.ts infra/db/dbMethodsRegistry.test.ts infra/db/worker.handlerRegistration.test.ts src/shared/provider/openai-chat-compatible/extensions/extensions.test.ts src/shared/provider/openai-chat-compatible/tools/toolContextReconstruction.test.ts` — 11 files / 115 tests passed. The final parser-only rerun after pending-state reporting was 33/33.
- `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 1006 files with exactly one classified compatible dual-transport hit, and `git diff --check` passed. Independent final review found no residual P0/P1; documentation review found no remaining frozen TP-12 blocker.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command previously run for the current Node target: `npm run rebuild:node`. Current ABI target: Node. Tests retried after rebuild: TP-12 parser/reasoning/schema/repository acceptance suites. Electron smoke retried: no. No native artifacts staged or committed: confirmed.
- No production compatible send, display/UI integration, external request, real endpoint or API key was introduced. TP-12 blockers: none. Stable TP-13 handoff: selected structured/inline source, segment IDs, sequence ranges and conflicts are authoritative; TP-13 must not recompute source priority.

### 2026-07-11 — TP-13 completed

- Added the provider-neutral `src/next/state/compatible/` event projector, stable display assembler, durable projection client and pinned historical projection. Real `CompatibleWireEvent` content/content-parts/tool/usage/error/terminal events now map without UI access to wire field names; selected TP-11/TP-12 reasoning remains source-locked and ordered by sequence/suborder.
- Added `compatible_choice_display_projections`, worker repository/runtime/handler wiring and renderer-safe projection methods. Streaming checkpoints are monotonic; terminal writes atomically cover every choice projection, route state and per-choice message status, while terminal success requires exact DB child sets for tools/results, reasoning, raw records and segment/conflict metadata.
- Reload bundles contain immutable route pins, the pinned reasoning mapping, all durable choices and choice-scoped TP-09 tool chains. Default history projection omits reasoning; explicit replay uses only the historical mapping and exact persisted tool chain, with mismatches failing closed.
- Added explicit `done`, `eof_without_done`, `aborted`, `interrupted`, `error` and `recovered_after_crash` causes; zero/partial checkpoint startup recovery produces worker-only `recovered_incomplete` projections rather than silent success. Usage is a strict numeric whitelist, response-scoped to one owner choice, and unique at SQLite level.
- Neutral content parts strip unknown fields; image URLs retain only HTTP(S) origin/path and remove query/hash. Raw retention deletes only expired terminal-route records not referenced by durable projections. Streaming projection size is capped at 31 MiB with 32 MiB reserved for terminal/recovery metadata; mutation failures roll back assembler state.
- Final focused command: `npx vitest --run --maxWorkers=1 --minWorkers=1 src/next/state/compatible/compatibleDisplayProjection.test.ts src/next/state/compatible/compatibleTurnProjector.test.ts src/next/state/compatible/compatibleTurnProjectionClient.test.ts src/next/state/compatible/compatibleHistoricalProjection.test.ts infra/db/repo/compatibleTurnProjectionRepo.test.ts infra/db/repo/compatibleDiagnosticsRepo.test.ts infra/db/repo/compatibleSchema.test.ts infra/db/worker.compatibleFreshSchema.test.ts src/shared/provider/openai-chat-compatible/productionBoundary.test.ts infra/db/dbMethodsRegistry.test.ts infra/db/worker.handlerRegistration.test.ts` — 11 files / 83 tests passed.
- `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run gate:network-egress` over 1017 files with exactly one classified compatible dual-transport hit, and `git diff --check` passed. Independent final reviews found no residual P0/P1/P2. No real request, endpoint or API key was used.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild command previously run for the current Node target: `npm run rebuild:node`. Current ABI target: Node. Tests retried after rebuild: TP-13 state/projector/client/history/schema/repository/worker acceptance suites. Electron smoke retried: no. No native artifacts staged or committed: confirmed.
- TP-13 blockers: none. No production compatible send or provider settings UI was enabled. Stable TP-14 handoff is a renderer-safe read/write application service for provider registry, catalog, profiles, diagnostics and durable route projections.

### 2026-07-11 — TP-17 acceptance in progress; mandatory closeout blockers recorded

- TP-01 through TP-16 are complete. TP-17 evidence collection remains in progress; `TEST-E2E`, `PLAN-PACKAGE` and `ACCEPT-GLOBAL` remain `planned-closed`. All implementation remains deliberately uncommitted and unstaged.
- Full relevant acceptance: `npm run rebuild:node` succeeded, then `npx vitest --run compatible` passed 56 files / 518 tests. This covers canonical domain/schema, registry/credentials, catalog, request/wire/SSE, tools, reasoning, display/persistence, UI selection, governed network, production orchestration, historical lifecycle and targeted reset using mocks and synthetic data only.
- The first normal Electron smoke exposed a production renderer-boundary defect: the renderer-safe compatible barrel reached `node:buffer` through the request builder. The builder now uses `TextEncoder` for UTF-8 byte counts; its focused builder/boundary suite passed 2 files / 22 tests and TypeScript passed.
- A second normal Electron smoke mounted the app and passed the core shell/preload assertions, then the overall command failed in the unrelated retained DFC attachment journey because no valid local-file selection grant was present. A durable `SV_ELECTRON_SHELL_ONLY=1` smoke mode ends after the existing core shell/preload assertions without changing normal DFC behavior; that shell-only command passed with app mount, composer draft visibility, typed bridges present and raw `ipcRenderer` absent.
- No compatible loopback-send exception was added: loopback is intentionally rejected by the canonical SSRF policy. Mocked integration and real Electron shell smoke are two separate evidence sets and do not satisfy TP-17's frozen renderer→preload→IPC→main→network→parser→display→SQLite Electron/Playwright end-to-end criterion.
- Final gates: `npx tsc --noEmit --pretty false`, `npx vue-tsc --noEmit`, `npm run i18n:check`, `npm run gate:no-legacy-compatible` (677 files) and `npm run gate:network-egress` (1,038 files; exactly one classified canonical dual-transport hit) passed. `npm run lint:changed` failed; compatible-scope ESLint independently failed with 21 restricted Electron→`src/next` import errors inside canonical compatible files. This is a mandatory TP-17 blocker. `git diff --check` passed separately.
- Live provider smoke was not run: it is optional, Owner-operated and forbidden in this Goal. No external API request, real endpoint or real credential was used. Targeted reset tests used in-memory synthetic state only; no user data/configuration was reset.
- D16 remains exact: `system` uses Electron session transport; `manual`, `environment` and `direct` preserve the Node/Undici transport family. `compatibility_first` performs pre-request and post-redirect address checks while preserving the chosen proxy route. `strict_ssrf` requires proof that an audited one-use address lease was consumed and blocks before credential resolution/network when neither current native transport can prove it. No route, policy or transport silently switches or degrades.
- `better-sqlite3 ABI mismatch encountered: no`. Rebuild commands run: `npm run rebuild:node` and `npm run rebuild:electron`. Current ABI target: Electron. Tests retried after rebuild: the 56-file compatible suite and Electron smoke. Electron smoke retried: yes. No native artifacts staged or committed: confirmed.
- TP-17 blockers: (1) canonical compatible Electron modules violate the mandatory Electron→`src/next` import boundary; (2) the required compatible Electron/Playwright local-mock end-to-end journey is missing; (3) independent review found renderer-authorized `compatibleProjection.saveStreaming/finalizeRoute` can bypass the main-owned production runtime and terminalize known routes. The retained DFC file-grant smoke failure is an environment block, not a compatible-provider regression.

### 2026-07-11 — TP-17 mandatory blockers closed

- Removed renderer persistence authority: `compatibleProjection.saveStreaming`, `finalizeRoute`, `finalizeBundle` and recovery remain worker/main-only; the obsolete renderer projection mutation client and its compatibility test were deleted. Renderer DB access retains only validated projection reload methods.
- Replaced the Electron→`src/next` layering violation rather than adding a lint exception. The canonical UI-free domain/request/wire/tool/reasoning/extension/runtime/display core now lives at `src/shared/provider/openai-chat-compatible/`; `TerminalArbiter` and the retained proxy/sanitization core moved to shared boundaries. Renderer-owned configuration clients remain under `src/next/.../ui`. No old-path re-export, alias or compatibility bridge remains.
- Added a durable, explicit development-only Electron compatible E2E transport fixture below the governed broker. It uses a non-loopback HTTPS fixture identity, an audited public test address and deterministic adapters while leaving production route selection, address policy, credential resolution, builder, parser, display projection and SQLite intact. Both fixture adapters truthfully remain `pre_request_audit_only`; strict SSRF therefore still blocks rather than using the fixture.
- Closed the final fixture-activation P1: both selection and fixture construction require `app.isPackaged === false`, `NODE_ENV=development` and the exact explicit smoke flag. Packaged binaries cannot activate the fixture even if both environment variables are supplied; focused tests cover that adversarial case.
- `npm run test:electron-compatible-e2e` passed the real renderer→preload→IPC→main→registry/catalog→governed transport→SSE parser→display→SQLite path. Evidence covers provider CRUD creation, remote `/models`, independent manual model merge, exact selection pins, streaming content, usage/terminal persistence, abort, renderer reload and historical route identity. The run made no network connection and used no real credential.
- The E2E exposed and closed a real stream bug: ordinary OpenAI chunks commonly carry `finish_reason: null`; the display projector previously finalized the tool accumulator on that provisional value and then rejected the later terminal `stop` as `tool_sequence_conflict`. Null finish events now advance sequence without finalizing tools; focused projector/parser/runtime tests passed.
- Final Node acceptance after `npm run rebuild:node`: `npx vitest --run compatible` passed 55 files / 512 tests; the explicit smoke-fixture guard added 1 file / 2 tests. TypeScript, Vue TypeScript, i18n, focused canonical ESLint (0 errors), `gate:no-legacy-compatible`, `gate:network-egress` and `git diff --check` passed.
- Repository-wide `npm run lint:changed` still reports exactly eight restricted imports in retained DeepSeek/OpenRouter credential/runtime files already present in the incoming dirty worktree. Canonical compatible files report zero ESLint errors. Those unrelated files were not changed merely to make this Goal's gate appear green.
- Independent review's P1 renderer-integrity finding is closed. The required Electron compatible E2E is now executable and green. TP-17 and every coverage row are complete; Goal 2 is `implementation_complete_ready_for_review`.
- ABI record: no mismatch encountered. Rebuild commands: `npm run rebuild:node` and `npm run rebuild:electron`. Final acceptance leaves Electron as the required endpoint after the final E2E rerun. No native artifact, generated build ID, staging or commit is included.

## Next action

- Begin only the independent Goal 3 review. Do not stage, commit, publish or run live-provider traffic as part of Goal 2.

## Continuation handoff

TP-01 through TP-17 are complete. The canonical implementation and acceptance evidence remain uncommitted and unstaged for Goal 3 review.

### 2026-07-12 — Provider-neutral New template and unified send lifecycle

- Added the provider-neutral lifecycle ADR at `docs/architecture/chat/new-chat-template/ADR-001-persistent-hidden-new-chat-template.md`; the Compatible plan and matrix reference it instead of treating New-chat ownership as a provider feature.
- Added hidden `project.system_key = 'new'` and unique `convo.system_key = 'new_template'` identities, repository/read-model filtering, mutation guards, revisioned reset, startup navigation settings, last-formal restoration and idempotent materialization receipts.
- First send now flushes the current draft, holds one renderer orchestration lock and completes capability/attachment/Compatible main-owned eligibility before materialization. The single SQLite transaction creates the Inbox conversation, new Main branch, user message, assistant placeholder, asset bindings and—when Compatible is selected—the immutable route provenance and choice. Builder, credential read and transport remain post-commit.
- Removed the renderer Compatible selection event and dedicated selection state. Native and `openai_chat_compatible` picker results use one event and `ChatSessionConfig`; the composer reads one running state for Send/Stop behavior.
- Preserved D16 without qualification: all four proxy modes retain their selected route and transport; `compatibility_first` performs pre-request/per-redirect address checks, while unavailable `strict_ssrf` blocks during main-owned eligibility before body construction, credential read or egress. No route, transport or security-policy fallback was added.
- Independent P0/P1 review initially found draft-debounce divergence, attachment post-materialization preparation, receipt deletion RESTRICTs, post-commit Compatible eligibility, invalid prepared-to-failed transitions, send re-entry and startup identity races. These findings were addressed with strict draft flush, pre-materialization send plans, receipt cascades, atomic Compatible provenance, prepared-to-interrupted failure handling, the orchestration lock and IMMEDIATE system-identity transactions.
- Final Node acceptance: `npx vitest --run compatible` passed 56 files / 516 tests; the full attachment UI regression passed 48 tests; focused New/template/route/settings/picker/session suites passed. TypeScript, Vue TypeScript, i18n, canonical focused ESLint (0 errors), no-legacy, network-egress and diff checks passed. Repository-wide `lint:changed` remains blocked only by the same eight retained DeepSeek/OpenRouter Electron→`src/next` restricted imports recorded before this session-layer work.
- Final Electron no-egress E2E passed after exercising provider/catalog setup, template config and draft, main-owned eligibility, atomic template materialization with Compatible provenance, hidden/reset assertions, streaming projection, reload persistence, historical identity and abort. It used the development-only controlled fixture and no real network or credential.
- ABI record: no ABI mismatch occurred. `npm run rebuild:node` preceded Node/Vitest acceptance; final `npm run rebuild:electron` and the Electron E2E left the active ABI target as Electron. No native artifact or generated build output was staged or committed.
- All work remains uncommitted and unstaged. No live provider request, real credential, user-data reset, staging or commit was performed.
