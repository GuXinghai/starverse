# Document Format Conversion Progress Ledger

This ledger is append-only. New rounds should add entries without rewriting prior round facts, except to correct a factual error with an explicit note.

| Round | Date | Branch | Commit | Objective | Files changed | Production code changed | Docs changed | Tests run | Validation result | Risks | Owner approval required | Next recommended round |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DFC-0 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | Not committed at initial record creation | Create the isolated docs-only foundation for the document format conversion and preview system. This round only creates the topic directory and context carrier files. | `docs/file-pipeline/document-format-conversion/starverse_format_conversion_preview_v1_2.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production functionality implemented. No schema, Send Plan, UI, test configuration, external engine, dependency, `src/**`, `infra/**`, or `electron/**` changes. | Yes. Added the v1.2 SSOT contract copy and two support documents only. | `git status --short`; `git diff --check`. Full tests not run because this is docs-only. | Passed after DFC-0 edits. | Existing legacy format-conversion docs remain untouched; their references were not changed in this round. Future implementation may require DB, asset model, Send Plan, preview, UI, and test decisions. | Yes, before continuing beyond DFC-0 or entering any production implementation. | DFC-1: read-only repository mapping. |
| DFC-1 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `72cee48` | Read-only repository mapping against the v1.2 contract before implementation. | `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production code, schema, Send Plan, UI, dependency, external engine, or test files changed. | Yes. Appended this ledger row and added durable recovery notes to important context. | `git status --short --branch`; `git rev-parse --short HEAD`; targeted `rg` symbol/path mapping; `rg --files tests/e2e`; no automated tests because this was a read-only mapping round. | Mapping completed. Existing pipeline has raw asset, derivative, draft/message attachment, send-plan, preview, UI, and Vitest coverage seams, but vocabulary and persistence do not yet satisfy v1.2. | Current code uses `native_file`/`hybrid`/`unsupported` send targets and `preferredSendMode`/`selectedSendMode`, not `original_file`/`selectedOptionId`. Message attachments do not explicitly persist `usedOptionId`/`usedAssetRefs`. Playwright attachment smoke appears absent. IPC/message asset DTOs expose path/fileUrl/full hash fields that require privacy review. | Yes before production implementation if DB attachment semantics, selected option ownership, Send Plan replacement, or privacy boundary changes are needed. | DFC-2: owner-aligned implementation design memo and minimal Phase 1 contract slice plan before code changes. |
| DFC-2 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `72cee48` | Produce the owner-aligned implementation design memo for isolated replacement, legacy quarantine, no silent fallback, Phase 1 scope, privacy boundary, migration impact, and test plan. | `docs/file-pipeline/document-format-conversion/dfc-2-implementation-design-memo.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No `src/**`, `infra/**`, `electron/**`, `tests/**`, DB schema, Send Plan behavior, UI behavior, IPC contracts, package files, dependencies, or conversion logic changed. | Yes. Added DFC-2 memo, appended this ledger row, and updated durable context for frozen owner decisions and recovery. The v1.2 SSOT was not modified. | `git status --short --branch`; `git rev-parse --short HEAD`; `git diff --check`; Markdown trailing-whitespace scan; doc consistency subagent. Full tests not run because this round changed Markdown only. | Passed. Design records isolated replacement as the approved strategy and identifies DB migration, privacy DTO, legacy bridge, and Playwright harness decisions before code. Doc consistency check found no must-fix conflicts or overclaims. | Durable v1.2 compliance likely requires DB migration for draft/message bindings. Existing derivative schema may or may not fully express `DerivedAsset`. Current renderer DTO privacy boundary needs remediation before or during Phase 1. | Yes before code that changes DB semantics, DFC-managed markers, privacy DTO contracts, explicit legacy bridges, Playwright harness, dependencies, or external engines. | DFC-3: owner approval checkpoint for schema/DTO/DerivedAsset facade decisions, then first Phase 1 implementation slice. |
| DFC-2A | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `2176a2a` | Commit the DFC docs foundation, DFC-1 mapping record, DFC-2 design memo, ledger, and recovery context before any implementation work. | `docs/file-pipeline/document-format-conversion/starverse_format_conversion_preview_v1_2.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md`; `docs/file-pipeline/document-format-conversion/dfc-2-implementation-design-memo.md` | No. The commit contains only DFC Markdown docs. | Yes. Created commit `2176a2a` with message `docs(file-conversion): establish DFC foundation and design memo`. | `git status --short --branch`; `git diff --check`; staged-file whitelist check for `docs/file-pipeline/document-format-conversion/**`. | Passed. Only DFC documentation files were staged and committed. | The committed SSOT retains source Markdown hard-break whitespace; it was not rewritten. | No for the docs commit itself. Owner approval remains required before implementation changes listed by DFC-2. | DFC-3: freeze owner decisions before code. |
| DFC-3 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `2176a2a` | Freeze owner decisions before Phase 1 implementation: selectedOptionId authority, DFC-managed boundary, target vocabulary, draft/message binding, DerivedAsset facade strategy, privacy DTO boundary, implementation order, and deferred scope. | `docs/file-pipeline/document-format-conversion/dfc-3-owner-decision-freeze.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No `src/**`, `infra/**`, `electron/**`, `tests/**`, DB schema, Send Plan behavior, UI behavior, IPC contracts, package files, dependencies, or conversion logic changed. | Yes. Added the DFC-3 decision freeze and updated durable context. | `git status --short --branch`; `git diff --check`; Markdown trailing-whitespace scan; doc consistency subagent. Full tests not run because this round changed Markdown only. | Passed after clarifying that `pdf_attachment` is a contract value only and not a Phase 1 implementation item. Doc consistency found no production overclaim or SSOT conflict. | DB migration shape, `DerivedAsset` facade feasibility, sanitized DTO shape, legacy migration bridge scope, and Playwright harness feasibility still need owner approval before code. | Yes before DFC-4 code involving DB migration, DFC-managed markers, sanitized DTO changes, dedicated DerivedAsset table, migration bridge, Playwright harness creation, dependencies, or external engines. | DFC-4: smallest approved code slice, starting with TS DFC contracts and sanitized DTO boundary if no DB migration is required first. |
| DFC-4 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `a52fc9c` | Add the first code slice: shared DFC TypeScript contracts, an unintegrated pure no-silent-fallback resolver scaffold, sanitized renderer DTO draft, and targeted Vitest coverage. | `src/shared/files/documentFormatConversion.ts`; `src/shared/files/documentFormatConversion.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes, but only shared-domain TypeScript contracts, pure scaffold, and tests. No DB schema, production Send Plan behavior, UI, IPC runtime, package files, dependencies, conversion implementation, Playwright harness, or migration bridge changed. | Yes. Updated ledger and recovery context for the DFC-4 slice. | `npx vitest --run src/shared/files/documentFormatConversion.test.ts`; `npx tsc -p tsconfig.json --noEmit`; `git diff --check`; `git status --short --branch`; scoped forbidden-path scan; risk_reviewer; doc_consistency. | Targeted Vitest passed with 15 tests. Risk review found no P0/P1 issues. `git diff --check` passed. Repo-wide typecheck failed in pre-existing UI Vue export typing at `src/ui-app/app/appChatApp.logic.ts:178-179`, unrelated to DFC-4 files. | DFC-4 is not wired into production Send Plan and does not prove runtime behavior yet. DB migration shape, DerivedAsset facade sufficiency, sanitized DTO runtime shape, legacy migration bridge scope, and Playwright harness feasibility remain open. | Yes before DFC-5 changes involving DB migration, production Send Plan wiring, IPC runtime DTO changes, UI, dedicated DerivedAsset table, migration bridge, Playwright harness creation, dependencies, or external engines. | DFC-5: owner-approved runtime boundary round, likely choosing either sanitized DTO runtime change or DB binding migration plan before any Send Plan integration. |
| DFC-5 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `8fdd187` | Freeze Phase 1 runtime boundary decisions before production wiring: sanitized DTO strategy, DerivedAsset facade feasibility, DB binding migration shape, and legacy quarantine enforcement. DFC-4 was committed at the start of this round as `8fdd187`. | `docs/file-pipeline/document-format-conversion/dfc-5-runtime-boundary-decision.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production code, DB schema, Send Plan behavior, UI behavior, IPC runtime contract, dependencies, conversion logic, Playwright harness, external engines, or migration bridge changed in DFC-5. | Yes. Added DFC-5 memo and updated durable recovery context. | `git status --short --branch`; read-only code mapping via code_mapper; targeted `rg` and line reads for DTO, derivative, DB binding, and Send Plan seams; `git diff --check`; risk_reviewer; doc_consistency. Full tests not run because DFC-5 changed Markdown docs only. | Passed. `git diff --check` passed with LF/CRLF warnings only. Risk review found no P0/P1 issues. Doc consistency found only a stale pending-validation wording, corrected in this row. | Recommendations still require owner approval before code: sanitized runtime DTO contract, explicit DB migration, file-derivatives metadata convention as the DFC `DerivedAsset` facade, production Send Plan branch, UI consumption, dedicated table, or legacy bridge. | Yes before DFC-6 code that changes IPC runtime DTOs, DB schema, production Send Plan, UI, DerivedAsset storage semantics, legacy migration, dependencies, external engines, or Playwright harness. | DFC-6: owner-approved sanitized DFC runtime DTO slice with privacy tests only; do not change DB schema or production Send Plan in the same round. |
| DFC-6 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `985c53e` | Implement the safe additive DFC runtime boundary slice, then stop before durable DB binding migration. | `src/shared/files/documentFormatConversion.ts`; `src/shared/files/documentFormatConversion.test.ts`; `src/next/ipc/contracts/dbBridgeContracts.ts`; `src/next/ipc/contracts/dbBridgeContracts.test.ts`; `docs/file-pipeline/document-format-conversion/dfc-6-owner-memo-db-binding-migration.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added DFC sanitized renderer DTO decoders and tests, plus a strict DerivedAsset facade helper and tests. No DB schema, production Send Plan behavior, UI behavior, conversion implementation, dependencies, external engines, Playwright harness, or legacy migration bridge changed. | Yes. Added an owner blocker memo and updated recovery context. | `npx vitest --run src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts`; `npx tsc --noEmit -p tsconfig.json --pretty false`; `git diff --check`; privacy `rg` scan over touched DFC/DTO files; code_mapper; risk_reviewer. | Targeted Vitest passed: 2 files, 197 tests. `git diff --check` passed with LF/CRLF warnings only. Risk review found no P0/P1 issues. Project typecheck still fails only in pre-existing Vue export typing at `src/ui-app/app/appChatApp.logic.ts:178-179`, unrelated to DFC-6. | DFC-managed Send Plan, durable draft/message binding, Phase 1 option generation runtime wiring, Playwright smoke, and message snapshot semantics remain blocked until owner approves DB binding migration. | Yes. Owner approval is required before adding DFC binding columns or any alternative durable storage, before Send Plan wiring, UI consumption, legacy bridge, dependencies, external engines, or Playwright harness creation. | Owner decision: approve DB binding migration shape, recommended explicit columns on `draft_attachments` and `message_attachments`, then implement DFC-7 migration/repo binding tests before Send Plan wiring. |
| DFC-7 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Implement owner-approved destructive dev migration and explicit repository persistence for DFC draft/message attachment bindings, without production Send Plan wiring. | `infra/db/schema.sql`; `infra/db/migrations/ensureFilePipelineSchema.ts`; `infra/db/migrations/ensureFilePipelineSchema.test.ts`; `infra/db/types.ts`; `infra/db/validation.ts`; `infra/db/repo/dfcAttachmentBinding.ts`; `infra/db/repo/conversationDraftRepo.ts`; `infra/db/repo/messageAttachmentRepo.ts`; `infra/db/repo/filePipelineRepo.test.ts`; `infra/db/worker.filePipeline.test.ts`; `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added explicit DFC binding columns, migration logic, repo/type/validation mapping, and targeted DB/repo/service/worker tests. No production Send Plan wiring, UI, CSV/TSV conversion runtime, DerivedAsset dedicated table, Playwright harness, package files, dependencies, external engines, or legacy migration bridge changed. | Yes. Appended this row and updated recovery context for the destructive dev migration decision, risk-review fixes, and remaining Send Plan boundary. | `npm run rebuild:node`; `npx vitest --run infra/db/migrations/ensureFilePipelineSchema.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts infra/db/worker.filePipeline.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx tsc --noEmit -p tsconfig.json --pretty false`; `git diff --check`; scoped forbidden-path and privacy diff scans; code_mapper; risk_reviewer; test_runner; doc_consistency. | Targeted Vitest passed after final risk-review fixes: 5 files, 59 tests. `git diff --check` passed with LF/CRLF warnings only. Risk review found no remaining P0/P1 issues. Full typecheck still fails only in pre-existing Vue export typing at `src/ui-app/app/appChatApp.logic.ts:178-179`, unrelated to DFC-7. | Message `targetKind` and `sendStrategy` are nullable until the future Send Plan seam supplies the actual selected option snapshot. Existing legacy columns remain physically present but are quarantined and cleared for DFC-managed writes. The migration remains intentionally destructive for old `draft_attachments` or `message_attachments` schemas missing DFC binding columns, but clearing is table-scoped and transactional. Production Send Plan still uses legacy behavior until DFC-8. | Yes before production Send Plan wiring, UI consumption, CSV/TSV conversion runtime, Playwright harness creation, external engines, new dependencies, legacy bridge, or any broader data clearing beyond attachment tables. | DFC-8: narrow selectedOptionId-driven Send Plan resolver seam using the persisted DFC fields; keep UI and conversion runtime out unless separately approved. |
| DFC-8R0 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `6eacf67` | Re-map the current repository against the v1.2 contract after DFC-7 and before new implementation work. | `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production code, schema, Send Plan behavior, UI behavior, tests, package files, dependencies, conversion runtime, Playwright harness, external engines, or legacy bridge changed. | Yes. Appended this read-only mapping row and added compact recovery notes to important context. | `git status --short --branch`; `git rev-parse --short HEAD`; `git log -1 --oneline`; targeted `rg --files` mapping; code_mapper. No automated tests because this round changed only DFC documentation after read-only mapping. | Mapping completed. DFC shared contracts, sanitized DTO helpers, DerivedAsset facade, and DB binding columns now exist. Production Send Plan is still not `selectedOptionId`-authoritative, preview/send lineage checks are still route-derived rather than selected-option-derived, and message `targetKind` / `sendStrategy` snapshots are still incomplete. | Live Send Plan still contains legacy `native_file` / `hybrid` / `unsupported` semantics and `selectAttachmentSendModeInternal`. DFC renderer privacy work exists, but mixed legacy DTO surfaces and lineage hash fields still need review before UI use. Playwright attachment smoke was not verified. | Yes before production Send Plan wiring, UI consumption, conversion runtime, Playwright harness creation, external engines, new dependencies, legacy bridge, or broader replacement of legacy Send Plan behavior. | DFC-8: owner-approved narrow selectedOptionId-driven Send Plan seam that reuses persisted DFC bindings and keeps legacy behavior quarantined to non-DFC rows. |
| DFC-8 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `6eacf67` | Implement the narrow selectedOptionId-driven Send Plan seam for DFC-managed draft attachments without broad legacy replacement. | `src/shared/files/sendPlanTypes.ts`; `src/next/ipc/contracts/dbBridgeContracts.ts`; `src/next/ipc/contracts/dbBridgeContracts.test.ts`; `src/next/openrouter/openRouterSendPlanSerializer.test.ts`; `infra/files/sendPlanService.ts`; `infra/files/sendPlanService.test.ts`; `infra/db/worker/runtime.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. DFC-managed draft attachments now collect persisted `selectedOptionId` / `selectedAssetRefs`, resolve selected `original_file` and selected derived-asset refs through the DFC resolver before legacy mode selection, expose `original_file` and `sendAssetRefs` in the Send Plan shape and IPC decoder, and keep non-DFC rows on the legacy path. No DB schema, UI, conversion runtime, dependencies, external engines, Playwright harness, or legacy migration bridge changed. | Yes. Appended this ledger row and updated recovery context for the Send Plan seam. | `npm run rebuild:node`; `npx vitest --run infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/openrouter/openRouterSendPlanSerializer.test.ts`; `npx vitest --run infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts infra/db/worker.filePipeline.test.ts`; `npx tsc --noEmit -p tsconfig.json --pretty false`; `git diff --check`; targeted `rg` over selected/asset refs; privacy/log diff scans; code_mapper; risk_reviewer; test_runner. | Targeted Vitest passed: 4 files, 266 tests. Additional worker-inclusive Vitest passed: 4 files, 251 tests. `git diff --check` passed with LF/CRLF warnings only. Risk review found no P0/P1 issues. Full typecheck still fails only in pre-existing Vue named-export typings at `src/ui-app/app/appChatApp.logic.ts:178-179`, unrelated to DFC-8. | DFC-8 reconstructs selected options from persisted refs and derivative metadata because durable option rows do not exist yet. `optionGenerationState` is not persisted, so missing selection vs pending generation remains coarse in production. Multi-derived mixed-target refs are not a Phase 1 use case and are only minimally guarded. Message `targetKind` / `sendStrategy` snapshots still need a future send-time persistence seam. Playwright smoke and UI option selection remain unimplemented. | Yes before UI consumption, conversion runtime beyond existing text derivative metadata, message send-time snapshot updates, Playwright harness creation, external engines, new dependencies, legacy bridge, or broad Send Plan replacement. | DFC-9: bind successful DFC send snapshots into `MessageAttachment.targetKind` / `sendStrategy` at send/commit time, or first write an owner memo if the send pipeline lacks a narrow snapshot seam. |
| DFC-9 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `6eacf67` | Bind successful DFC send snapshots into `MessageAttachment.targetKind` and `sendStrategy` at send/commit time through a narrow attachment-id-keyed seam. | `src/shared/files/documentFormatConversion.ts`; `infra/db/types.ts`; `infra/db/validation.ts`; `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `infra/db/worker/handlers/branchContextHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `src/next/branch/branchClient.ts`; `src/next/files/conversationDraftClient.ts`; `src/ui-app/app/appChatApp.logic.ts`; `src/next/openrouter/openRouterSendPlanSerializer.ts`; `src/next/openrouter/openRouterSendPlanSerializer.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added `DfcAttachmentSendSnapshot`, forwards DFC draft attachment send-plan snapshots through `beginTurn` and `conversationDraft.attachToMessage` / `commitToUserMessage`, validates snapshot asset/ref/target coherence before message persistence, stores `MessageAttachment.targetKind` and `sendStrategy`, and prevents `original_file` text attachments from using derived converted-text payloads. No DB schema, dependencies, external engines, Playwright harness, broad Send Plan replacement, or legacy migration bridge changed. | Yes. Appended this row and updated recovery context for the send/commit snapshot seam. | `npm run rebuild:node`; `npx vitest --run infra/files/conversationAttachmentService.test.ts infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/openrouter/openRouterSendPlanSerializer.test.ts`; `npx vitest --run src/next/openrouter/openRouterSendPlanSerializer.test.ts infra/files/conversationAttachmentService.test.ts infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts`; `npx tsc --noEmit -p tsconfig.json --pretty false`; `git diff --check`; production privacy/log scan; code_mapper; risk_reviewer; test_runner. | Focused Vitest passed after the serializer fix: 6 files, 290 tests. `git diff --check` passed with LF/CRLF warnings only. Follow-up risk review found no P0/P1 issues and confirmed the prior `original_file` serializer P1 was resolved. Full typecheck still fails only in pre-existing Vue named-export typings at `src/ui-app/app/appChatApp.logic.ts:179-180`, unrelated to DFC-9. | DFC snapshots are currently forwarded only for send-plan entries present in the UI flow; direct callers that omit `dfcAttachmentSendSnapshots` retain nullable message `targetKind` / `sendStrategy` for compatibility. Durable option rows, persisted option-generation state, UI option selection, Playwright smoke, conversion runtime expansion, and history-attachment DFC semantics remain unimplemented. | Yes before UI option selection, conversion runtime expansion, Playwright harness creation, external engines, new dependencies, legacy bridge, broad Send Plan replacement, or changing compatibility behavior for callers that omit snapshots. | DFC-10: implement the minimal Phase 1 option-selection / preview UI bridge using sanitized DFC DTOs, or perform a UI/Playwright feasibility mapping first if the existing harness cannot cover the v1.2 smoke. |
| DFC-10 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `6eacf67` | Perform UI and Playwright feasibility mapping before implementing the Phase 1 option-selection / preview bridge, then stop for owner decisions. | `docs/file-pipeline/document-format-conversion/dfc-10-ui-playwright-feasibility-owner-memo.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production code, DB schema, Send Plan behavior, UI behavior, conversion runtime, dependencies, external engines, Playwright harness, tests, or legacy bridge changed. | Yes. Added the DFC-10 owner memo, appended this ledger row, and updated recovery context. | `git status --short --branch`; `git rev-parse --short HEAD`; targeted UI/IPC/test harness mapping; code_mapper; `git diff --check`; doc_consistency. Automated tests not run because DFC-10 changed documentation only. | Passed. Mapping found existing attachment strip/card/details UI and sanitized DFC DTO seams, but no owned draft option-candidate endpoint and no browser Playwright harness. `git diff --check` passed with LF/CRLF warnings only. Doc consistency found no required corrections. | UI option selection would need an owned source of candidate options before renderer controls can edit `selectedOptionId` / `selectedAssetRefs`. A real browser Playwright smoke would require new harness scaffolding. Inferring choices from current Send Plan rows would risk inventing option identity and violating v1.2 authority. | Yes. Owner approval is required before adding a backend-owned DFC draft option DTO/update contract or before creating browser Playwright harness scaffolding. | DFC-11: owner-approved backend-owned sanitized DFC draft option DTO endpoint and selected-option update contract; defer visible UI picker and Playwright harness to later bounded rounds. |
| DFC-11R0 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `6eacf67` | Revalidate whether any DFC-11 implementation slice can proceed without the DFC-10 owner approvals. | `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production code, DB schema, Send Plan behavior, UI behavior, conversion runtime, dependencies, external engines, Playwright harness, tests, or legacy bridge changed. | Yes. Appended this ledger row and added a compact recovery note that the owner gate still applies. | `git status --short --branch`; `git rev-parse --short HEAD`; targeted reads of DFC docs and client/service seams; code_mapper; `git diff --check`; doc_consistency. Automated tests not run because this round changed documentation only. | Passed. Revalidation found no safe production implementation slice: only documentation or characterization tests could proceed without owner approval, and those do not implement the required DFC option DTO/update contract. `git diff --check` passed with LF/CRLF warnings only. Doc consistency found no required corrections. | Proceeding without approval would either invent renderer-side option identity, expose an underspecified DTO/privacy boundary, desynchronize `selectedOptionId` and `selectedAssetRefs`, or create new browser Playwright scaffolding outside the approved plan. | Yes. Owner approval is still required before the backend-owned DFC draft option DTO/update contract or browser Playwright harness work. | DFC-11: approve and implement the backend-owned sanitized DFC draft option DTO endpoint plus selected-option update contract; keep visible UI picker and Playwright harness for later rounds. |
| DFC-11R1 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | `6eacf67` | Complete the blocked audit for the repeated DFC-11 owner gate after three consecutive continuation rounds. | `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production code, DB schema, Send Plan behavior, UI behavior, conversion runtime, dependencies, external engines, Playwright harness, tests, or legacy bridge changed. | Yes. Appended this blocked-audit row and added durable context that the same blocker has repeated across DFC-10, DFC-11R0, and DFC-11R1. | `git status --short --branch`; `git rev-parse --short HEAD`; targeted reads of DFC-10 memo, ledger, and important context; code_mapper; `git diff --check`. Automated tests not run because this round changed documentation only. | Passed. The same owner gate remains: no explicit owner approval is present for the backend-owned DFC draft option DTO/update contract or for browser Playwright harness scaffolding. `git diff --check` passed with LF/CRLF warnings only. | The project is at an impasse for further production DFC work without external owner input. Continuing implementation would violate the recorded stop conditions and risk incorrect option authority, privacy leaks, stale selections, or unapproved E2E scaffolding. | Yes. Owner approval is required to unblock the next production round. | DFC-11 after unblock: approve and implement the backend-owned sanitized DFC draft option DTO endpoint plus selected-option update contract; defer visible UI picker and Playwright harness unless separately approved. |
| DFC-11 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Implement the owner-approved backend-owned sanitized DFC draft option DTO endpoint and selected-option update contract, after first committing the coherent DFC-8/9/10/11 recovery diff as `0f40a84`. | `src/shared/files/documentFormatConversion.ts`; `infra/db/types.ts`; `infra/db/validation.ts`; `infra/db/dbMethodsRegistry.ts`; `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/files/conversationAttachmentService.ts`; `src/next/ipc/contracts/dbBridgeContracts.ts`; `src/next/files/conversationDraftClient.ts`; `infra/files/conversationAttachmentService.test.ts`; `infra/db/worker.filePipeline.test.ts`; `src/next/ipc/contracts/dbBridgeContracts.test.ts`; `src/next/files/conversationDraftClient.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added `conversationDraft.getDfcOptions`, backend-generated draft option DTOs from raw file plus eligible derived assets, selected-option update validation against backend-owned option ids and exact `SendAssetRef` matches, renderer IPC/client DTO decoding, and targeted no-silent-fallback/privacy tests. No DB schema, UI redesign, Playwright harness, dependencies, external engines, broad Send Plan replacement, conversion runtime expansion, legacy bridge, XLSX/Office/PDF/HTML/PS-EPS runtime, or migration bridge changed. | Yes. Appended this row and updated recovery context with owner approval, implemented seams, validation, and remaining scope. | `npm run rebuild:node`; `npx vitest --run infra/files/conversationAttachmentService.test.ts infra/db/worker.filePipeline.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run infra/files/sendPlanService.test.ts src/next/openrouter/openRouterSendPlanSerializer.test.ts`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; option-identity grep; privacy/log diff scan; risk_reviewer; doc_consistency. | Targeted Vitest passed after the canonical-option fix: 5 files / 239 tests and 2 files / 70 tests. `git diff --check` passed with LF/CRLF warnings only. Privacy/log scan found only internal backend `storageUri` metadata reads, with no new renderer/log path, file URL, content token, file body, or full-hash exposure. Follow-up risk review and doc consistency passed. Full project typecheck still fails only in the pre-existing Vue named-export typings at `src/ui-app/app/appChatApp.logic.ts:179-180`, unrelated to DFC-11. | Option ids are backend-generated from current raw/derived state; preexisting non-canonical persisted selections now produce `needs_user_selection` / `selected_option_not_found` until updated with a backend-issued option id. There are still no durable ConversionOption rows or persisted option-generation state. The endpoint is UI-facing plumbing only; visible option picker, preview panel work, browser Playwright smoke, broader Send Plan replacement, and conversion-runtime expansion remain deferred. Direct service/repo test helpers can still create DFC bindings for lower-level tests, but renderer client selection now goes through `getDfcOptions` plus `updateAttachmentSettings`. | No for the implemented DFC-11 slice because owner approval was granted in-thread. Owner approval remains required before visible UI picker, browser Playwright harness, new dependencies, external engines, legacy compatibility bridge, broad Send Plan replacement, or forbidden conversion-runtime families. | DFC-12: wire the existing attachment detail UI to consume the backend-owned DTO and update selected options, then plan Playwright smoke separately unless owner approves harness work. |
| DFC-12 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Wire the existing attachment details dialog to consume backend-owned DFC draft options and persist selected options without a broad UI redesign or new Playwright harness. | `src/ui-app/app/appChatApp.logic.ts`; `src/ui-app/AppChatApp.vue`; `src/ui-app/components/DraftAttachmentDetailsDialog.vue`; `src/ui-app/AppChatApp.attachments.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. The existing details flow now fetches `conversationDraft.getDfcOptions` on open, renders backend-owned option candidates, and persists selected options through `conversationDraft.updateAttachmentSettings` with `dfcManaged`, `selectedOptionId`, and `selectedAssetRefs`. No DB schema, backend conversion runtime, Send Plan replacement, new dependencies, external engines, Playwright harness, legacy bridge, or broad UI redesign changed. | Yes. Appended this row and updated recovery context for the UI-plumbing seam. | `npx vitest --run src/ui-app/AppChatApp.attachments.test.ts src/next/files/conversationDraftClient.test.ts`; `npx vitest --run infra/files/conversationAttachmentService.test.ts infra/db/worker.filePipeline.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts infra/files/sendPlanService.test.ts src/next/openrouter/openRouterSendPlanSerializer.test.ts`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; UI privacy/log diff scan; risk_reviewer; doc_consistency. | UI attachment/client Vitest passed, and focused backend/contract/send-plan Vitest passed: 5 files / 283 tests. `git diff --check` passed with LF/CRLF warnings only. UI privacy/log scan found no new renderer path, file URL, storage URI, content token, file body, base64, or full-hash exposure. Risk review found no P0/P1 issues. Doc consistency passed. Full project typecheck still fails only in the pre-existing Vue named-export typings at `src/ui-app/app/appChatApp.logic.ts:181-182`, unrelated to DFC-12. | This is only the minimal existing-dialog option picker plumbing. It does not add a full detail inspector, browser Playwright smoke, preview visibility assertions, conversion runtime expansion, durable ConversionOption rows, persisted option-generation state, or broad Send Plan replacement. | No for this narrow UI-plumbing slice under the approved UI boundary. Owner approval remains required before adding a new Playwright harness, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, or forbidden conversion-runtime families. | DFC-13: plan and implement approved browser smoke coverage or continue backend/preview wiring if Playwright harness approval is not granted. |
| DFC-13 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Tighten backend DFC preview/send coherence by validating selected derived assets through the DFC `DerivedAsset` facade and redacting DFC lineage in Send Plan DTOs. | `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `infra/files/sendPlanService.ts`; `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Backend option candidates now mark malformed or preview-only derived assets unavailable from DFC facade metadata. Send Plan now derives DFC-managed lineage from selected `SendAssetRef` records and `file_derivatives`, blocks preview-only, missing, deleted, malformed, or mismatched derived assets, and never falls back to raw asset lineage for DFC-managed attachments. No DB schema, IPC contract, UI redesign, Playwright harness, new dependency, external engine, legacy bridge, broad Send Plan replacement, or forbidden conversion-runtime family changed. | Yes. Appended this row and updated recovery context. | `npx vitest --run infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/shared/files/documentFormatConversion.test.ts src/next/openrouter/openRouterSendPlanSerializer.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log diff scans; code_mapper; risk_reviewer. | Focused DFC service Vitest passed: 2 files / 66 tests. Broader targeted worker/contract/client/shared/serializer/UI suite passed. `git diff --check` passed with LF/CRLF warnings only. Privacy scans found no new log/path/file URL/content token/file body leaks; internal storage URI reads remain backend-only. Initial risk review found a P1 hash-leak path for non-ready DFC decisions; it was fixed by redacting all DFC-managed fallback lineage, and follow-up risk review passed with no P0/P1. Full project typecheck still fails only in the pre-existing Vue named-export typings at `src/ui-app/app/appChatApp.logic.ts:181-182`, unrelated to DFC-13. | This is backend send-plan coherence, not a renderer-visible selected-option preview DTO. Send Plan lineage for DFC-managed attachments intentionally returns null hash fields to avoid leaking full hashes over IPC. Durable ConversionOption rows, persisted option-generation state, browser Playwright smoke, full preview inspector, and selected-option preview DTOs remain deferred. | No for this backend-only slice. Owner approval is still required before adding a new Playwright harness, selected-option preview DTO/API, new dependencies, external engines, broad UI redesign, legacy compatibility bridge, broad Send Plan replacement, or forbidden conversion-runtime families. | DFC-14: decide whether to add a backend-owned selected-option preview DTO/API for renderer-visible preview/send coherence, or obtain owner approval for browser smoke harness work. |
| DFC-14 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add a backend-owned selected-option DFC preview DTO/API that reads preview content from the same selected option and `SendAssetRef` authority used for send. | `src/shared/files/documentFormatConversion.ts`; `infra/db/types.ts`; `infra/db/validation.ts`; `infra/db/dbMethodsRegistry.ts`; `infra/db/worker/runtime.ts`; `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/files/conversationAttachmentService.ts`; `src/next/ipc/contracts/dbBridgeContracts.ts`; `src/next/files/conversationDraftClient.ts`; `infra/files/conversationAttachmentService.test.ts`; `infra/files/sendPlanService.test.ts`; `infra/db/worker.filePipeline.test.ts`; `src/next/ipc/contracts/dbBridgeContracts.test.ts`; `src/next/files/conversationDraftClient.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added `conversationDraft.getDfcPreview`, a sanitized DFC preview DTO/decoder/client, worker validation/registry routing, backend selected-option preview resolution, metadata-only `original_file` preview, capped text preview for Phase 1 text targets, and backend coherence guards so corrupted persisted `selectedAssetRefs` block preview and message binding. No DB schema, UI redesign, Playwright harness, new dependency, external engine, legacy bridge, broad Send Plan replacement, or forbidden conversion-runtime family changed. | Yes. Appended this row and updated recovery context. | `npm run rebuild:node`; `npx vitest --run infra/files/conversationAttachmentService.test.ts infra/db/worker.filePipeline.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run infra/files/sendPlanService.test.ts src/next/openrouter/openRouterSendPlanSerializer.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log diff scan; code_mapper; risk_reviewer. | Focused DFC/IPC/client/shared suite passed: 5 files / 248 tests. Broader send-plan/serializer/UI suite passed: 3 files / 134 tests. `git diff --check` passed with LF/CRLF warnings only. Privacy scan found only backend internal storage URI reads and tests asserting sensitive fields are stripped. Initial risk review found a P1 selected-option/ref mismatch path; the fix blocks preview before reading content and blocks message binding when persisted refs disagree with the backend-selected option. Follow-up risk review passed. Full project typecheck still fails only in the pre-existing Vue named-export typings at `src/ui-app/app/appChatApp.logic.ts:181-182`, unrelated to DFC-14. | DFC-14 exposes a renderer client wrapper but does not yet render the preview inside the attachment details UI. Text preview intentionally returns capped derived text content only through the explicit preview endpoint; it does not expose paths, storage refs, content tokens, raw file body, or full hashes. Durable ConversionOption rows, persisted option-generation state, browser Playwright smoke, and a full preview inspector remain deferred. | No for this backend-owned additive DTO/API slice. Owner approval remains required before adding a new Playwright harness, external engines, new dependencies, legacy compatibility bridge, broad UI redesign, broad Send Plan replacement, or forbidden conversion-runtime families. | DFC-15: wire the existing attachment details dialog to display the selected-option preview DTO if it remains a small local extension; separately seek owner approval before any new browser Playwright harness. |
| DFC-15 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Wire the existing attachment details dialog to display the backend-owned selected-option DFC preview DTO without a broad UI redesign or new Playwright harness. | `src/ui-app/app/appChatApp.logic.ts`; `src/ui-app/components/DraftAttachmentDetailsDialog.vue`; `src/ui-app/AppChatApp.attachments.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. The existing attachment details flow now calls `conversationDraft.getDfcPreview` on dialog open, draft refresh, and selected-option update, stores preview DTO state separately from option/send-plan state, and renders backend-provided status, target kind, send strategy, capped text preview, raw-file selected state, and diagnostic codes. No renderer-inferred `selectedOptionId`, `SendAssetRef`, `raw_file`, `derived_asset`, or target-kind semantics were added. No DB schema, backend conversion runtime, broad UI redesign, Playwright harness, new dependency, external engine, legacy bridge, broad Send Plan replacement, or forbidden conversion-runtime family changed. | Yes. Appended this row and updated recovery context for DFC-15. | `npm run rebuild:node`; `npx vitest --run src/ui-app/AppChatApp.attachments.test.ts src/next/files/conversationDraftClient.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts`; `npx vitest --run infra/files/conversationAttachmentService.test.ts infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts src/next/openrouter/openRouterSendPlanSerializer.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; diff-only privacy/log scan; risk_reviewer. | Focused UI/client/IPC suite passed: 3 files / 278 tests. Focused backend/send-plan/shared suite passed: 5 files / 121 tests. `git diff --check` passed with LF/CRLF warnings only. Diff-only privacy scan found no added paths, storage refs/URIs, file URLs, content tokens, raw body, or full hashes. Risk review found no P0/P1 findings. Full project typecheck still fails only in the pre-existing Vue named-export typings at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to DFC-15. | DFC-15 displays the selected-option preview but does not add a full Attachment Detail Inspector, browser Playwright smoke, new harness, conversion runtime expansion, durable ConversionOption rows, persisted option-generation state, or broad Send Plan replacement. Preview text is intentionally displayed only from the explicit backend preview DTO. | No for this narrow existing-dialog preview display slice. Owner approval remains required before adding a new Playwright harness, external engines, new dependencies, legacy compatibility bridge, broad UI redesign, broad Send Plan replacement, or forbidden conversion-runtime families. | DFC-16: either obtain owner approval for browser Playwright harness scaffolding, or continue a non-harness backend conversion/runtime slice that remains within the approved Phase 1 scope and avoids XLSX/Office/PDF/HTML/PS-EPS. |
| DFC-16 | 2026-05-24 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Mark existing worker-generated text derivatives as DFC-ready for allowed Phase 1 text-like assets and close the raw bridge metadata privacy gap. | `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `src/next/ipc/contracts/dbBridgeContracts.ts`; `src/next/ipc/contracts/dbBridgeContracts.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. The existing send-plan worker bridge now backfills DFC facade metadata onto ready `extracted_text` derivatives for allowed text, Markdown, source-code, CSV, and TSV Phase 1 targets, while explicitly excluding HTML, PS/EPS, PDF, Office, and XLS/XLSX assets. Public renderer raw asset/derivative decoders now redact top-level full hashes and scrub private JSON metadata inside `sourceMetaJson` / `metaJson`, including nested DFC conversion metadata, storage refs, content tokens, file bodies, hash fields, converter identity, and path-like metadata, while preserving existing top-level fields such as `storageUri` where still required; DFC-specific sanitized decoders still use private parser state and expose only their approved DTO shape. No DB schema, dependency, external engine, UI redesign, Playwright harness, legacy bridge, broad Send Plan replacement, or forbidden conversion family runtime changed. | Yes. Appended this ledger row and updated recovery context for the worker bridge and privacy-boundary fix. | `npx vitest --run src/next/ipc/contracts/dbBridgeContracts.test.ts infra/db/worker.filePipeline.test.ts src/next/files/conversationDraftClient.test.ts infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run src/ui-app/AppChatApp.attachments.test.ts src/next/files/fileAssetClient.test.ts`; combined touched suite `npx vitest --run src/next/ipc/contracts/dbBridgeContracts.test.ts infra/db/worker.filePipeline.test.ts src/next/files/conversationDraftClient.test.ts infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts src/ui-app/AppChatApp.attachments.test.ts src/next/files/fileAssetClient.test.ts`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; diff privacy/log scans; risk_reviewer follow-up. | Targeted backend/contract suite passed: 6 files / 298 tests. Combined touched suite passed: 8 files / 378 tests. `git diff --check` passed with LF/CRLF warnings only. Follow-up risk review passed with no P0/P1 findings after fixing the raw bridge metadata exposure. Privacy scans found no added runtime logs or absolute paths; sensitive terms in the diff are limited to backend-internal metadata writes, sanitizer denylist keys, and regression tests proving redaction. Full project typecheck still fails only in the pre-existing Vue named-export typings at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to DFC-16. | This slice only makes derivatives DFC-ready when the existing worker send-plan bridge generates or rechecks an `extracted_text` derivative. It does not add upload-time option generation, durable ConversionOption rows, persisted option-generation state, browser Playwright smoke, full inspector work, or runtime conversion for HTML, PS/EPS, PDF, Office, or XLS/XLSX. Public raw file asset DTOs now deliberately redact `sha256` and private conversion metadata; focused UI/client tests passed, but any future renderer feature needing raw internal metadata must use a backend-owned sanitized endpoint instead. | No for this owner-approved backend/runtime and privacy-boundary slice. Owner approval remains required before adding a new Playwright harness, external engines, new dependencies, legacy compatibility bridge, broad UI redesign, broad Send Plan replacement, upload-time conversion orchestration, or forbidden conversion-runtime families. | DFC-17: either obtain owner approval for browser Playwright harness scaffolding, or make an explicit owner decision on conversion option generation timing before adding upload/open-triggered generation. |
| DFC-17 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Record the owner-level decision gate for DFC option generation timing and browser Playwright harness work after DFC-16. | `docs/file-pipeline/document-format-conversion/dfc-17-owner-memo-generation-trigger.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production code, DB schema, Send Plan behavior, UI behavior, conversion runtime, dependencies, external engines, Playwright harness, tests, or legacy bridge changed. | Yes. Added the DFC-17 owner memo, appended this ledger row, and updated recovery context. | `git status --short --branch`; `git rev-parse --short HEAD`; targeted reads of DFC docs and current seams; code_mapper; `git diff --check`; doc_consistency. Automated tests not run because this round changed documentation only. | Passed. Mapping confirmed `getDfcOptions`, `getDfcPreview`, and the existing details dialog are backend-owned read/display plumbing, while DFC-ready text derivative generation is still coupled to `sendPlan.buildCurrent`. No `playwright.config.*` or `@playwright/test` harness was found; existing `tests/e2e` files are Vitest smokes. `git diff --check` passed with LF/CRLF warnings only. | Proceeding into upload/open-triggered conversion generation would choose among multiple viable lifecycle designs and alter API/job semantics. Proceeding into browser smoke would require new Playwright harness scaffolding. Both are owner-level decisions under the active stop conditions. | Yes. Owner approval is required before implementing an explicit `conversationDraft.ensureDfcOptions` generation contract or before adding browser Playwright harness scaffolding. | DFC-18 after approval: implement an explicit backend-owned `conversationDraft.ensureDfcOptions` contract for approved Phase 1 text-like conversions, or separately approve and implement the browser Playwright harness. |
| DFC-17R0 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Revalidate the DFC-17 owner gate after recovering the clean worktree at `26adca2`. | `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production code, DB schema, Send Plan behavior, UI behavior, conversion runtime, dependencies, external engines, Playwright harness, tests, or legacy bridge changed. | Yes. Appended this ledger row and added blocked revalidation recovery notes. | `git status --short --branch`; `git rev-parse --short HEAD`; `git log --oneline -5`; targeted reads and `rg` scans of DFC generation/DTO seams; code_mapper; `git diff --check`; doc_consistency. Automated tests not run because this revalidation changed documentation only. | Passed. Read-only mapping again confirmed backend-owned option/preview/update plumbing is live, DFC-ready derivative generation remains coupled to `sendPlan.buildCurrent`, `conversationDraft.ensureDfcOptions` is not implemented, and no browser Playwright harness exists. `git diff --check` passed with LF/CRLF warnings only. Doc consistency found no overclaims or SSOT conflicts. | There is still no safe implementation slice that avoids choosing option-generation lifecycle semantics or adding new harness scaffolding. `getDfcOptions` should remain read-only until owner approval changes that contract. | Yes. Owner approval remains required for explicit `conversationDraft.ensureDfcOptions` generation semantics or new browser Playwright harness scaffolding. | DFC-18 after approval: implement the explicit backend-owned `conversationDraft.ensureDfcOptions` contract for approved Phase 1 text-like conversions, or separately approve and implement browser Playwright harness work. |
| DFC-17R1 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Close the blocked audit after the same DFC-17 owner gate repeated across three consecutive goal turns. | `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. No production code, DB schema, Send Plan behavior, UI behavior, conversion runtime, dependencies, external engines, Playwright harness, tests, or legacy bridge changed. | Yes. Appended this ledger row and added blocked audit closure notes. | `git status --short --branch`; `git rev-parse --short HEAD`; `git log --oneline -5`; targeted `rg` scans for DFC generation/DTO seams and Playwright harness files; `git diff --check`; doc_consistency. Automated tests not run because this blocked-audit closure changed documentation only. | Passed. Current-state scans again found no implemented `conversationDraft.ensureDfcOptions`, DFC-ready derivative generation still tied to `sendPlan.buildCurrent`, and no `playwright.config.*` / `@playwright/test` harness. `git diff --check` passed with LF/CRLF warnings only. Added-line privacy pattern scan passed. Doc consistency passed with no overclaims or SSOT conflicts. | The active goal cannot make further meaningful progress without owner input or an external-state change because both next viable paths are owner-gated. | Yes. Owner approval is required for explicit `conversationDraft.ensureDfcOptions` generation semantics or new browser Playwright harness scaffolding. | Resume with DFC-18 after approval for one blocked path: implement explicit backend-owned `conversationDraft.ensureDfcOptions` for approved Phase 1 text-like conversions, or implement the separately approved browser Playwright harness. |
| DFC-18 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Implement owner-approved explicit backend-owned DFC draft option generation through `conversationDraft.ensureDfcOptions`. | `infra/db/dbMethodsRegistry.ts`; `infra/db/types.ts`; `infra/db/validation.ts`; `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `src/next/ipc/contracts/dbBridgeContracts.ts`; `src/next/ipc/contracts/dbBridgeContracts.test.ts`; `src/next/files/conversationDraftClient.ts`; `src/next/files/conversationDraftClient.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added the explicit ensure IPC/worker/client contract, reused the sanitized options DTO as the response, and generated DFC-ready derivatives only for stored local Phase 1 text-like assets. No DB schema, durable ConversionOption rows, new dependency, external engine, browser Playwright harness, UI redesign, legacy bridge, broad Send Plan rewrite, or forbidden runtime family changed. | Yes. Appended this row and updated recovery context for DFC-18. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts`; `npx vitest --run infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts infra/db/worker.handlerRegistration.test.ts infra/db/dbMethodsRegistry.test.ts`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; production and test privacy scans; risk_reviewer; test_runner. Browser Playwright was not run because harness scaffolding remains forbidden in this slice. | Targeted worker/IPC/client Vitest passed: 3 files / 220 tests. Broader DFC service/Send Plan/shared/registry Vitest passed: 5 files / 95 tests. `git diff --check` passed with LF/CRLF warnings only. Production added-line privacy scan found no new sensitive DTO/log exposure; test-only sensitive fixtures assert stripping. Risk review passed with no P0/P1 findings. Test runner attributed the full typecheck failure to the pre-existing Vue named-export issue in `src/ui-app/app/appChatApp.logic.ts:183-184`. | The endpoint is explicit generation plumbing, not a visible UI call site. The existing `sendPlan.buildCurrent` lazy bridge remains as backend fallback behavior but is no longer the only backend path that can produce DFC-ready options. Pending/concurrent generation state still has no durable `ConversionOption` rows; this slice is synchronous and uses the existing derivative job path. | No for this owner-approved DFC-18 slice. Owner approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, UI redesign, legacy compatibility bridge, broad Send Plan rewrite, or forbidden conversion runtime families. | DFC-19: wire the existing attachment detail flow to call `conversationDraft.ensureDfcOptions` before refreshing options, if it remains a small local UI/client plumbing change. |
| DFC-19 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Wire the existing attachment details option-refresh flow to request backend-owned DFC option generation through `conversationDraft.ensureDfcOptions`. | `src/ui-app/app/appChatApp.logic.ts`; `src/ui-app/AppChatApp.attachments.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. The existing draft attachment details flow now calls the backend-owned ensure endpoint before storing/displaying DFC option candidates. The renderer sends only `conversationId` and `assetId` and still receives sanitized backend option DTOs; it does not invent target kind, `selectedOptionId`, `selectedAssetRefs`, `SendAssetRef`, `raw_file`, or `derived_asset` semantics. No DB schema, backend conversion runtime, new dependency, external engine, browser Playwright harness, UI redesign, legacy bridge, broad Send Plan rewrite, or forbidden runtime family changed. | Yes. Appended this row and updated recovery context for DFC-19. | `npx vitest --run src/ui-app/AppChatApp.attachments.test.ts src/next/files/conversationDraftClient.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts`; `npx vue-tsc --noEmit --pretty false`; `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts infra/db/worker.handlerRegistration.test.ts infra/db/dbMethodsRegistry.test.ts src/next/files/conversationDraftClient.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `git diff --check`; diff-only privacy and renderer authority scans; risk_reviewer; doc_consistency. Browser Playwright was not run because new harness scaffolding remains forbidden in this slice. | Focused UI/client/IPC Vitest passed. Combined targeted DFC backend/client/UI suite passed: 9 files / 387 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Added-line privacy and renderer-authority scans found no path, `fileUrl`, full hash, `contentToken`, file body, `storageRef`, or renderer-invented DFC identity leaks. Risk review and doc consistency passed. | This slice changes the details-flow request path only; it does not add durable `ConversionOption` rows, asynchronous pending state, browser Playwright smoke, a full Attachment Detail Inspector, runtime expansion for PDF/Office/XLSX/HTML/PS-EPS, or broad Send Plan replacement. The backend ensure endpoint remains the owner of generated option identity and availability. | No for this narrow existing-dialog plumbing slice. Owner approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, durable option-generation storage, or forbidden conversion runtime families. | DFC-20: close remaining non-Playwright Phase 1 gaps, likely replacing the backend `sendPlan.buildCurrent` lazy DFC-generation fallback with explicit ensure-driven readiness where safe, or seek separate owner approval for browser Playwright harness scaffolding. |
| DFC-20 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Quarantine Send Plan lazy text derivative generation so it no longer makes DFC options available; explicit `conversationDraft.ensureDfcOptions` is now the DFC-ready option-generation path. | `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Worker-side Send Plan can still create legacy text derivatives and lineage for existing non-DFC sendability, but calls that originate from `sendPlan.buildCurrent` / OpenRouter preparation pass `exposeDfcOption: false` and do not stamp or replace the DFC `textConversion` facade metadata that backend option DTOs require for availability. The explicit `conversationDraft.ensureDfcOptions` path passes `exposeDfcOption: true` and remains the only path that makes Phase 1 text-like derivatives available as backend-owned DFC options. No DB schema, IPC contract, renderer option identity, new dependency, external engine, browser Playwright harness, UI redesign, legacy bridge, broad Send Plan rewrite, or forbidden runtime family changed. | Yes. Appended this row and updated recovery context for DFC-20. | `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; diff-only privacy scan; code_mapper; test_runner; risk_reviewer. Browser Playwright was not run because new harness scaffolding remains outside this slice. | Targeted backend/client/UI Vitest passed after adding the explicit-ensure-preservation regression: 7 files / 307 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Diff privacy scan found no added logs, paths, `fileUrl`, `contentToken`, file body, or full-hash exposure; internal metadata field names remain backend-only and covered by sanitizer tests. Risk review passed with no P0/P1 findings. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, and test_runner attributed it as unrelated to the current `infra/db` diff. | This slice deliberately preserves legacy Send Plan derivative self-healing for non-DFC route sendability; it only prevents that path from creating DFC-ready option metadata. Existing assets that already have coherent DFC-ready `textConversion` metadata for the same derivative are preserved rather than stripped, and the new worker regression covers that preserve-after-ensure behavior. Browser/Electron smoke, durable `ConversionOption` rows, async pending state, and forbidden conversion families remain deferred. | No for this narrow Send Plan quarantine over the already approved explicit ensure path. Owner approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, durable option-generation storage, or forbidden conversion runtime families. | DFC-21: continue closing Phase 1 gaps by tightening preview/send behavior around explicit ensure state and stale selected refs, or seek separate owner approval for browser Playwright harness scaffolding. |
| DFC-21 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Tighten DFC preview/send/source coherence so derived options and selected derived refs become stale when their DFC facade `sourceHash` no longer matches the current raw asset hash. | `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `infra/files/sendPlanService.ts`; `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Backend option DTO generation now marks ready derived candidates with mismatched source hashes as unavailable `stale` options with a sanitized diagnostic, and preview/commit paths reject a stale selected option instead of sending or previewing it. Send Plan selected-ref synthesis and lineage checks now treat mismatched derived source hashes as stale, blocking DFC-managed sends without falling back to legacy selected send modes, extension, MIME, or route-derived behavior. No DB schema, IPC contract, renderer option identity, conversion runtime family expansion, dependency, external engine, browser Playwright harness, UI redesign, legacy bridge, or broad Send Plan rewrite changed. | Yes. Appended this row and updated recovery context for DFC-21. | `npm run rebuild:node`; `npx vitest --run infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `git diff --check`; diff-only privacy scans; test_runner; risk_reviewer. Browser Playwright was not run because harness scaffolding remains outside this slice. | Targeted DFC service/shared Vitest passed after preserving the existing malformed-derivative lineage diagnostic path: 3 files / 88 tests. Broader backend/client/UI DFC suite passed: 7 files / 307 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Privacy scans found no added runtime logs, absolute paths, `fileUrl`, `contentToken`, file body, or full-hash exposure; hash/storage metadata mentions in the diff are backend-internal checks and test fixtures. test_runner and risk_reviewer both passed with no P0/P1 findings. | Source-hash mismatch detection can only fire when the raw asset has a `sha256`; assets with null raw hashes still cannot be proven stale by hash comparison. Future `SendPlanService` consumers must keep providing `fileDerivativeRepo` for selected derived-asset source-hash gating. Browser/Electron smoke, durable `ConversionOption` rows, async pending state, and forbidden conversion families remain deferred. | No for this narrow backend coherence slice. Owner approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, durable option-generation storage, or forbidden conversion runtime families. | DFC-22: continue non-Playwright Phase 1 hardening around null-hash/pending-state semantics and durable generated-option state if approved, or seek separate owner approval for browser Playwright harness scaffolding. |
| DFC-22 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Harden DFC derived-option lineage when the raw file source hash is missing, so backend-owned derived options cannot be selected, previewed, or sent with unverifiable source lineage. | `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `infra/files/sendPlanService.ts`; `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Backend option DTO generation now treats a ready DFC derived facade as unavailable `blocked` when the current raw asset has no `sha256`, using only the symbolic diagnostic `raw_file_source_hash_missing`. Preview and commit reject such selected options through the existing blocked decision path. Send Plan selected-ref synthesis and lineage validation now also block source-unverifiable selected derived refs, while preserving `original_file` raw-file behavior and avoiding legacy route fallback. No DB schema, IPC contract, renderer option identity, conversion runtime family expansion, dependency, external engine, browser Playwright harness, UI redesign, legacy bridge, durable option storage, or broad Send Plan rewrite changed. | Yes. Appended this row and updated recovery context for DFC-22. | `npx vitest --run infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `git diff --check`; diff-only privacy scans; code_mapper; risk_reviewer. Browser Playwright was not run because harness scaffolding remains outside this slice. | Targeted DFC service/shared Vitest passed: 3 files / 89 tests. Broader backend/client/UI DFC suite passed: 7 files / 308 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Privacy scans found no added runtime logs, absolute paths, `fileUrl`, `contentToken`, file body, or full-hash exposure; hash/storage metadata mentions are backend-internal checks and test fixtures. code_mapper identified the null-hash gap, and risk_reviewer passed with no P0/P1 findings after implementation. | Explicit ensure already avoids exposing DFC metadata when the raw source hash is null; this slice closes hand-existing/legacy-metadata derived-facade availability. Legacy non-DFC text lineage may still be written with null source hashes for route-level sendability, but it is not DFC-ready option exposure. Browser/Electron smoke, durable `ConversionOption` rows, async pending state, and forbidden conversion families remain deferred. | No for this narrow backend coherence slice. Owner approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, durable option-generation storage, or forbidden conversion runtime families. | DFC-23: continue non-Playwright Phase 1 hardening around pending/concurrent generation semantics, or seek separate owner approval for browser Playwright harness scaffolding. |
| DFC-23 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Make DFC selected derived refs fail closed when Send Plan cannot verify the backing derivative repository, so asset metadata cannot stand in for a verified `DerivedAsset`. | `infra/files/sendPlanService.ts`; `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. DFC selected derived refs now require a `fileDerivativeRepo`-verified ready derivative before Send Plan marks them available. When the repo is absent, the DFC branch keeps the selected target semantics but blocks the attachment through the selected-option decision path and redacted lineage, without falling back to legacy send modes, extension, MIME, or route-derived behavior. Production worker runtime still injects `fileDerivativeRepo`. No DB schema, IPC contract, renderer option identity, conversion runtime family expansion, dependency, external engine, browser Playwright harness, UI redesign, legacy bridge, durable option storage, or broad Send Plan rewrite changed. | Yes. Appended this row and updated recovery context for DFC-23. | `npx vitest --run infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `git diff --check`; diff-only privacy scans; risk_reviewer; doc_consistency. Browser Playwright was not run because harness scaffolding remains outside this slice. | Targeted Send Plan/shared Vitest passed: 2 files / 66 tests. Broader backend/client/UI DFC suite passed. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Privacy scans found no new DTO/log exposure; internal hash/storage metadata mentions are backend-only checks and test fixtures. risk_reviewer passed with no P0/P1 findings. | The SendPlanService constructor still permits non-production/test callers to omit `fileDerivativeRepo`, but the DFC selected-derived path now fails closed in that case. Browser/Electron smoke, durable `ConversionOption` rows, async pending state, and forbidden conversion families remain deferred. | No for this narrow backend fail-closed slice. Owner approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, durable option-generation storage, or forbidden conversion runtime families. | DFC-24: continue non-Playwright Phase 1 hardening around pending/concurrent generation semantics, or seek separate owner approval for browser Playwright harness scaffolding. |
| DFC-24 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Require verified derivative rows for DFC draft derived options and preview sources, so asset-level text-conversion metadata cannot stand in for a `DerivedAsset`. | `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Backend draft option generation now builds derived DFC options only from `file_derivatives` rows, and preview source resolution now refuses selected derived refs without a matching derivative row. Orphaned asset text-conversion metadata no longer creates backend-owned options or previewable sources. Explicit ensure remains compatible because it writes the DFC facade onto the derivative row. No DB schema, IPC contract, renderer option identity, conversion runtime family expansion, dependency, external engine, browser Playwright harness, UI redesign, legacy bridge, durable option storage, async generation state, or broad Send Plan rewrite changed. | Yes. Appended this row and updated recovery context for DFC-24. | `npx vitest --run infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `git diff --check`; diff-only privacy scans; risk_reviewer; doc_consistency. Browser Playwright was not run because harness scaffolding remains outside this slice. | Targeted DFC service/shared Vitest passed: 2 files / 43 tests. Broader backend/client/UI DFC suite passed. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Production added-line privacy scan found no new DTO/log exposure; test-only metadata fixtures assert non-disclosure of storage, content, and source values. | Existing asset source metadata may still retain `textConversion` for lineage/cache purposes, but the draft option and preview DTO path no longer treats it as authority. Browser/Electron smoke, durable `ConversionOption` rows, async pending/concurrent generation state, and forbidden conversion families remain deferred. | No for this narrow backend DTO/preview fail-closed slice. Owner approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, durable option-generation storage, or forbidden conversion runtime families. | DFC-25: continue non-Playwright Phase 1 hardening around pending/concurrent `ensureDfcOptions` generation semantics, or seek separate owner approval for browser Playwright harness scaffolding. |
| DFC-25 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Coalesce concurrent explicit DFC option generation calls in the worker so identical `conversationDraft.ensureDfcOptions` requests do not create duplicate text derivative jobs. | `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added runtime-local in-flight coalescing around `ensureTextDerivativeAsset`, keyed by source asset, inferred target kind, and DFC-exposure mode. Concurrent identical explicit ensures now await the same backend generation promise and re-read the same backend-owned option DTO after completion. No DB schema, IPC contract, renderer option identity, Send Plan behavior, conversion runtime family expansion, dependency, external engine, browser Playwright harness, UI redesign, legacy bridge, durable option storage, or broad Send Plan rewrite changed. | Yes. Appended this row and updated recovery context for DFC-25. | `npx vitest --run infra/db/worker.filePipeline.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `git diff --check`; diff-only privacy scans; code_mapper; risk_reviewer; doc_consistency. Browser Playwright was not run because harness scaffolding remains outside this slice. | Targeted worker/shared Vitest passed: 2 files / 33 tests. Broader backend/client/UI DFC suite passed. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Production added-line privacy scan found no new DTO/log exposure; test-only metadata fixture mentions stay inside the regression setup. | This is a runtime-local coalescing guard, not a durable cross-process uniqueness guarantee. A durable pending/job uniqueness boundary would require a schema/repo design decision. The explicit ensure endpoint still runs synchronously and returns the final generated options rather than introducing a new UI-visible pending lifecycle. Browser/Electron smoke, durable `ConversionOption` rows, durable async pending state, and forbidden conversion families remain deferred. | No for this narrow worker in-flight guard. Owner approval remains required before DB uniqueness/migration, browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, durable option-generation storage, or forbidden conversion runtime families. | DFC-26: continue non-Playwright Phase 1 hardening around failed-generation/no-silent-fallback semantics, or seek separate owner approval for browser Playwright harness scaffolding or durable job uniqueness. |
| DFC-26 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Surface explicit DFC option-generation failures as backend-owned sanitized failed options and fail unreadable selected derivatives closed. | `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Explicit `conversationDraft.ensureDfcOptions` failures now stamp a DFC-exposed, symbolic failure marker for approved Phase 1 text-like targets, and backend draft option DTOs can surface that marker as a deterministic unavailable `failed` option without storage refs, hashes, paths, file body, or raw error text. Existing selected derivatives whose storage becomes missing or unreadable are marked failed and resolve to `selected_option_failed`, so preview/send remain blocked instead of silently falling back. Synthetic failed options are not emitted for `pdf_attachment` or legacy non-explicit failure metadata, and failed options with empty refs are not persistable as selected draft refs. No DB schema, IPC shape, renderer option identity, Send Plan rewrite, conversion runtime family expansion, dependency, external engine, browser Playwright harness, UI redesign, or legacy bridge changed. | Yes. Appended this row and updated recovery context for DFC-26. | `npx vitest --run infra/files/conversationAttachmentService.test.ts infra/db/worker.filePipeline.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; diff-only privacy scans; code_mapper; risk_reviewer; test_runner. Browser Playwright was not run because harness scaffolding remains outside this slice. | Focused worker/service/shared Vitest passed: 3 files / 61 tests. Broader backend/client/UI DFC suite passed: 7 files / 315 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Production added-line privacy/log scans found no DTO/log exposure; test-only sensitive fixtures assert non-disclosure. Risk review passed with no P0/P1 findings. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, and test_runner attributed it as unrelated to the current infra diff. | The synthetic failed-option marker is stored in existing asset source metadata as recovery/generation state, not as preview/send authority. A real `DerivedAsset` row remains required for ready preview/send options. Durable option-generation state, durable failed option rows, async pending UI, browser/Electron smoke, and forbidden conversion families remain deferred. | No for this narrow backend failure-state slice. Owner approval remains required before durable option-generation storage or DB uniqueness/migration, browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, or forbidden conversion runtime families. | DFC-27: continue non-Playwright Phase 1 hardening around pending-state visibility and retry semantics if it fits existing storage, or seek separate owner approval for durable option-generation storage or browser Playwright harness scaffolding. |
| DFC-27 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Harden existing pending DFC derivative-option semantics without adding durable job-state storage. | `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `infra/files/sendPlanService.test.ts`; `src/shared/files/documentFormatConversion.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Backend draft option DTO generation now surfaces existing `file_derivatives.status = 'pending'` DFC derivative rows as deterministic, unavailable pending options with the sanitized diagnostic `derived_asset_pending`. Persisted selection of such a backend-owned pending option remains coherent only when `selectedOptionId` and `selectedAssetRefs` match the generated option, but preview resolves to `selected_option_pending` and commit/send remain blocked without falling back to legacy send modes, extension, MIME, or route-derived behavior. No DB schema, IPC shape, renderer option identity, Send Plan rewrite, conversion runtime family expansion, dependency, external engine, browser Playwright harness, UI redesign, durable option-generation storage, durable job-state linkage, or legacy bridge changed. | Yes. Appended this row and updated recovery context for DFC-27. | `npx vitest --run src/shared/files/documentFormatConversion.test.ts infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; diff-only privacy scans; code_mapper; risk_reviewer; test_runner; doc_consistency. Browser Playwright was not run because harness scaffolding remains outside this slice. | Focused service/shared Vitest passed: 3 files / 95 tests. Broader backend/client/UI DFC suite passed: 7 files / 316 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Production added-line privacy/log scan found no DTO/log exposure; the only test-only storage token mention asserts non-disclosure. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, and test_runner attributed it as unrelated to the current DFC diff. | This slice only covers existing pending `file_derivatives` rows. Durable pending/retry visibility from `derivative_jobs`, durable option-generation storage, and cross-process job-state linkage still require an owner-approved storage/IPC design. Browser/Electron smoke and forbidden conversion families remain deferred. | No for this narrow pending-option DTO/decision slice. Owner approval remains required before durable option-generation storage or DB uniqueness/migration, browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, or forbidden conversion runtime families. | DFC-28: choose between an owner-approved durable pending/retry/job-state design and continued narrow backend hardening around failed/retry semantics that can stay within existing storage. |
| DFC-28 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add read-path visibility for persisted DFC selected-ref mismatches in backend-owned option DTOs. | `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `conversationDraft.getDfcOptions` now attaches the existing sanitized `dfc_selection_refs_mismatch` diagnostic to the selected backend option candidate when persisted `selectedAssetRefs` diverge from the backend-selected `selectedOptionId` option refs. Preview and commit/send behavior are unchanged: preview already blocks with the same symbolic diagnostic, and message binding still rejects mismatched refs. No DB schema, IPC shape, renderer option identity, Send Plan behavior, conversion runtime family expansion, dependency, external engine, browser Playwright harness, UI redesign, durable option-generation storage, durable job-state linkage, or legacy bridge changed. | Yes. Appended this row and updated recovery context for DFC-28. | `npx vitest --run infra/files/conversationAttachmentService.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; diff-only privacy scans; code_mapper; risk_reviewer; test_runner; doc_consistency. Browser Playwright was not run because harness scaffolding remains outside this slice. | Focused service Vitest passed: 1 file / 27 tests. Broader backend/client/UI DFC suite passed: 7 files / 316 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Production added-line privacy/log scan found no DTO/log exposure; the only test-only storage token mention asserts non-disclosure. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this DFC diff. | This is read-path diagnostic parity only. It does not change the underlying decision shape, add durable option storage, or implement durable pending/retry job-state visibility. Browser/Electron smoke and forbidden conversion families remain deferred. | No for this narrow DTO diagnostic parity slice. Owner approval remains required before durable option-generation storage or DB uniqueness/migration, browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, or forbidden conversion runtime families. | DFC-29: either seek owner approval for durable pending/retry/job-state design or browser Playwright harness scaffolding, or continue only with narrow backend hardening if a concrete gap remains outside owner-gated scope. |
| DFC-29 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Require verified derivative target metadata for DFC Send Plan target reconstruction when the derivative repository is available. | `infra/files/sendPlanService.ts`; `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. DFC Send Plan selected-ref reconstruction no longer infers a selected derived target kind from `file_assets.sourceMetaJson.textConversion` when `fileDerivativeRepo` is available and the selected derivative row is missing or lacks DFC `targetKind` metadata. Such draft selections and malformed history snapshots now fail closed as `selected_option_not_found` with unsupported semantics instead of treating asset-level conversion metadata as target authority. The no-repo fallback remains fail-closed for incomplete/test callers. No DB schema, IPC shape, renderer option identity, UI, conversion runtime family expansion, dependency, external engine, browser Playwright harness, durable option-generation storage, durable job-state linkage, legacy bridge, or broad Send Plan rewrite changed. | Yes. Appended this row and updated recovery context for DFC-29. | `npx vitest --run infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; diff-only privacy scans; risk_reviewer; test_runner; doc_consistency. Browser Playwright was not run because harness scaffolding remains outside this slice. | Focused Send Plan/shared Vitest passed: 2 files / 70 tests. Broader backend/client/UI DFC suite passed: 7 files / 364 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF warnings only. Production added-line privacy/log scan found no DTO/log exposure; test-only storage/hash metadata fixtures assert non-disclosure. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this DFC diff. | This slice tightens normal backend Send Plan behavior only. It does not add durable option storage, durable pending/retry job-state visibility, new browser smoke coverage, or forbidden conversion runtime families. | No for this narrow Send Plan fail-closed slice. Owner approval remains required before durable option-generation storage or DB uniqueness/migration, browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, or forbidden conversion runtime families. | DFC-30: seek owner approval for durable pending/retry/job-state storage or browser Playwright harness scaffolding, unless another concrete narrow backend gap remains outside owner-gated scope. |
| DFC-30 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add persisted DFC selected-option rehydrate and preview/send same-source regression coverage. | `infra/files/conversationAttachmentService.test.ts`; `src/ui-app/AppChatApp.attachments.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. Backend service coverage now proves a selected backend-owned derived DFC option survives draft restore, resolves options and preview from the same `derived_asset` ref, commits to a message without a renderer-supplied snapshot, and surfaces the same used option/ref/target/send strategy in candidate history snapshots. UI coverage now proves the attachment details dialog rehydrates the selected backend DFC option after close/reopen, keeps the selected marker and preview stable, and does not issue an extra `conversationDraft.updateAttachmentSettings` call on reopen. No production code, DB schema, IPC shape, renderer-inferred option identity, Send Plan behavior, conversion runtime family expansion, dependency, external engine, browser Playwright harness, UI redesign, durable option-generation storage, durable job-state linkage, legacy bridge, or broad Send Plan rewrite changed. | Yes. Appended this row and updated recovery context for DFC-30. | `npx vitest --run infra/files/conversationAttachmentService.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; diff-only privacy scans. Browser Playwright was not run because harness scaffolding remains outside this slice. | Focused backend/UI Vitest passed: 2 files / 73 tests. Broader backend/client/UI DFC suite passed: 7 files / 366 tests. `npx vue-tsc --noEmit --pretty false` passed. Production diff privacy scan found no hits; test-only `storageUri` fixture values are local regression inputs, not DTO/log output. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this test-only DFC diff. | This slice is regression coverage only. It does not add durable pending/retry job-state visibility, durable option storage, browser smoke coverage, or forbidden conversion runtime families. | No for this test-hardening slice. Owner approval remains required before durable option-generation storage or DB uniqueness/migration, browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, or forbidden conversion runtime families. | DFC-31: continue with another narrow non-gated coverage/contract gap such as selected-option mismatch-state UI coverage, or stop for owner approval before durable job-state storage or browser Playwright harness work. |
| DFC-31 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Surface backend-sanitized DFC selected-ref mismatch diagnostics in the attachment details UI and cover the no-auto-rewrite renderer behavior. | `src/ui-app/app/appChatApp.logic.ts`; `src/ui-app/components/DraftAttachmentDetailsDialog.vue`; `src/ui-app/AppChatApp.attachments.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes, but only a narrow renderer display path. The attachment details view model now carries backend-sanitized option diagnostic codes, and the details dialog displays them under the backend-owned option. The renderer still does not invent option ids, refs, target kinds, compatibility, or conversion identity, and it does not rewrite persisted selections. No DB schema, IPC shape, Send Plan behavior, conversion runtime family expansion, dependency, external engine, browser Playwright harness, durable option-generation storage, durable job-state linkage, legacy bridge, or broad UI redesign changed. | Yes. Appended this row and updated recovery context for DFC-31. | `npx vitest --run src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `git diff --check`; diff-only privacy/log scan over the touched UI files. Browser Playwright was not run because harness scaffolding remains outside this slice. | Focused UI Vitest passed: 1 file / 46 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF working-copy warnings only. Diff-only privacy/log scan found no DTO/log exposure. | This slice only surfaces already-sanitized backend option diagnostics and adds UI coverage for mismatch visibility. It does not add durable pending/retry job-state visibility, durable option storage, browser smoke coverage, or forbidden conversion runtime families. | No for this narrow renderer visibility and UI regression slice. Owner approval remains required before durable option-generation storage or DB uniqueness/migration, browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, legacy compatibility bridge, broad Send Plan rewrite, or forbidden conversion runtime families. | DFC-32: seek owner approval for durable pending/retry/job-state storage or browser Playwright harness scaffolding, unless another concrete narrow contract or coverage gap remains outside owner-gated scope. |
| DFC-32 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Create an owner-decision memo for the next gated DFC direction: durable option-generation/job-state storage or browser Playwright harness scaffolding. | `docs/file-pipeline/document-format-conversion/dfc-32-owner-memo-durable-state-and-browser-harness.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. This is docs-only. No production code, DB schema, Send Plan behavior, UI behavior, IPC shape, conversion runtime, dependencies, external engines, browser Playwright harness, durable storage, or legacy bridge changed in DFC-32. | Yes. Added the DFC-32 owner memo and updated recovery context. | `git status --short --branch`; `git rev-parse --short HEAD`; targeted `rg` over SSOT/job-state terms and Playwright harness paths; `git diff --check`; Markdown trailing-whitespace scan over the DFC-32 memo; concrete sensitive-value/log scan over DFC-32 docs. Full tests were not run because DFC-32 changed Markdown docs only. | Passed. `git diff --check` passed with LF/CRLF working-copy warnings only. No Playwright harness path was found. No trailing whitespace or concrete sensitive-value/log scan hits were found in the DFC-32 docs. | DFC-32 does not itself unblock durable state or browser smoke; it records the owner gate and recommends durable DFC generation state first. The worktree also still contains the completed uncommitted DFC-31 UI slice. | Yes before implementing either durable DFC option-generation/job-state storage or new browser Playwright harness scaffolding. | DFC-33: proceed only after owner approval for durable DFC option-generation/job-state storage or new browser Playwright harness scaffolding. |
| DFC-33 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Implement owner-approved durable DFC option-generation/job-state storage for explicit Phase 1 ensure lifecycle. | `infra/db/schema.sql`; `infra/db/migrations/ensureFilePipelineSchema.ts`; `infra/db/migrations/ensureFilePipelineSchema.test.ts`; `infra/db/types.ts`; `infra/db/repo/dfcOptionGenerationStateRepo.ts`; `infra/db/repo/filePipelineRepo.test.ts`; `infra/db/worker/runtime.ts`; `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `infra/files/conversationAttachmentService.ts`; `infra/files/conversationAttachmentService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added `dfc_option_generation_states` as a durable DFC generation-state companion table, repository/types/runtime wiring, explicit ensure lifecycle writes for running/ready/failed outcomes, retry-on-later-ensure behavior for failed durable states, and sanitized option DTO read-path visibility for durable pending/running/failed states. Ready preview/send authority still requires verified `file_derivatives` and DerivedAsset facade metadata. No browser Playwright harness, external engine, new dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-33. | `npm run rebuild:node`; `npx vitest --run infra/db/migrations/ensureFilePipelineSchema.test.ts infra/db/repo/filePipelineRepo.test.ts infra/db/worker.filePipeline.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; diff privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused migration/repo/worker/service Vitest passed: 4 files / 67 tests. Broader backend/client/UI DFC suite passed: 7 files / 369 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF working-copy warnings only. Concrete path/token/log privacy scan found no production DTO/log exposures; broader hash/storage-pattern hits were internal DB fields or test fixtures. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this durable-state slice. | This is the first durable DFC generation-state slice. It does not yet implement explicit cancellation, attachment-removal suppression for completed background jobs beyond existing behavior, a user-visible retry control, browser smoke, or new runtime families. | No additional approval was required because the Owner explicitly approved DFC-32 Option A before DFC-33. Separate approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, or forbidden conversion runtime families. | DFC-34: continue durable lifecycle hardening around stale/cancellation/removal semantics, or add a narrow retry-control endpoint if it preserves backend-owned identity and privacy boundaries. |
| DFC-34 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Harden explicit DFC ensure against stale derived outputs whose source hash no longer matches the raw asset. | `infra/db/repo/fileDerivativeRepo.ts`; `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `infra/files/conversationAttachmentService.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Explicit DFC ensure now rejects reuse of an existing ready text derivative when its DFC `sourceHash` no longer matches the current raw asset hash, regenerates a fresh derivative/job, and updates durable generation-state output. Backend option DTO generation reads derivative candidates latest-first and exposes one candidate per derived target kind, so regenerated current output supersedes older stale output for the same target. No DB schema change beyond DFC-33, IPC shape change, renderer option identity, browser Playwright harness, external engine, new dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-34. | `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/conversationAttachmentService.test.ts infra/db/repo/filePipelineRepo.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; production diff privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused worker/service Vitest passed: 2 files / 48 tests. Focused worker/service/repo Vitest passed: 3 files / 62 tests. Broader backend/client/UI DFC suite passed: 7 files / 370 tests. `npx vue-tsc --noEmit --pretty false` passed. `git diff --check` passed with LF/CRLF working-copy warnings only. Production diff privacy/log scan found no DTO/log exposure; the only broader privacy-pattern hit was DFC documentation describing forbidden leakage. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this durable lifecycle slice. | This slice covers stale source-hash regeneration only. It does not implement explicit cancellation, attachment-removal suppression for completed background jobs, user-visible retry controls, browser smoke, or new runtime families. | No additional approval required; this stays inside the owner-approved durable state lifecycle direction. Separate approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, or forbidden conversion runtime families. | DFC-35: continue durable lifecycle hardening around cancellation/removal semantics, especially completed generation after draft attachment removal. |
| DFC-35 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Block durable DFC option-generation writeback when a draft attachment is removed during explicit ensure. | `infra/db/types.ts`; `infra/db/repo/dfcOptionGenerationStateRepo.ts`; `infra/db/repo/filePipelineRepo.test.ts`; `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `infra/files/conversationAttachmentService.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added a repo-level blocked-state writer for durable DFC generation state and a draft-attachment relevance guard around explicit DFC derivative generation. If the attachment is detached before generation starts or after conversion completes but before DFC metadata/asset ready state is written, the generation state becomes blocked with sanitized `draft_attachment_detached`, no output derivative id, and no renderer-visible fallback. No DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, new dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-35. | `npm run rebuild:node`; `npx vitest --run infra/db/repo/filePipelineRepo.test.ts infra/db/worker.filePipeline.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; production diff privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 63 tests. Broader backend/client/UI DFC suite passed: 7 files / 371 tests. `vue-tsc` passed. `git diff --check` and privacy/log scans passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this durable lifecycle slice. | This slice covers draft attachment removal around explicit ensure writeback. It does not add user-visible retry controls, browser smoke, external runtimes, or new conversion families. A completed detached derivative may still remain as asset-level cache, but the removed draft UI does not receive a ready durable generation-state writeback. | No additional approval required; this stays inside the owner-approved durable state lifecycle direction. Separate approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, or forbidden conversion runtime families. | DFC-36: continue with narrow durable retry/stale/blocked recovery behavior if needed, or stop before any browser Playwright harness or external runtime work. |
| DFC-36 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Clear stale durable DFC output pointers on non-ready generation-state transitions. | `infra/db/repo/dfcOptionGenerationStateRepo.ts`; `infra/db/repo/filePipelineRepo.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Durable DFC generation state now clears `output_derivative_id` whenever a state moves to running, failed, or blocked, preserving the invariant that only ready state carries the authoritative output derivative pointer. Repo coverage proves ready -> running, ready -> failed, and ready -> blocked transitions do not retain stale output ids. No DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, new dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-36. | `npm run rebuild:node`; `npx vitest --run infra/db/repo/filePipelineRepo.test.ts infra/db/worker.filePipeline.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; production diff privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 63 tests. Broader backend/client/UI DFC suite passed: 7 files / 371 tests. `vue-tsc` passed. `git diff --check` and production privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this durable lifecycle slice. | This slice only tightens durable generation-state transition semantics. It does not add user-visible retry controls, browser smoke, external runtimes, or new conversion families. | No additional approval required; this stays inside the owner-approved durable state lifecycle direction. Separate approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, or forbidden conversion runtime families. | DFC-37: continue with narrow durable retry/stale/blocked recovery behavior if needed, or stop before any browser Playwright harness or external runtime work. |
| DFC-37 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Honor non-retryable durable DFC generation failures during explicit ensure. | `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Explicit DFC ensure now skips repeated derivative-job creation when the backend-owned durable generation state for the same option identity is `failed` and `retryable=false`, unless a verified reusable derived asset already exists. The read path continues to expose the existing sanitized failed option with empty refs and no legacy fallback. No DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, new dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-37. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; production diff privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 64 tests. Broader backend/client/UI DFC suite passed: 7 files / 372 tests. `vue-tsc` passed. `git diff --check` and production privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this durable lifecycle slice. | This slice only suppresses repeated explicit generation for terminal non-retryable durable failures. It does not add user-visible retry controls, browser smoke, external runtimes, or new conversion families. | No additional approval required; this stays inside the owner-approved durable state lifecycle direction. Separate approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, or forbidden conversion runtime families. | DFC-38: continue with narrow durable lifecycle hardening if a concrete gap remains, or stop for owner decision before browser harness, external runtime, or broad redesign work. |
| DFC-38 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add regression coverage for recovery from detached durable DFC blocked state after re-attachment. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The worker regression for draft-attachment removal now continues through re-adding the same asset and running explicit ensure again, proving the durable state recovers from `blocked`/`draft_attachment_detached` to `ready`, reuses the existing verified derivative, writes the output derivative id, and does not create a duplicate derivative job. No production code, DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, new dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-38. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 64 tests. Broader backend/client/UI DFC suite passed: 7 files / 372 tests. `vue-tsc` passed. `git diff --check` and privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this durable lifecycle regression. | This slice is regression coverage only. It does not add user-visible retry controls, browser smoke, external runtimes, or new conversion families. | No additional approval required; this stays inside the owner-approved durable state lifecycle direction. Separate approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, or forbidden conversion runtime families. | DFC-39: continue with a concrete narrow durable lifecycle gap if one remains, or pause for owner decision before browser harness, external runtime, or broad redesign work. |
| DFC-39 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add regression coverage that verified derivatives recover non-retryable durable DFC failures. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The worker regression now seeds a non-retryable durable failed state and a verified ready derivative for the same DFC option identity, then proves explicit ensure recovers the durable state to `ready`, uses the existing `derived_asset` ref, writes the output derivative id, and creates no derivative job. No production code, DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, new dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-39. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 65 tests. Broader backend/client/UI DFC suite passed: 7 files / 373 tests. `vue-tsc` passed. `git diff --check` and privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this durable lifecycle regression. | This slice is regression coverage only. It does not add user-visible retry controls, browser smoke, external runtimes, or new conversion families. | No additional approval required; this stays inside the owner-approved durable state lifecycle direction. Separate approval remains required before browser Playwright harness scaffolding, external engines, new dependencies, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, or forbidden conversion runtime families. | DFC-40: continue with a concrete narrow durable lifecycle or contract coverage gap if one remains, or pause for owner decision before browser harness, external runtime, or broad redesign work. |
| DFC-40 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add TSV table_markdown preview/send same-source regression coverage. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The worker regression now verifies a TSV asset explicitly ensures to a `table_markdown` `derived_asset`, writes the expected Markdown table output, persists the selected backend option/ref, previews from the same selected derived ref, and commits a message attachment with matching `usedOptionId`, `usedAssetRefs`, `targetKind`, and `sendStrategy`. No production code, DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, new dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-40. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 66 tests. Broader backend/client/UI DFC suite passed: 7 files / 374 tests. `vue-tsc` passed. `git diff --check` and privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this TSV same-source regression. | This slice is regression coverage for existing TSV/table_markdown runtime behavior only. It does not add browser smoke, external runtimes, new conversion families, or user-visible retry controls. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, or forbidden conversion runtime families. | DFC-41: continue with another concrete narrow contract/runtime coverage gap, or prepare an owner-decision memo for the next larger step. |
| DFC-41 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add quoted CSV table_markdown conversion regression coverage. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The worker regression now verifies quoted CSV table conversion keeps escaped commas in a single cell, escapes Markdown pipe characters, renders quoted multiline cell content as `<br>`, and exposes the backend-owned `table_markdown` `derived_asset` option/ref without leaking raw or derived storage/source metadata. No production code, DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, new dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-41. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 67 tests. Broader backend/client/UI DFC suite passed: 7 files / 375 tests. `vue-tsc` passed. `git diff --check` and privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this CSV conversion regression. | This slice is regression coverage for existing CSV/table_markdown runtime behavior only. It does not add browser smoke, external runtimes, new conversion families, or user-visible retry controls. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, or forbidden conversion runtime families. | DFC-42: continue with another concrete narrow runtime/contract coverage gap, or prepare an owner-decision memo for the next larger step. |
| DFC-42 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add BOM-aware UTF-16 text decoding for explicit DFC text/table conversion. | `infra/files/derivativeJobService.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Text derivative generation now detects explicit UTF-16LE/UTF-16BE BOMs and decodes those assets before falling back to strict UTF-8, preserving binary detection and avoiding heuristic encoding guesses. Worker coverage proves a UTF-16LE BOM TSV asset ensures to a `table_markdown` `derived_asset`, writes expected Markdown table output, and records `sourceEncoding: utf-16le`. No DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-42. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 68 tests. Broader backend/client/UI DFC suite passed: 7 files / 376 tests. `vue-tsc` passed. `git diff --check` and privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this encoding runtime slice. | This slice adds deterministic BOM-based UTF-16 support only. It does not implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, or forbidden conversion runtime families. | DFC-43: continue with another concrete narrow runtime/contract coverage gap, or prepare an owner-decision memo for the next larger step. |
| DFC-43 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add UTF-16BE BOM table_markdown conversion regression coverage. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The worker regression now verifies a UTF-16BE BOM TSV asset explicitly ensures to a `table_markdown` `derived_asset`, writes expected Markdown table output, records `sourceEncoding: utf-16be`, and exposes the backend-owned option/ref without leaking raw or derived storage/source metadata. No production code, DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-43. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 69 tests. Broader backend/client/UI DFC suite passed: 7 files / 377 tests. `vue-tsc` passed. `git diff --check` and privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this encoding regression. | This slice is regression coverage for existing deterministic BOM-based UTF-16BE support only. It does not implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, or forbidden conversion runtime families. | DFC-44: continue with another concrete narrow runtime/contract coverage gap, or prepare an owner-decision memo for the next larger step. |
| DFC-44 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add no-BOM UTF-16 table_markdown fail-closed regression coverage. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The worker regression now verifies a UTF-16LE TSV asset without an explicit BOM does not get guessed into a conversion path: explicit DFC ensure exposes a sanitized failed `table_markdown` option, writes no `DerivedAsset`, records the derivative job and durable generation state as failed with `derivative_input_missing`, and does not leak storage URI, source hash, or raw decoder detail in the DTO. No production code, DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-44. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed: 3 files / 70 tests. Broader backend/client/UI DFC suite passed: 7 files / 378 tests. `vue-tsc` passed. `git diff --check` and privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this encoding boundary regression. | This slice is regression coverage for the deterministic BOM-only encoding boundary. It does not implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, or forbidden conversion runtime families. | DFC-45: continue with another concrete narrow runtime/contract coverage gap, or prepare an owner-decision memo for the next larger step such as browser harness or external conversion runtime. |
| DFC-45 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Sanitize unknown decoder/internal error codes during DFC text derivative generation. | `infra/files/derivativeJobService.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `DerivativeJobService` now trusts only known `DerivativeErrorCode` values when normalizing caught errors; Node/TextDecoder internal `error.code` values no longer enter derivative job state, DFC generation state, failed option diagnostics, or DTOs. Worker coverage proves an invalid UTF-8 CSV asset without a BOM fails closed as sanitized `derivative_input_missing`, writes no `DerivedAsset`, records the durable state as failed, and does not leak storage URI, source hash, or raw decoder wording. No DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-45. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed after rebuild: 3 files / 71 tests. Broader backend/client/UI DFC suite passed after rebuild: 7 files / 379 tests. `vue-tsc` passed. `git diff --check` and privacy/log scan passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this decoder error-code sanitization slice. | This slice tightens error normalization for the built-in text derivative runtime. It does not implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, or forbidden conversion runtime families. | DFC-46: continue with another concrete narrow runtime/contract coverage gap, or prepare an owner-decision memo for the next larger step such as browser harness or external conversion runtime. |
| DFC-46 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Redact unknown decoder/runtime error messages from DFC text derivative failure state. | `infra/files/derivativeJobService.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Unknown/internal errors normalized to `derivative_input_missing` now use the fixed safe message `Derivative input could not be decoded.` instead of retaining raw runtime wording. Known `DerivativeErrorCode` failures still keep their existing sanitized message path. Worker coverage now checks invalid UTF-8 explicit DFC `table_markdown` ensure stores the safe message in derivative job state and asset `textConversion` metadata, writes no `DerivedAsset`, keeps durable generation state failed, and does not retain raw decoder wording. No DB schema, IPC shape, renderer option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-46. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused repo/worker/service Vitest passed after rebuild: 3 files / 71 tests. Broader backend/client/UI DFC suite passed: 7 files / 379 tests. `vue-tsc` passed. `git diff --check` and privacy/log scans passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this decoder message redaction slice. | This slice tightens internal failure-state redaction for unknown text decoder/runtime errors. It does not implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, or forbidden conversion runtime families. | DFC-47: continue with another concrete narrow runtime/contract coverage gap, or prepare an owner-decision memo for browser harness or external conversion runtime. |
| DFC-47 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Redact POSIX absolute paths from derivative runtime failure messages. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `sanitizeDerivativeErrorMessage` now redacts POSIX absolute paths in addition to storage-root, data URL, long base64, Windows drive, and UNC path redaction. Focused service coverage proves a provider failure containing `/var/tmp/.../private-token/...` is stored as `[redacted-path]` in derivative job failure state. No DB schema, IPC shape, renderer option identity, DFC option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-47. | `npm run rebuild:node`; attempted `npx vitest --run infra/files/derivativeJobService.test.ts infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "redacts POSIX absolute paths" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | New POSIX redaction focused test passed: 1 selected test. Focused repo/worker/service DFC Vitest passed: 3 files / 71 tests. Broader backend/client/UI DFC suite passed: 7 files / 379 tests. `vue-tsc` passed. `git diff --check` and privacy/log scans passed after docs. Full `infra/files/derivativeJobService.test.ts` currently still has a pre-existing HTML expectation mismatch: HTML with `<script>` is inferred as `code` by current runtime while the test expects safe markdown conversion. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this path-redaction slice. | This slice tightens runtime failure-message redaction only. It does not decide HTML conversion policy, implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, HTML policy changes, or forbidden conversion runtime families. | DFC-48: either address the HTML runtime/test policy mismatch with an owner-aware memo if needed, continue with another narrow privacy/contract gap, or prepare an owner-decision memo for browser harness/external runtime. |
| DFC-48 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Redact http URLs from derivative runtime failure messages. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `sanitizeDerivativeErrorMessage` now redacts `http://` and `https://` URLs before messages are persisted to derivative job failure state, reducing risk of signed URL, token, or remote storage details leaking through runtime diagnostics. Focused service coverage proves a provider failure containing a URL with query token and fragment is stored as `[redacted-url]`. No DB schema, IPC shape, renderer option identity, DFC option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, HTML policy change, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-48. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "redacts (POSIX absolute paths|http URLs)" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused runtime redaction tests passed: 2 selected tests. Focused repo/worker/service DFC Vitest passed: 3 files / 71 tests. Broader backend/client/UI DFC suite passed: 7 files / 379 tests. `vue-tsc` passed. `git diff --check` and privacy/log scans passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this URL-redaction slice. Full `infra/files/derivativeJobService.test.ts` still has the separately documented HTML policy/test mismatch pending Owner decision. | This slice tightens runtime failure-message redaction only. It does not decide HTML conversion policy, implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, HTML policy changes, or forbidden conversion runtime families. | DFC-49: continue with another narrow privacy/contract gap, or resume the HTML policy decision path if Owner approves Option A/B/C, or prepare an owner-decision memo for browser harness/external runtime. |
| DFC-49 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Redact file URLs from derivative runtime failure messages. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `sanitizeDerivativeErrorMessage` now redacts `file://` URLs before messages are persisted to derivative job failure state, reducing risk of local file URL, username, and app-support path leakage. Focused service coverage proves a provider failure containing a local `file:///Users/.../Application%20Support/...` URL is stored as `[redacted-url]`. No DB schema, IPC shape, renderer option identity, DFC option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, HTML policy change, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-49. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "redacts (POSIX absolute paths|http URLs|file URLs)" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused runtime redaction tests passed: 3 selected tests. Focused repo/worker/service DFC Vitest passed: 3 files / 71 tests. Broader backend/client/UI DFC suite passed: 7 files / 379 tests. `vue-tsc` passed. `git diff --check` and privacy/log scans passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this file-URL-redaction slice. Full `infra/files/derivativeJobService.test.ts` still has the separately documented HTML policy/test mismatch pending Owner decision. | This slice tightens runtime failure-message redaction only. It does not decide HTML conversion policy, implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, HTML policy changes, or forbidden conversion runtime families. | DFC-50: continue with another narrow privacy/contract gap, resume the HTML policy decision path if Owner approves Option A/B/C, or prepare an owner-decision memo for browser harness/external runtime. |
| DFC-50 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Redact common secret assignments from derivative runtime failure messages. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `sanitizeDerivativeErrorMessage` now redacts common secret-bearing fragments such as `Authorization: Bearer ...`, `api_key=...`, `token:...`, access/refresh/content token keys, and generic `secret` assignments before messages are persisted to derivative job failure state. Focused service coverage proves provider failures containing bearer, API key, and token values are stored with `[redacted-secret]`. No DB schema, IPC shape, renderer option identity, DFC option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, HTML policy change, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-50. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "redacts (POSIX absolute paths|http URLs|file URLs|common secret assignments)" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused runtime redaction tests passed: 4 selected tests. Focused repo/worker/service DFC Vitest passed: 3 files / 71 tests. Broader backend/client/UI DFC suite passed: 7 files / 379 tests. `vue-tsc` passed. `git diff --check` and privacy/log scans passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this secret-redaction slice. Full `infra/files/derivativeJobService.test.ts` still has the separately documented HTML policy/test mismatch pending Owner decision. | This slice tightens runtime failure-message redaction only. It does not decide HTML conversion policy, implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, HTML policy changes, or forbidden conversion runtime families. | DFC-51: continue with another narrow privacy/contract gap, resume the HTML policy decision path if Owner approves Option A/B/C, or prepare an owner-decision memo for browser harness/external runtime. |
| DFC-51 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Redact email identifiers from derivative runtime failure messages. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `sanitizeDerivativeErrorMessage` now redacts email-like identifiers before messages are persisted to derivative job failure state, reducing risk of account/user identifiers leaking through provider/runtime diagnostics. Focused service coverage proves a provider failure containing `alice.sensitive+dfc@example.test` is stored as `[redacted-email]`. No DB schema, IPC shape, renderer option identity, DFC option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, HTML policy change, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-51. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "redacts (POSIX absolute paths|http URLs|file URLs|common secret assignments|email identifiers)" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused runtime redaction tests passed: 5 selected tests. Focused repo/worker/service DFC Vitest passed: 3 files / 71 tests. Broader backend/client/UI DFC suite passed: 7 files / 379 tests. `vue-tsc` passed. `git diff --check` and privacy/log scans passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this email-redaction slice. Full `infra/files/derivativeJobService.test.ts` still has the separately documented HTML policy/test mismatch pending Owner decision. | This slice tightens runtime failure-message redaction only. It does not decide HTML conversion policy, implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, HTML policy changes, or forbidden conversion runtime families. | DFC-52: continue with another narrow privacy/contract gap, resume the HTML policy decision path if Owner approves Option A/B/C, or prepare an owner-decision memo for browser harness/external runtime. |
| DFC-52 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Redact dotted token values from derivative runtime failure messages. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `sanitizeDerivativeErrorMessage` now redacts JWT-like dotted token values before messages are persisted to derivative job failure state, covering shorter three-segment tokens that can bypass long-base64 and key-assignment redaction. Focused service coverage proves a provider failure containing a dotted token is stored as `[redacted-token]`. No DB schema, IPC shape, renderer option identity, DFC option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, HTML policy change, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-52. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "redacts (POSIX absolute paths|http URLs|file URLs|common secret assignments|email identifiers|dotted token values)" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused runtime redaction tests passed: 6 selected tests. Focused repo/worker/service DFC Vitest passed: 3 files / 71 tests. Broader backend/client/UI DFC suite passed: 7 files / 379 tests. `vue-tsc` passed. `git diff --check` and privacy/log scans passed after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this dotted-token-redaction slice. Full `infra/files/derivativeJobService.test.ts` still has the separately documented HTML policy/test mismatch pending Owner decision. | This slice tightens runtime failure-message redaction only. It does not decide HTML conversion policy, implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, HTML policy changes, or forbidden conversion runtime families. | DFC-53: continue with another narrow privacy/contract gap, resume the HTML policy decision path if Owner approves Option A/B/C, or prepare an owner-decision memo for browser harness/external runtime. |
| DFC-53 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Redact environment-variable secret assignments from derivative runtime failure messages. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `sanitizeDerivativeErrorMessage` now redacts common uppercase environment-style secret assignments such as `OPENAI_API_KEY=...`, `AWS_SECRET_ACCESS_KEY=...`, and `STARVERSE_CONTENT_TOKEN=...` before messages are persisted to derivative job failure state. Focused service coverage proves those provider failure fragments are stored with `[redacted-secret]`. No DB schema, IPC shape, renderer option identity, DFC option identity, browser Playwright harness, external engine, external dependency, broad UI redesign, broad Send Plan rewrite, legacy bridge, heuristic encoding policy, HTML policy change, or new conversion runtime family changed. | Yes. Appended this row and updated recovery context for DFC-53. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "redacts (POSIX absolute paths|http URLs|file URLs|common secret assignments|environment secret assignments|email identifiers|dotted token values)" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/db/repo/filePipelineRepo.test.ts infra/files/conversationAttachmentService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vue-tsc --noEmit --pretty false`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `git diff --check`; privacy/log scans. Browser Playwright was not run because harness scaffolding remains deferred. | Focused runtime redaction tests passed after fixing the new env-secret expectation gap: 7 selected tests. Focused repo/worker/service DFC Vitest passed: 3 files / 71 tests. Broader backend/client/UI DFC suite passed: 7 files / 379 tests. `vue-tsc` passed. `git diff --check` and production privacy/log scans passed before docs; final scans ran after docs. Plain `tsc` still fails only in the pre-existing Vue SFC named-export mismatch at `src/ui-app/app/appChatApp.logic.ts:183-184`, unrelated to this env-secret-redaction slice. Full `infra/files/derivativeJobService.test.ts` still has the separately documented HTML policy/test mismatch pending Owner decision. | This slice tightens runtime failure-message redaction only. It does not decide HTML conversion policy, implement heuristic encoding detection, user-visible encoding selection controls, browser smoke, external runtimes, or new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, HTML policy changes, or forbidden conversion runtime families. | DFC-54: continue with another narrow privacy/contract gap, resume the HTML policy decision path if Owner approves Option A/B/C, or prepare an owner-decision memo for browser harness/external runtime. |
| DFC-54 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Remove the plain `tsc` Vue SFC named-type blocker from the DFC validation chain. | `src/ui-app/components/SearchModal.types.ts`; `src/ui-app/components/ConversationList.types.ts`; `src/ui-app/components/SearchModal.vue`; `src/ui-app/components/ConversationList.vue`; `src/ui-app/app/appChatApp.logic.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Search modal and conversation list prop/list types now live in regular `.ts` type modules and are imported type-only by both the SFCs and `appChatApp.logic.ts`, so plain TypeScript no longer depends on named type exports from `.vue` modules. This restores full `npx tsc -p tsconfig.json --noEmit --pretty false` coverage for DFC work without changing runtime DFC behavior, DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, browser Playwright harness, external engine, external dependency, broad UI redesign, legacy bridge, heuristic encoding policy, HTML policy, or conversion runtime families. | Yes. Appended this row and updated recovery context for DFC-54. | `npm run rebuild:node`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `npx vue-tsc --noEmit --pretty false`; `npx vitest --run src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; renderer diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred. | Plain `tsc` passed. `vue-tsc` passed. Focused UI attachment Vitest passed: 1 file / 46 tests. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 379 tests. Renderer diff privacy/log scan found no added DTO/log/path/token exposure. `git diff --check` passed after docs with LF/CRLF warnings only. Full `infra/files/derivativeJobService.test.ts` still has the separately documented HTML policy/test mismatch pending Owner decision. | This slice removes a validation-chain blocker only. It does not decide HTML conversion policy, add browser smoke, implement external runtimes, change Send Plan compatibility, or add new conversion families. | No additional approval required. Owner decision remains appropriate before browser Playwright harness scaffolding, external engines/dependencies with security impact, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding policy, HTML policy changes, or forbidden conversion runtime families. | DFC-55: continue with a narrow contract/privacy gap, resume the HTML policy decision path if Owner approves Option A/B/C, or prepare an owner-decision memo for browser harness/external runtime. |
| DFC-55 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Implement owner-approved HTML dual-option policy with safe markdown and code conversions. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Owner approved Option C with corrected default strategy. HTML runtime now treats explicit `configJson.targetKind` as the authoritative conversion target, exposes safe markdown and code conversions for DFC HTML ensure, preserves source text for `code`, strips script/style execution semantics for safe markdown, emits HTML safety warnings for markdown, and uses a v1.2-style profile heuristic so static/document-like HTML defaults to `markdown` while template/script-heavy HTML defaults to `code`. DFC option generation now creates both `markdown` and `code` derived options for HTML while the existing `original_file` raw_file option remains available when raw-file rules allow it. Existing reusable text-derivative lookup now searches for the latest reusable derivative matching the requested target, avoiding duplicate generation when both HTML targets exist. No HTML->PDF, Chromium/Puppeteer, external resource loading, new dependency, browser Playwright harness, broad UI redesign, broad Send Plan rewrite, legacy bridge, or external conversion engine was added. | Yes. Appended this row and updated recovery context for DFC-55. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "HTML code and safe markdown|approved Phase 1 local text DFC target families|previews and commits selected TSV" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `npx tsc -p tsconfig.json --noEmit --pretty false`; `npx vue-tsc --noEmit --pretty false`; `git diff --check`; conversion/diagnostics/DTO/log privacy scans. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Full derivative job service Vitest passed: 1 file / 26 tests. Focused worker HTML/DFC option/preview tests passed: 1 file / 3 selected tests. Broader backend/client/UI DFC suite passed: 7 files / 380 tests. Plain `tsc` passed. `vue-tsc` passed. `git diff --check` passed with LF/CRLF warnings only. Privacy/log scan found no production DTO/log/path/token exposure; broader hits were internal source-hash/settings-hash handling or test fixtures with explicit non-leak assertions. | This slice resolves the prior HTML policy/test mismatch for Phase 1 text conversion. It does not implement HTML->PDF, browser rendering, external resource loading, external engines/dependencies, Playwright smoke, broad UI redesign, broad Send Plan rewrite, or additional HTML preference UI. Ambiguous HTML selection remains governed by existing DFC selectedOptionId flow unless a future preference/default-selection UI is approved. | Owner approval for Option C was received before implementation. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-56: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-56 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Lock HTML dual-option unselected state to `needs_user_selection`. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The HTML dual-option worker regression now also proves that after backend ensure exposes ready `original_file`, `markdown`, and `code` options, an unselected draft attachment still has `selectedOptionId: null`, empty `selectedAssetRefs`, and a `needs_user_selection` / `selected_option_missing` decision with no backend-invented default `targetKind` or refs. This preserves the Owner-approved Option C rule that ambiguous or not-yet-chosen HTML selection remains driven by explicit backend option identity rather than silent defaulting. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed. | Yes. Appended this row and updated recovery context for DFC-56. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "HTML code and safe markdown" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused HTML worker regression passed: 1 selected test. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 380 tests. `git diff --check` passed with LF/CRLF warnings only. Privacy/log scan only hit test fixture storage/source-hash strings covered by explicit non-leak assertions; no production DTO/log code changed. | This slice adds regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-57: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-57 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add HTML `code` selected preview/send same-source regression coverage. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The HTML dual-option worker regression now continues from safe `markdown` preview to explicit `code` selection, then proves selected preview reads the same `code` `derived_asset` ref and message commit records matching `usedOptionId`, `usedAssetRefs`, `targetKind: code`, and `sendStrategy: text_in_prompt`. This locks the Owner-approved HTML dual-option contract for both safe markdown and source-preserving code paths without backend-invented refs or renderer-side conversion identity. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed. | Yes. Appended this row and updated recovery context for DFC-57. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "HTML code and safe markdown" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused HTML worker regression passed: 1 selected test. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 380 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit only test fixture raw/derived refs and explicit non-leak assertions; no production DTO/log code changed. | This slice adds regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-58: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-58 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add HTML `original_file` selected raw preview regression coverage. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The HTML dual-option worker regression now explicitly selects the `original_file` option before switching to derived `markdown` and `code`, proving selected preview returns a `raw_file` metadata-only payload, preserves the selected raw_file ref, reports `sendStrategy: file_attachment`, and does not expose the raw storage URI, derived storage URIs, or HTML file body. This locks the first-class `original_file` path inside the HTML dual-option profile without creating a DerivedAsset or falling back to derived conversion identity. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed. | Yes. Appended this row and updated recovery context for DFC-58. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "HTML code and safe markdown" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused HTML worker regression passed: 1 selected test. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 380 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit only test fixture storage refs and explicit non-leak assertions; no production DTO/log code changed. | This slice adds regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-59: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-59 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add HTML `original_file` raw send snapshot regression coverage. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The HTML dual-option worker regression now attaches the same HTML asset to a second draft, selects the backend-issued `original_file` option, commits it, and proves the resulting MessageAttachment snapshot persists matching `usedOptionId`, `usedAssetRefs` with the raw_file ref, `targetKind: original_file`, and `sendStrategy: file_attachment`. This complements the raw preview coverage and locks first-class HTML `original_file` send binding without creating a DerivedAsset, falling back to derived conversion identity, or using renderer-invented refs. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed. | Yes. Appended this row and updated recovery context for DFC-59. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "HTML code and safe markdown" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused HTML worker regression passed: 1 selected test. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 380 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit only test fixture raw/derived refs and explicit non-leak assertions; no production DTO/log code changed. | This slice adds regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-60: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-60 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add HTML safe `markdown` send snapshot regression coverage. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The HTML dual-option worker regression now attaches the same HTML asset to a separate draft, selects the backend-issued safe `markdown` option, commits it, and proves the resulting MessageAttachment snapshot persists matching `usedOptionId`, `usedAssetRefs` with the markdown `derived_asset` ref, `targetKind: markdown`, and `sendStrategy: text_in_prompt`. This complements the selected markdown preview assertion and locks safe HTML markdown preview/send same-source behavior without renderer-invented conversion identity or leaking raw/derived storage details. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed. | Yes. Appended this row and updated recovery context for DFC-60. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "HTML code and safe markdown" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused HTML worker regression passed: 1 selected test. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 380 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit only test fixture raw/derived refs and explicit non-leak assertions; no production DTO/log code changed. | This slice adds regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-61: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-61 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add Send Plan regression for HTML DFC derived options without legacy fallback. | `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. Send Plan coverage now proves HTML raw assets with backend-selected `markdown` or `code` `derived_asset` refs are planned from DFC option/ref semantics as `text_in_prompt`, `mappedFromLegacy: false`, and `eligibility: included`, despite the raw asset extension/MIME being HTML. The regression asserts the plan does not fall back to legacy `conversion_required_before_send` or `unsupported` routing and does not expose raw/derived storage URIs or lineage hashes in the plan DTO. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed. | Yes. Appended this row and updated recovery context for DFC-61. | `npm run rebuild:node`; `npx vitest --run infra/files/sendPlanService.test.ts -t "DFC-managed HTML derived options" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; Send Plan diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused Send Plan regression passed: 1 selected test. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 381 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit only test fixture storage/hash fields and explicit non-leak assertions; no production DTO/log code changed. | This slice adds Send Plan regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-62: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-62 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Fix Send Plan compatibility for HTML `original_file` raw_file selection. | `infra/files/sendPlanService.ts`; `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. The new HTML `original_file` Send Plan regression exposed that `processingStatus: convertible` still forced `conversion_required_before_send` even when a backend-issued DFC `original_file` option selected the raw_file ref. Send Plan now treats `targetKind: original_file` plus `sendStrategy: file_attachment` as a ready raw-file semantic, so it does not require a derived text conversion before mode selection. Coverage proves HTML `original_file`, safe `markdown`, and `code` selections all plan from selected DFC refs without legacy HTML fallback. No DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed. | Yes. Appended this row and updated recovery context for DFC-62. | `npm run rebuild:node`; first `npx vitest --run infra/files/sendPlanService.test.ts -t "DFC-managed HTML" --reporter=dot --silent` exposed the blocked HTML `original_file` gap; after the fix the same focused command passed with 2 selected tests. Then `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; Send Plan production/test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused Send Plan HTML DFC tests passed after the fix: 2 selected tests. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 382 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan found the production diff only changes a semantic gate condition; storage/hash hits are test fixtures with explicit non-leak assertions. | This slice is a narrow Send Plan semantic fix plus regression coverage. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required because this is a narrow contract-alignment fix, not a broad Send Plan rewrite. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-63: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-63 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add Send Plan negative capability gate coverage for HTML `original_file`. | `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The HTML `original_file` Send Plan regression now also evaluates the same backend-issued raw_file selection against a text-only model. It proves the overall plan is blocked by model capability, the attachment remains DFC semantic `targetKind: original_file` / `sendStrategy: file_attachment` / `mappedFromLegacy: false`, the selected raw_file ref is preserved, and the note names missing `file_in` instead of falling back to legacy `conversion_required_before_send` or `unsupported` HTML routing. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-63. | `npm run rebuild:node`; first focused negative-gate assertion used the wrong per-attachment eligibility term and failed; after aligning with existing Send Plan semantics (`excluded` attachment, blocked overall plan), `npx vitest --run infra/files/sendPlanService.test.ts -t "DFC-managed HTML original_file" --reporter=dot --silent` passed. Then `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; Send Plan diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused Send Plan HTML original_file regression passed: 1 selected test. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 382 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit only DFC-62 production semantic gate plus test fixture storage/hash fields and explicit non-leak assertions. | This slice adds negative compatibility gate coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-64: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-64 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add Send Plan negative capability gate coverage for HTML derived options. | `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The HTML `markdown`/`code` Send Plan regression now also evaluates each backend-selected `derived_asset` option against a model without `text_in`. It proves the overall plan is blocked by model capability, each attachment remains DFC semantic `targetKind: markdown` or `code`, `sendStrategy: text_in_prompt`, and `mappedFromLegacy: false`, the selected derived_asset ref is preserved, and the note names missing `text_in` instead of falling back to legacy HTML `conversion_required_before_send` or `unsupported` routing. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-64. | `npm run rebuild:node`; `npx vitest --run infra/files/sendPlanService.test.ts -t "DFC-managed HTML" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; Send Plan diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused Send Plan HTML DFC regressions passed: 2 selected tests. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 382 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit only DFC-62 production semantic gate plus test fixture storage/hash fields and explicit non-leak assertions. | This slice adds negative compatibility gate coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-65: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-65 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add history Send Plan regression for HTML derived DFC snapshots. | `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. Send Plan coverage now creates historical MessageAttachment snapshots for HTML `markdown` and `code` derived options, then proves history planning preserves `targetKind`, `sendStrategy: text_in_prompt`, `mappedFromLegacy: false`, and the selected `derived_asset` refs from the message snapshot. The regression asserts historical HTML DFC attachments do not re-enter legacy HTML `conversion_required_before_send` or `unsupported` routing and do not expose raw/derived storage URIs or lineage hashes in the plan DTO. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-65. | `npm run rebuild:node`; `npx vitest --run infra/files/sendPlanService.test.ts -t "history HTML derived" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; Send Plan diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused history HTML DFC regression passed: 1 selected test. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 383 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit test fixture storage/hash fields and explicit non-leak assertions; no production code changed in this round. | This slice adds history Send Plan regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-66: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-66 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add history Send Plan regression for HTML `original_file` DFC snapshots. | `infra/files/sendPlanService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The existing history `original_file` Send Plan regression now covers both PDF and HTML raw_file snapshots. Coverage proves historical HTML `original_file` attachments preserve `usedOptionId`, selected raw_file refs, `targetKind: original_file`, `sendStrategy: file_attachment`, and `mappedFromLegacy: false` from the MessageAttachment snapshot instead of re-entering legacy HTML `conversion_required_before_send` or `unsupported` routing. The plan DTO must not expose the raw storage URI. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, conversion runtime, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-66. | `npm run rebuild:node`; `npx vitest --run infra/files/sendPlanService.test.ts -t "history original_file" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; Send Plan diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused history original_file regression passed: 1 selected test. Broader backend/client/UI DFC suite passed after node ABI rebuild: 7 files / 383 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit test fixture storage/hash fields and explicit non-leak assertions; no production code changed in this round. | This slice adds history Send Plan regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-67: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-67 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add template-like HTML runtime regression for the v1.2 code default heuristic. | `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The derivative runtime HTML default-code regression now covers both script-heavy and template-like HTML. Coverage proves template-like HTML such as `<template>` / `{{ }}` / `v-if` defaults to `targetKind: code`, has no conversion warnings, and writes the exact source text as the derived output instead of safe-markdown conversion. This complements the existing static/document-like default markdown and explicit markdown/code tests. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, broad HTML runtime change, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-67. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "html assets as code by default" --reporter=dot --silent`; `npx vitest --run infra/files/derivativeJobService.test.ts infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; derivative runtime test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused HTML runtime regression passed: 2 selected tests. Broader backend/client/UI DFC suite including full derivative runtime tests passed after node ABI rebuild: 8 files / 410 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit cumulative test fixture storage URIs, template literals, and existing redaction-test secret samples; no production DTO/log code changed in this round. | This slice adds runtime regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-68: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-68 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add template-like HTML option-generation regression for the owner-approved dual-option policy. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. Worker-level option generation now covers template-like HTML in addition to the static/document-like HTML fixture. Coverage proves `conversationDraft.ensureDfcOptions` leaves `selectedOptionId` null with `needs_user_selection`, exposes backend-owned `original_file` raw_file, safe `markdown` derived_asset, and `code` derived_asset options, keeps safe markdown diagnostics/warnings on the markdown option, writes template visible text through safe markdown, and preserves exact HTML source in the code derivative. DTO assertions ensure the ensure response does not expose raw storage URI, source hash, or original HTML body. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, broad HTML runtime change, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-68. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "template-like HTML DFC options" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/derivativeJobService.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; worker test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused worker regression passed: 1 selected test. Broader backend/client/UI DFC suite including full derivative runtime tests passed after node ABI rebuild: 8 files / 411 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit cumulative test fixture storage/hash fields, selected ref assertions, and explicit non-leak assertions; no production DTO/log code changed in this round. | This slice adds option-generation regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-69: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-69 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add template-like HTML selected `code` preview/send same-source regression. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The template-like HTML worker regression now selects the backend-issued `code` option and proves preview and message binding use the same selected `derived_asset` ref. Coverage checks `conversationDraft.getDfcPreview` returns `selectedOptionId`, `selectedAssetRefs`, `targetKind: code`, `sendStrategy: text_in_prompt`, and the exact code derivative text, then `conversationDraft.commitToUserMessage` snapshots matching `usedOptionId`, `usedAssetRefs`, `targetKind`, and `sendStrategy`. DTO assertions ensure preview/commit responses do not expose raw or derived storage URIs. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, broad HTML runtime change, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-69. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "template-like HTML DFC options" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/derivativeJobService.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; worker test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused worker regression passed: 1 selected test. Broader backend/client/UI DFC suite including full derivative runtime tests passed after node ABI rebuild: 8 files / 411 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit cumulative test fixture storage/hash fields, selected/used ref assertions, and explicit non-leak assertions; no production DTO/log code changed in this round. | This slice adds preview/send binding regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-70: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-70 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add template-like HTML selected safe `markdown` preview/send same-source regression. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The template-like HTML worker regression now also selects the backend-issued safe `markdown` option in a separate draft conversation and proves preview and message binding use the same selected `derived_asset` ref. Coverage checks `conversationDraft.getDfcPreview` returns `selectedOptionId`, `selectedAssetRefs`, `targetKind: markdown`, `sendStrategy: text_in_prompt`, and the safe markdown derivative text, then `conversationDraft.commitToUserMessage` snapshots matching `usedOptionId`, `usedAssetRefs`, `targetKind`, and `sendStrategy`. DTO assertions ensure preview/commit responses do not expose raw or derived storage URIs or the original HTML body. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, broad HTML runtime change, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-70. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "template-like HTML DFC options" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/derivativeJobService.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; worker test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused worker regression passed: 1 selected test. Broader backend/client/UI DFC suite including full derivative runtime tests passed after node ABI rebuild: 8 files / 411 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit cumulative test fixture storage/hash fields, selected/used ref assertions, and explicit non-leak assertions; no production DTO/log code changed in this round. | This slice adds preview/send binding regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | Paused by user request after DFC-70. Resume with DFC-71 when the owner asks to continue. |
| DFC-71 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add template-like HTML selected `original_file` preview/send same-source regression. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. After the owner resumed from the DFC-70 pause, the template-like HTML worker regression now also selects the backend-issued `original_file` option in a separate draft conversation and proves preview and message binding use the same selected `raw_file` ref. Coverage checks `conversationDraft.getDfcPreview` returns `selectedOptionId`, `selectedAssetRefs`, `targetKind: original_file`, `sendStrategy: file_attachment`, and metadata-only raw preview diagnostics, then `conversationDraft.commitToUserMessage` snapshots matching `usedOptionId`, `usedAssetRefs`, `targetKind`, and `sendStrategy`. DTO assertions ensure preview/commit responses do not expose raw or derived storage URIs or the original HTML body. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, broad HTML runtime change, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-71. | `npm run rebuild:node`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "template-like HTML DFC options" --reporter=dot --silent`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/derivativeJobService.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; worker test diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused worker regression passed: 1 selected test. Broader backend/client/UI DFC suite including full derivative runtime tests passed after node ABI rebuild: 8 files / 411 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit cumulative test fixture storage/hash fields, selected/used ref assertions, and explicit non-leak assertions; no production DTO/log code changed in this round. | This slice adds preview/send binding regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-72: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-72 | 2026-05-25 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Tighten safe HTML markdown handling for external image resources. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Safe HTML markdown conversion now rewrites `<img>` tags to their `alt` text only, preserving visible/semantic text while dropping external `src` URLs from the derived markdown output. The static/document-like HTML runtime regression now includes an external image URL and proves the output keeps the `alt` text, removes the remote URL/domain, does not execute script/style semantics, and still carries safe HTML warnings. No DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, broad HTML runtime change, external resource loading, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-72. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "static document-like html" --reporter=dot --silent`; `npx vitest --run infra/files/derivativeJobService.test.ts infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; derivative runtime diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused safe HTML runtime regression passed: 1 selected test. Broader backend/client/UI DFC suite including full derivative runtime tests passed after node ABI rebuild: 8 files / 411 tests. `git diff --check` passed after docs with LF/CRLF warnings only. Privacy/log scan hit the intentional external URL fixture, explicit non-leak assertions, and cumulative redaction-test samples; no DTO/log code changed in this round. | This slice tightens safe markdown conversion only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required because this uses existing no-dependency string-level safe conversion and does not load external resources. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-73: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-73 | 2026-05-26 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Add safe HTML markdown regression for external link attributes. | `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests only. The static/document-like HTML runtime regression now includes an external `<a href>` URL with a token-like query value and proves safe markdown preserves the visible link text while dropping the URL, remote domain, and token from the derived output. This complements DFC-72 image-resource handling and keeps the no-JS/no-external-resource boundary covered for common link attributes. No production code, DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, broad HTML runtime change, external resource loading, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-73. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "static document-like html" --reporter=dot --silent`; `npx vitest --run infra/files/derivativeJobService.test.ts infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; derivative runtime diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused safe HTML runtime regression passed: 1 selected test. Broader backend/client/UI DFC suite including full derivative runtime tests passed after node ABI rebuild: 8 files / 411 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit the intentional external link/image URL fixtures, explicit non-leak assertions, and cumulative redaction-test samples; no production DTO/log code changed in this round. | This slice adds safe markdown regression coverage only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required because this uses existing no-dependency string-level safe conversion and does not load external resources. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-74: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-74 | 2026-05-26 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Preserve basic HTML list semantics in safe markdown output. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Safe HTML markdown conversion now maps `<li>` items to markdown bullet lines, preserving basic visible list semantics instead of flattening list item text into plain adjacent words. The static/document-like HTML runtime regression now includes a list and asserts the derived output contains `- First item` and `- Second item` while still stripping script/style semantics and external URL attributes. No DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, broad HTML runtime change, external resource loading, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-74. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "static document-like html" --reporter=dot --silent`; `npx vitest --run infra/files/derivativeJobService.test.ts infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; derivative runtime diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused safe HTML runtime regression passed: 1 selected test. Broader backend/client/UI DFC suite including full derivative runtime tests passed after node ABI rebuild: 8 files / 411 tests. `git diff --check` passed before docs with LF/CRLF warnings only. Privacy/log scan hit intentional safe markdown fixture text, external URL non-leak assertions, and cumulative redaction-test samples; no DTO/log code changed in this round. | This slice tightens safe markdown conversion only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required because this uses existing no-dependency string-level safe conversion and does not load external resources. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-75: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-75 | 2026-05-26 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; final hash in completion report | Preserve basic HTML blockquote semantics in safe markdown output. | `infra/files/derivativeJobService.ts`; `infra/files/derivativeJobService.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Safe HTML markdown conversion now maps `<blockquote>` to markdown quote text, preserving a basic visible document semantic while keeping conversion string-level, dependency-free, non-executing, and external-resource-free. The static/document-like HTML runtime regression now includes a blockquote and asserts the derived output contains `> Important note` while retaining prior script/style stripping, external URL non-leak, image alt, link text, and list semantics checks. No DB schema, IPC shape, renderer option identity, DFC option identity, Send Plan policy, UI, browser Playwright harness, external engine, external dependency, broad HTML runtime change, external resource loading, or HTML->PDF behavior changed in this round. | Yes. Appended this row and updated recovery context for DFC-75. | `npm run rebuild:node`; `npx vitest --run infra/files/derivativeJobService.test.ts -t "static document-like html" --reporter=dot --silent`; `npx vitest --run infra/files/derivativeJobService.test.ts infra/db/worker.filePipeline.test.ts infra/files/sendPlanService.test.ts infra/files/conversationAttachmentService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts src/ui-app/AppChatApp.attachments.test.ts --reporter=dot --silent`; `git diff --check`; derivative runtime diff privacy/log scan. Browser Playwright was not run because harness scaffolding remains deferred by Owner. | Focused safe HTML runtime regression passed: 1 selected test. Broader backend/client/UI DFC suite including full derivative runtime tests passed after node ABI rebuild: 8 files / 411 tests. Privacy/log scan hit intentional safe markdown fixture text plus existing URL/token/storage/hash fixture assertions; no DTO/log code changed in this round. `git diff --check` passed after docs with LF/CRLF warnings only. | This slice tightens safe markdown conversion only. It does not add ambiguous-HTML preference UI, HTML->PDF, browser rendering, external engines/dependencies, broad UI redesign, broad Send Plan rewrite, or Playwright smoke. | No additional approval required because this uses existing no-dependency string-level safe conversion and does not load external resources. Separate approval remains required before browser Playwright harness scaffolding, external engines/dependencies with security impact, HTML->PDF, broad UI redesign, broad Send Plan rewrite, legacy compatibility bridge, heuristic encoding preference UI, or forbidden conversion runtime families. | DFC-76: continue with another narrow DFC contract/runtime gap, or prepare an owner-decision memo for the next larger runtime/browser step. |
| DFC-M1 | 2026-05-26 | `docs/dfc-0-format-conversion-foundation` | Uncommitted; checkpoint skipped because unrelated `.codex/agents/*.toml` files are dirty | Close out DFC Phase 1 into a manual-supervision supported matrix and next-decision map. | `docs/file-pipeline/document-format-conversion/dfc-m1-phase-1-closeout-supported-matrix.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. Documentation only. No runtime, DB schema, Send Plan flow, asset model, IPC shape, UI, dependency, Playwright harness, external engine, or legacy bridge changes. | Yes. Added the DFC-M1 closeout matrix and appended durable recovery notes. | `git status --short --branch`; `git rev-parse --short HEAD`; targeted read-only DFC vocabulary/runtime/Send Plan inspection; `git diff --check`. No tests run because this is docs-only. | `git diff --check` passed with LF/CRLF warnings only. | Worktree contains unrelated `.codex/agents/*.toml` modifications, so no checkpoint commit should be created from this state. Phase 1 still lacks owner-approved browser/Electron end-to-end smoke and next runtime-family decisions. | Yes before any Playwright/Electron harness, XLSX/XLS runtime, DOCX/Office runtime, HTML->PDF, Office->PDF, external engine sandbox, dependency, broad Send Plan rewrite, DB schema change, or IPC expansion. | M2: End-to-End confidence path owner decision. M3: Next runtime family pilot owner decision after confidence path scope is fixed. |
| DFC-M2 | 2026-05-26 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; M1 checkpoint is `ce56da8` | Establish the minimal DFC Phase 1 end-to-end confidence path without adding Playwright/Electron scaffold. | `src/ui-app/AppChatApp.attachments.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No production code. Strengthened an existing Vitest/jsdom UI smoke mock to prove the selected markdown DFC option reaches preview and Send Plan-visible semantic/ref state. No DB schema, asset model, IPC shape, Send Plan main flow, dependency, Playwright harness, Electron launcher, runtime family, or UI feature changed. | Yes. Appended this row and durable recovery notes. | `git diff --check`; `npx vue-tsc --noEmit --pretty false`; `npx vitest --run src/ui-app/AppChatApp.attachments.test.ts -t "loads backend DFC options and updates the selected option from attachment details" --reporter=dot --silent`. | Passed. Targeted Vitest: 1 test passed, 45 skipped in `src/ui-app/AppChatApp.attachments.test.ts`. `vue-tsc` passed after narrowing the test mock array type. `git diff --check` passed with LF/CRLF warning only. | This is not a real browser or Electron launch. It does not cover OS file picker, real upload persistence, production DB worker execution, real Send Plan service execution, Playwright automation, CI integration, or new runtime families. | Yes before real Playwright/Electron harness, XLSX/XLS runtime, DOCX/Office runtime, HTML->PDF, Office->PDF, external engine sandbox, dependency, broad Send Plan rewrite, DB schema change, or IPC expansion. | M3: Next Runtime Family Pilot only after owner accepts the Vitest/jsdom confidence path or separately approves a true Browser/Electron harness package. |
| DFC-M3 | 2026-05-26 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; baseline checkpoint is `5e22ab8` | Choose the next runtime family after M1/M2 and decide whether a low-risk pilot can be implemented. | `docs/file-pipeline/document-format-conversion/dfc-m3-next-runtime-family-owner-memo.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. Documentation only. No runtime, dependency, DB schema, Send Plan flow, IPC shape, asset model, UI, Playwright/Electron harness, external engine, or legacy bridge changed. | Yes. Added M3 owner memo and appended durable recovery notes. | `git status --short --branch`; dependency keyword scan over `package.json` / `package-lock.json`; read-only code seam inspection; code_mapper; doc_consistency; `git diff --check`. No tests run because this is docs-only. | `git diff --check` passed with LF/CRLF warnings only. | XLSX/XLS -> `table_markdown` is the recommended next runtime family, but no low-risk pilot is possible without an owner-approved parser dependency or equivalent engine. Existing CSV/TSV support is text-delimiter based and Office workbook formats are explicitly not implemented. | Yes before adding XLSX/XLS parser dependencies, external engines, browser rendering, DB schema changes, Send Plan main-flow changes, IPC expansion, or workbook feature productization. | DFC-M4: XLSX/XLS parser dependency decision and, only if approved, a backend-only XLSX-first `table_markdown` pilot. |
| DFC-M4 | 2026-05-26 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; baseline checkpoint is `e9c8b77` | Implement owner-approved XLSX-first backend-only `table_markdown` parser pilot using ExcelJS. | `package.json`; `package-lock.json`; `infra/files/derivativeJobService.ts`; `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `.xlsx` local stored assets can now generate a DFC `table_markdown` derived option via backend-owned ensure/preview/selected refs/Send Plan paths. `.xls` remains unsupported. No DB schema, IPC shape, Send Plan main-flow, asset model, renderer UI, Playwright/Electron harness, external engine, or legacy bridge changed. | Yes. Appended durable recovery notes. | `npm install exceljs` after one transient `ECONNRESET` retry; `npm run rebuild:node`; `git diff --check`; `npx vue-tsc --noEmit --pretty false`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts --reporter=dot --silent`. | Passed. Targeted Vitest passed: 6 files, 340 tests. `vue-tsc` passed. `git diff --check` passed with LF/CRLF warnings only. Node ABI rebuild succeeded and current ABI target is node. | ExcelJS adds parser dependency footprint and transitive packages. Pilot only handles visible worksheet table extraction with basic guards and warnings; it does not support `.xls`, formula evaluation, macros, images/charts extraction, hidden sheet UI, merged-cell productization, pagination, sheet selection UI, DOCX/Office, HTML->PDF, Office->PDF, PS/EPS, or external engines. | Yes before `.xls`, DOCX/Office, HTML->PDF, Office->PDF, PS/EPS, external engines, DB schema, IPC expansion, Send Plan rewrite, renderer UI sheet selection, formula evaluation, pagination, or broader workbook productization. | DFC-M5: checkpoint or harden XLSX pilot with targeted dependency/security review and guard/warning coverage; do not expand runtime family without owner decision. |
| DFC-M5 | 2026-05-26 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; baseline checkpoint is `03c7486` | Harden the XLSX-first backend-only `table_markdown` pilot dependency and parser boundaries. | `infra/files/derivativeJobService.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added an XLSX worksheet-count guard, strengthened markdown escaping for worksheet headings, and added targeted worker tests for markdown escaping, worksheet guard fail-closed behavior, malformed workbook fail-closed behavior, and the existing `.xls` unsupported boundary. No dependency, DB schema, IPC shape, Send Plan main-flow, asset model, renderer UI, Playwright/Electron harness, external engine, or legacy bridge changed. | Yes. Appended durable recovery notes. | ExcelJS package-lock footprint inspection; `npm run rebuild:node`; `git diff --check`; `npx vue-tsc --noEmit --pretty false`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "XLSX\|legacy XLS" --reporter=dot --silent`. | Passed. Targeted Vitest passed: 1 file, 5 selected tests, 30 skipped. `vue-tsc` passed. `git diff --check` passed with LF/CRLF warnings only. Node ABI rebuild succeeded and current ABI target is node. | ExcelJS remains a backend-only parser dependency for the `.xlsx` pilot. Lockfile inspection found no ExcelJS native binary, browser rendering dependency, external engine, or ExcelJS postinstall hook. Residual hygiene risk is transitive package footprint and deprecated transitive packages inherited through the ExcelJS stack; this does not expand renderer or runtime-family scope. `.xls` remains unsupported. | Yes before `.xls`, second parser, DOCX/Office, HTML->PDF, Office->PDF, PS/EPS, external engines, DB schema, IPC expansion, Send Plan rewrite, UI sheet selection, formula evaluation, pagination, or broader spreadsheet productization. | Next owner decision: either continue spreadsheet hardening with similarly targeted guards/tests, or switch to a DOCX/Office runtime owner-decision memo. |
| DFC-M6 | 2026-05-26 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; baseline checkpoint is `c6cf2c9` | Decide the DOCX/Office markdown runtime strategy and add XLSX follow-up regression tests without expanding XLSX behavior. | `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/dfc-m6-docx-office-markdown-runtime-owner-memo.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Tests and docs only. Added XLSX regressions for visible worksheet output order with hidden-sheet warning stability and formula cells without cached values producing fail-safe empty output plus warning. Added DOCX/Office owner memo recommending a DOCX-first backend-only markdown pilot only after owner approves a parser dependency. No runtime implementation, dependency, DB schema, IPC shape, Send Plan main-flow, asset model, UI, Playwright/Electron harness, external engine, `.doc`, `.rtf`, Office-to-PDF, HTML-to-PDF, PS/EPS, or legacy bridge changed. | Yes. Added M6 owner memo and durable recovery notes. | `git status --short --branch`; `git rev-parse --short HEAD`; dependency keyword scan over `package.json` and `package-lock.json`; DFC markdown seam keyword scan; `npm run rebuild:node`; `git diff --check`; `npx vitest --run infra/db/worker.filePipeline.test.ts -t "XLSX\|legacy XLS" --reporter=dot --silent`. | Passed. Targeted Vitest passed: 1 file, 7 selected tests, 30 skipped. `git diff --check` passed with LF/CRLF warnings only. Node ABI rebuild succeeded and current ABI target is node. | DOCX/Office remains blocked on an owner dependency decision because no Mammoth, Turndown, Pandoc, LibreOffice, or equivalent wrapper exists in current dependencies. XLSX remains a backend-only pilot; follow-up tests do not add product features. | Yes before adding Mammoth/Turndown or any DOCX parser dependency, `.doc`, `.rtf`, Office-to-PDF, HTML-to-PDF, PS/EPS, external engines, DB schema, IPC expansion, Send Plan rewrite, asset model changes, UI changes, browser/Electron harness, or broader runtime-family implementation. | Owner decision: approve or reject a DOCX-first Mammoth pilot. If not approved, continue spreadsheet hardening only. |
| DFC-M7 | 2026-05-27 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; baseline checkpoint is `d1a20bf` | Implement owner-approved DOCX-first backend-only `markdown` parser pilot using Mammoth. | `package.json`; `package-lock.json`; `infra/files/derivativeJobService.ts`; `infra/db/worker/handlers/filePipelineHandlers.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/dfc-m1-phase-1-closeout-supported-matrix.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. `.docx` local stored assets can now generate a DFC `markdown` derived option via backend-owned ensure/preview/selected refs/Send Plan paths. Mammoth converts DOCX to semantic HTML and the existing internal safe HTML markdown path strips tags/resources into markdown text. `.doc` and `.rtf` remain unsupported. No DB schema, IPC shape, Send Plan main-flow, asset model, renderer UI, Playwright/Electron harness, external engine, Turndown, Pandoc, LibreOffice, Office-to-PDF, HTML-to-PDF, PS/EPS, or legacy bridge changed. | Yes. Updated supported matrix and appended durable recovery notes. | `npm install mammoth`; package-lock footprint inspection; `npm run rebuild:node`; `git diff --check`; `npx vue-tsc --noEmit --pretty false`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts --reporter=dot --silent`. | Passed. Targeted Vitest passed: 6 files, 347 tests. `vue-tsc` passed after using namespace import for Mammoth. `git diff --check` passed with LF/CRLF warnings only. Node ABI rebuild succeeded and current ABI target is node. | Mammoth adds a BSD-2-Clause parser dependency and transitive packages, plus a CLI bin entry, but no native binary, browser rendering dependency, external engine, or postinstall hook was observed in the lockfile entry. DOCX pilot supports ordinary paragraphs, headings, and link visible text through backend-only conversion; hyperlink targets are omitted from output. Images, comments, revisions, headers/footers, footnotes/endnotes, complex tables/layout, macros, embedded objects, external resources, and Office/PDF conversion are not productized. | Yes before `.doc`, `.rtf`, Turndown, Pandoc, LibreOffice, Office-to-PDF, HTML-to-PDF, PS/EPS, external engines, DB schema, IPC expansion, Send Plan rewrite, asset model changes, UI changes, browser/Electron harness, image extraction, tracked-change/comment semantics, or broader Office productization. | Next: harden DOCX pilot dependency/parser boundaries with targeted malformed/embedded-resource tests, or pause for an owner decision on whether DOCX pilot should remain Phase 1 supported pilot. |
| DFC-M8 | 2026-05-27 | `docs/dfc-0-format-conversion-foundation` | Uncommitted at ledger write; baseline checkpoint is `b888c73` | Harden the DOCX-first backend-only `markdown` parser pilot without changing functional semantics. | `infra/files/derivativeJobService.ts`; `infra/db/worker.filePipeline.test.ts`; `docs/file-pipeline/document-format-conversion/dfc-m1-phase-1-closeout-supported-matrix.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | Yes. Added stable symbolic DOCX image omission warnings and targeted worker regressions for malformed DOCX fail-closed behavior, embedded media/resource omission, sanitized DTO/privacy boundaries, hyperlink target omission through the existing DOCX flow, and continued `.doc` / `.rtf` unsupported behavior. No dependency, DB schema, IPC shape, Send Plan main-flow, asset model, renderer UI, Playwright/Electron harness, external engine, Turndown, Pandoc, LibreOffice, Office-to-PDF, HTML-to-PDF, PS/EPS, layout fidelity, image extraction, comment/revision productization, or legacy bridge changed. | Yes. Updated supported matrix and durable recovery notes. | `npm run rebuild:node`; `git diff --check`; `npx vue-tsc --noEmit --pretty false`; `npx vitest --run infra/db/worker.filePipeline.test.ts infra/files/conversationAttachmentService.test.ts infra/files/sendPlanService.test.ts src/shared/files/documentFormatConversion.test.ts src/next/ipc/contracts/dbBridgeContracts.test.ts src/next/files/conversationDraftClient.test.ts --reporter=dot --silent`. | Passed. Targeted Vitest passed: 6 files, 349 tests. `vue-tsc` passed. `git diff --check` passed with LF/CRLF warnings only. Node ABI rebuild succeeded and current ABI target is node. | DOCX pilot remains backend-only and semantic-text oriented. Mammoth is used as a library only; the CLI bin is not invoked, and no shell/external process/browser rendering path is added. Parser diagnostics remain symbolic (`docx_*`) rather than raw Mammoth warning text. | Yes before `.doc`, `.rtf`, Turndown, Pandoc, LibreOffice, Office-to-PDF, HTML-to-PDF, PS/EPS, external engines, DB schema, IPC expansion, Send Plan rewrite, asset model changes, UI changes, browser/Electron harness, image extraction, tracked-change/comment semantics, or broader Office productization. | Treat DOCX as a Phase 1 backend-only supported pilot. Next choose either another narrow DOCX hardening slice or an owner decision for Office/PDF/external-engine boundaries. |
| DFC-M9 | 2026-05-29 | `docs/dfc-0-format-conversion-foundation` | Baseline `20c7162`; docs-only closeout, uncommitted at ledger write | Close out the current DFC runtime pilot expansion stage after XLSX/DOCX pilots and Safety Gate 1/2. | `docs/file-pipeline/document-format-conversion/dfc-m9-runtime-pilot-closeout.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. Documentation only. No runtime, dependency, DB schema, IPC shape, Send Plan flow, asset model, UI, Playwright/Electron harness, external engine, packaging, npm audit, ESLint, or legacy bridge change. | Yes. Added M9 runtime pilot closeout and appended durable recovery notes. | `git status --short --branch`; targeted `rg` extraction over M1-M8/Safety Gate documentation; `git diff --check`. No tests run because this is docs-only. | `git diff --check` passed with LF/CRLF warnings only. | Runtime pilots are closed as Phase 1 backend-only supported pilots, not full Office/workbook productization. Safety-Gate-2 found no P0/P1 and Gate-3 is skipped. Remaining risk is owner-gated packaged smoke confidence and future heavy-runtime/external-engine decisions. | Yes before Browser/Electron packaged smoke implementation, Office-to-PDF, HTML-to-PDF, PS/EPS, external engine sandbox, `.xls`, `.doc`, `.rtf`, new dependencies, DB schema, IPC shape, Send Plan main-flow, asset-model, UI, or legacy bridge changes. | Next owner choice: Packaging / smoke confidence first, or Heavy runtime owner decision for Office/PDF/PS-EPS/external-engine sandbox. |
| DFC-M10 | 2026-05-29 | `docs/dfc-0-format-conversion-foundation` | Baseline `38a81ac`; docs-only owner memo, uncommitted at ledger write | Decide whether a low-intrusion real Electron/browser/packaged smoke can be added after Phase 1 runtime pilot closeout. | `docs/file-pipeline/document-format-conversion/dfc-m10-packaging-smoke-confidence-owner-memo.md`; `docs/file-pipeline/document-format-conversion/progress-ledger.md`; `docs/file-pipeline/document-format-conversion/important-context.md` | No. Documentation only. No smoke harness, runtime, dependency, DB schema, IPC shape, Send Plan flow, asset model, UI, CI, packaged installer, external engine, npm audit, ESLint, or legacy bridge change. | Yes. Added M10 owner memo and appended durable recovery notes. | `git status --short --branch`; `git rev-parse --short HEAD`; targeted `rg` and config/script reads for package scripts, Vite/Vitest/Electron config, existing smoke scripts, Playwright dependency, e2e tests, and preload/UI seams; `git diff --check`. No tests run because this is docs-only. | `git diff --check` passed with LF/CRLF warnings only. | Existing `tc14` is an OpenRouter network smoke, `tests/e2e` are Vitest fixture replay, and no Playwright config, Electron launch helper, packaged smoke runner, or app readiness helper was found. A real smoke needs a dedicated owner package rather than a low-intrusion M10 addition. | Yes before adding an Electron/Playwright/packaged smoke harness, package scripts, CI wiring, app bootstrap hooks, OS file picker automation, heavy runtime, external engine, DB schema, IPC shape, Send Plan main-flow, asset-model, or UI changes. | DFC-M11: owner-approved Electron smoke harness package, starting with app shell plus scoped preload; add DFC attachment UI seam only if it stays low-intrusion. |
