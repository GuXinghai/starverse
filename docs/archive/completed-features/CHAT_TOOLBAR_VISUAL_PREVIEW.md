# ChatToolbar 视觉效果预览

## 完整工具栏外观

```
┌────────────────────────────────────────────────────────────────────────────────┐
│  [📎] [📄 PDF Text ▼] [🖼️]  │  [🎨 图像生成] [画面比例 ─●───]               │
│                               │  [🌍 网络搜索 ▼] [💡 推理] [⚙️ 参数]          │
│                               │                           [●增强模式]  [⋮]     │
└────────────────────────────────────────────────────────────────────────────────┘
   ↑ 附件工具组               ↑ 高级功能胶囊组                     ↑ 状态区
```

## 按钮状态对比

### 1. 图像生成按钮

**未启用**:
```
┌─────────────────┐
│  🎨  图像生成   │  ← 白色背景，灰色边框
└─────────────────┘
```

**已启用**:
```
┌─────────────────┐
│  🎨  图像生成   │  ← 紫粉渐变背景，白色文字
└─────────────────┘
     ↓
┌──────────────────────────────┐
│  正方形  ─────●──────        │  ← 内联比例滑块
└──────────────────────────────┘
```

### 2. 网络搜索按钮（胶囊式）

**未启用**:
```
┌─────────────┬───┐
│  🌍 网络搜索 │ ▼ │  ← 白色背景，灰色边框
└─────────────┴───┘
```

**已启用**:
```
┌─────────────┬───┐
│  🌍 网络搜索 │ ▼ │  ← 翠绿渐变背景，白色文字
└─────────────┴───┘
```

### 3. PDF 引擎选择器

**按钮**:
```
┌──────────────────┐
│  📄 PDF Text  ▼  │  ← 白色背景，小字体
└──────────────────┘
```

**下拉菜单**:
```
┌────────────────────────────────┐
│  📄  PDF Text            ✓     │  ← 当前选中（蓝色背景）
│      免费，文本为主              │
├────────────────────────────────┤
│  🔍  Mistral OCR                │
│      扫描件/图片，$2/千页       │
├────────────────────────────────┤
│  ⚡  Native                     │
│      模型原生，按 tokens 计费   │
└────────────────────────────────┘
```

## 响应式变化

### 桌面端（>= 1024px）
```
[📎] [📄 PDF Text ▼] [🖼️]  │  [🎨] [1:1 ─●─] [🌍▼] [💡] [⚙️]  │  [●增强模式]
```

### 平板端（640px - 1023px）
```
[📎] [📄▼] [🖼️]  │  [🎨] [1:1 ─●─] [🌍▼]  │  [●增强] [⋮]
                                              │
                                              ↓ 更多菜单
                                          [💡 推理控制]
                                          [⚙️ 采样参数]
```

### 移动端（< 640px）
```
[📎] [📄▼] [🖼️]  │  [🎨] [🌍▼]  │  [⋮]
                                  │
                                  ↓ 更多菜单
                              [💡 推理控制]
                              [⚙️ 采样参数]
```

## 交互动画

### 1. 按钮悬停
```
┌──────────┐          ┌──────────┐
│  按钮    │  hover → │  按钮    │  ← 背景色变深
└──────────┘          └──────────┘     阴影增强
```

### 2. 滑块拖动
```
正方形  ────○────           正方形  ──────○──
        ↓ 拖动                         ↓
宽屏    ──────────○─        宽屏    ──────────●─  ← 滑块放大
```

### 3. 增强模式指示器
```
┌──────────────┐
│  ● 增强模式  │  ← 绿点脉冲动画（2s 循环）
└──────────────┘
```

## 颜色渐变细节

### 图像生成按钮
```
  紫色 #a855f7 ──────→ 粉色 #ec4899
┌────────────────────────────────┐
│  🎨  图像生成                   │
└────────────────────────────────┘
```

### 网络搜索按钮
```
  翠绿 #10b981 ──────→ 青绿 #14b8a6
┌────────────────────────────────┐
│  🌍  网络搜索                   │
└────────────────────────────────┘
```

### 滑块轨道
```
  紫色 #a855f7 ──────→ 粉色 #ec4899
━━━━━━━━━●━━━━━━━━━
         ↑ 白色滑块 + 紫色边框
```

## 阴影与深度

```
工具栏背景: backdrop-filter: blur(8px);
          ← 毛玻璃效果

按钮阴影:   box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
          ← 微妙立体感

下拉菜单:   box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
          ← 明显浮起效果
```

## 间距与尺寸

```
工具栏高度:   py-2  (8px 上下)
按钮高度:     h-auto (内容自适应)
图标尺寸:     w-5 h-5 (20px × 20px)
间距:         gap-3 (12px)
内边距:       px-2 (8px 左右)
圆角:         rounded-lg (8px)
```

## 文字大小

```
主按钮文字:   text-sm (14px)
PDF 引擎:     text-xs (12px)
状态指示:     text-xs (12px)
图标:        w-4 h-4 (16px)
```

## 实际效果图说明

由于这是纯文本文档，实际效果需要在浏览器中查看。关键视觉特征：

1. **紧凑单行** - 所有元素水平排列，不换行
2. **视觉分组** - 分隔线清晰区分功能区
3. **渐变美学** - 启用状态使用渐变色，视觉吸引力强
4. **微交互** - 悬停、点击有明显反馈
5. **毛玻璃** - 背景半透明，现代感十足

---

**查看实际效果**: 运行 `npm run dev` 并打开应用查看完整视觉效果。
